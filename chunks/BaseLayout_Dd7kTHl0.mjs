import { q as createComponent, D as renderTemplate, u as defineScriptVars, o as addAttribute, v as maybeRenderHead, p as createAstro, w as renderComponent, C as renderSlot, x as renderHead, K as unescapeHTML } from './astro/server_D37m5tNR.mjs';
import 'piccolore';
/* empty css                         */
import 'clsx';
import { g as getCollection } from './_astro_content_XxXwbPTn.mjs';

var __freeze$1 = Object.freeze;
var __defProp$1 = Object.defineProperty;
var __template$1 = (cooked, raw) => __freeze$1(__defProp$1(cooked, "raw", { value: __freeze$1(raw || cooked.slice()) }));
var _a$1;
const $$MAi = createComponent(async ($$result, $$props, $$slots) => {
  const base = "/".endsWith("/") ? "/" : "//";
  return renderTemplate(_a$1 || (_a$1 = __template$1(["", '<div id="mai-root" class="print:hidden"> <!-- Backdrop (mobile only — desktop keeps the page interactive behind the\n       panel so M.ai can guide the user through the UI bidirectionally). --> <div id="mai-backdrop" hidden class="fixed inset-0 z-40 bg-ink/30 backdrop-blur-[1px] transition-opacity md:hidden"></div> <!-- Right-side drawer panel. On mobile it floats over the page; on\n       desktop it claims a 24rem rail and the page shifts left to make\n       room (see html.mai-open rule below). --> <aside id="mai-panel" role="dialog" aria-modal="true" aria-labelledby="mai-title" hidden class="fixed top-0 right-0 z-50 h-full w-full md:w-[24rem] bg-paper border-l border-ink/10 shadow-2xl shadow-ink/20 md:shadow-none flex flex-col"> <header class="flex items-start justify-between gap-2 border-b border-ink/10 px-4 py-4 bg-cream"> <div> <h2 id="mai-title" class="mai-panel-title font-serif italic text-2xl leading-none"><span class="mai-wordmark">M<span class="mai-dot">.</span>ai</span></h2> <p class="mt-1.5 text-[11px] text-muted">A thoughtful guide to Flora&rsquo;s work, writing, and ideas. Ask anything &mdash; I&rsquo;ll read across the site and synthesize, with citations.</p> </div> <div class="flex items-center gap-1"> <button id="mai-expand" type="button" aria-label="Expand M.ai to full screen" aria-pressed="false" title="Expand" class="rounded p-1.5 text-muted hover:text-accent transition-colors hidden md:inline-flex"> <svg id="mai-expand-icon-expand" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <polyline points="15 3 21 3 21 9"></polyline> <polyline points="9 21 3 21 3 15"></polyline> <line x1="21" y1="3" x2="14" y2="10"></line> <line x1="3" y1="21" x2="10" y2="14"></line> </svg> <svg id="mai-expand-icon-collapse" hidden width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <polyline points="4 14 10 14 10 20"></polyline> <polyline points="20 10 14 10 14 4"></polyline> <line x1="14" y1="10" x2="21" y2="3"></line> <line x1="3" y1="21" x2="10" y2="14"></line> </svg> </button> <button id="mai-speak-toggle" type="button" aria-label="Read replies aloud" aria-pressed="false" title="Read replies aloud" class="rounded p-1.5 text-muted hover:text-accent transition-colors"> <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon> <path d="M15.54 8.46a5 5 0 0 1 0 7.07"></path> </svg> </button> <button id="mai-close" type="button" aria-label="Close M.ai" class="rounded p-1.5 text-muted hover:text-accent transition-colors"> <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="6" y1="6" x2="18" y2="18"></line> <line x1="6" y1="18" x2="18" y2="6"></line> </svg> </button> </div> </header> <div id="mai-classic-nav" hidden class="px-4 py-2 border-b border-ink/10 bg-paper"> <p class="text-[10px] uppercase tracking-widest text-muted">Prefer traditional UX?</p> <div class="mt-1.5 flex flex-wrap gap-1.5"> <a', ' class="mai-classic-link">Home</a> <a', ' class="mai-classic-link">Toybox</a> <a', ' class="mai-classic-link">Work</a> <a', ' class="mai-classic-link">Play</a> <a', ' class="mai-classic-link">Writing</a> <a', ` class="mai-classic-link">About</a> </div> </div> <!-- Topic grid: shown on first open, hidden after the user asks something. --> <div id="mai-topics" class="px-4 py-3 border-b border-ink/5"> <p class="text-[11px] uppercase tracking-widest text-muted">Start exploring</p> <p class="mt-1 text-[11px] text-muted italic">Ask me about ideas, frameworks, or specific work. I'll search Flora's site and synthesize with citations.</p> <div class="mt-2 grid grid-cols-2 gap-2"> <button type="button" data-q="Who is Flora?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Who is Flora</span> <span class="mt-0.5 block text-[11px] text-muted">Her design approach</span> </button> <button type="button" data-q="What playbooks exist?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Playbooks</span> <span class="mt-0.5 block text-[11px] text-muted">Thinking frameworks</span> </button> <button type="button" data-q="Show me case studies" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Case Studies</span> <span class="mt-0.5 block text-[11px] text-muted">Design in practice</span> </button> <button type="button" data-q="What's in the writing?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Essays & Writing</span> <span class="mt-0.5 block text-[11px] text-muted">Systems and trust</span> </button> <button type="button" data-q="What tools are there?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Tools & Experiments</span> <span class="mt-0.5 block text-[11px] text-muted">Interactive systems</span> </button> <button type="button" data-q="How do I contact her?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Get in Touch</span> <span class="mt-0.5 block text-[11px] text-muted">Email, LinkedIn, message</span> </button> </div> </div> <div id="mai-log" class="flex-1 overflow-y-auto px-4 py-3 space-y-3 text-sm" aria-live="polite"></div> <div id="mai-chips" class="px-4 pt-2 pb-1 flex flex-wrap gap-1.5"></div> <form id="mai-form" class="flex items-center gap-2 border-t border-ink/10 px-4 pt-3 pb-8 bg-paper"> <button id="mai-mic" type="button" aria-label="Speak your question" aria-pressed="false" title="Speak" class="rounded p-2 text-muted hover:text-accent transition-colors"> <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <rect x="9" y="3" width="6" height="12" rx="3"></rect> <path d="M5 11a7 7 0 0 0 14 0"></path> <line x1="12" y1="18" x2="12" y2="22"></line> </svg> </button> <label for="mai-input" class="sr-only">Ask M.ai a question</label> <input id="mai-input" type="text" autocomplete="off" placeholder="Ask about a project, an idea, or where to start…" class="flex-1 min-w-0 rounded border border-ink/15 bg-paper px-3 py-2.5 text-sm text-ink placeholder:text-muted focus:outline-none focus:border-accent"> <button type="submit" aria-label="Send" class="rounded bg-ink px-3 py-2.5 text-paper text-xs hover:bg-accent transition-colors">Send</button> </form> </aside> </div>  <script>(function(){`, "\n  (function () {\n    // ---------- LLM proxy (optional) ----------\n    // When set to a deployed Cloudflare Worker URL, M.ai will ask the worker\n    // to write a grounded answer using the top retrieved chunks. When empty,\n    // M.ai stays in keyword-only mode (the original behavior). See\n    // workers/mai/README.md for deploy steps.\n    const MAI_WORKER_URL = 'https://mai.floramaydc-mai.workers.dev';\n    const MAI_CLIENT_VERSION = '2026-05-26-whisper-v1';\n\n    function maiStorageKey(name) {\n      return `mai:${MAI_CLIENT_VERSION}:${name}`;\n    }\n\n    function clearLegacyMaiState() {\n      const legacyKeys = ['mai-transcript', 'mai-chips', 'mai-topics-hidden', 'mai-open'];\n      for (const key of legacyKeys) sessionStorage.removeItem(key);\n    }\n\n    function safeJsonParse(raw) {\n      if (!raw) return null;\n      try {\n        return JSON.parse(raw);\n      } catch {\n        return null;\n      }\n    }\n\n    function trimText(value, max = 140) {\n      return String(value || '').replace(/\\s+/g, ' ').trim().slice(0, max);\n    }\n\n    function pathFromHref(href) {\n      if (!href) return '';\n      try {\n        const url = new URL(href, window.location.href);\n        return `${url.pathname}${url.hash || ''}`;\n      } catch {\n        return '';\n      }\n    }\n\n    // ---------- Hand-written answers ----------\n    // A short list of structured replies M.ai should answer the same way every\n    // time — contact info (needs real links) and pure greeting/thanks. Anything\n    // about who Flora is, what she does, or how M.ai works flows through to the\n    // worker so the personality + memory in workers/mai/src/ can speak.\n    const KB = [\n      {\n        id: 'contact',\n        // \"contract\"/\"contracting\" included as belt-and-braces: even if\n        // SPEECH_FIXES somehow fails to fire (stale cache, etc.) the KB\n        // still routes \"how do I contract her\" to the contact answer\n        // instead of falling through to search.\n        patterns: ['contact', 'contract her', 'contract flora', 'contracting her', 'contracting flora', 'email', 'reach', 'hire', 'message', 'how do i contact', 'how do i contract', 'get in touch', 'reach out', 'linkedin'],\n        answer: `Three paths, pick whichever fits:<br><br>• <a class=\"text-accent underline\" href=\"https://linkedin.com/in/floramaydelacruz\">LinkedIn</a> — fastest for professional intros<br>• <a class=\"text-accent underline\" href=\"${base}about\">About page</a> — email and Instagram at the bottom<br>• Or leave a message right here and I'll forward it to her inbox<br><br><button type=\"button\" class=\"rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-start-message>Send Flora a message</button>`,\n      },\n      {\n        id: 'hi',\n        patterns: ['hi', 'hello', 'hey', 'yo', 'sup', 'greetings'],\n        answer: `Hello. Ask about Flora's work, writing, or frameworks in the Toybox — I'll search the site and synthesize with citations.`,\n        followups: ['What playbooks exist?', 'Show me case studies', 'Explore the Toybox'],\n      },\n      {\n        id: 'thanks',\n        patterns: ['thanks', 'thank you', 'thx', 'ty', 'appreciate'],\n        answer: `Of course. I'll be here whenever a new question comes up.`,\n        followups: ['Explore the Toybox', 'Show me case studies'],\n      },\n    ];\n\n    const STARTER_CHIPS = ['What playbooks exist?', 'Show me case studies', 'Explore the Toybox', 'Send a message'];\n\n    function uniqueChips(items, max = 4) {\n      const out = [];\n      const seen = new Set();\n      for (const item of items || []) {\n        const text = trimText(String(item || ''), 80);\n        if (!text) continue;\n        const key = text.toLowerCase();\n        if (seen.has(key)) continue;\n        seen.add(key);\n        out.push(text);\n        if (out.length >= max) break;\n      }\n      return out;\n    }\n\n    // Build follow-up chips from the user's latest ask + retrieved docs so\n    // suggestions change as the conversation evolves.\n    function getDynamicChips(query = '', top = []) {\n      const q = normalize(query || '');\n      const chips = [];\n      const add = (...items) => chips.push(...items);\n\n      if (/\\b(dark|theme|contrast|accessib)\\w*\\b/.test(q)) {\n        add('Turn on dark mode', 'Show dark mode one-pager', 'Run accessibility checklist');\n      }\n      if (/\\b(playbook|pattern|template|prompt|prototype|tool|toybox)\\b/.test(q)) {\n        add('What playbooks exist?', 'Show newest Toybox updates', 'Explore the Toybox');\n      }\n      if (/\\b(work|case study|project|pacbot|vera|mindelf|codecatalyst|aws|ebbc)\\b/.test(q)) {\n        add('Show me case studies', 'Take me to Work', 'Compare flagship projects');\n      }\n      if (/\\b(writing|article|blog|post|essay|reflection)\\b/.test(q)) {\n        add('Take me to Writing', 'Show recent writing', 'What did Flora publish lately?');\n      }\n      if (/\\b(about|background|experience|career|who is flora|who\\s+is\\s+flora)\\b/.test(q)) {\n        add('Take me to About', 'What is Flora working on?', 'Show me case studies');\n      }\n\n      const collections = new Set((top || []).map((r) => r?.d?.collection).filter(Boolean));\n      if (collections.has('work')) add('Take me to Work', 'Compare flagship projects');\n      if (collections.has('toybox')) add('Explore the Toybox', 'Show newest Toybox updates');\n      if (collections.has('writing')) add('Take me to Writing', 'Show recent writing');\n\n      return uniqueChips([...chips, 'Send a message', ...STARTER_CHIPS], 4);\n    }\n\n    // ---------- Speech-to-text corrections ----------\n    // Web Speech API tends to split known compound words into two tokens\n    // (\"toy box\" instead of \"toybox\") and to spell-out acronyms\n    // (\"M A I\"). These break BM25 retrieval and the KB matcher, so we\n    // collapse the common ones BEFORE classifying or searching. Applied\n    // to typed input too — same correction is helpful either way.\n    const SPEECH_FIXES = [\n      [/\\btoy\\s*box(es)?\\b/gi, 'toybox$1'],\n      [/\\btool\\s*box\\b/gi, 'toybox'], // \"tool box\" → \"toybox\" (common misshear)\n      [/\\bplay\\s*book(s)?\\b/gi, 'playbook$1'],\n      [/\\bm\\s*\\.?\\s*a\\s*\\.?\\s*i\\b/gi, 'M.ai'],\n      [/\\bmay\\s*eye\\b/gi, 'M.ai'],\n      [/\\bmay\\s*i\\b/gi, 'M.ai'],\n      [/\\bcode\\s*catalyst\\b/gi, 'CodeCatalyst'],\n      [/\\bpac\\s*bot\\b/gi, 'PacBot'],\n      [/\\bdark\\s*patterns?\\s*detector\\b/gi, 'Dark Pattern Detector'],\n      [/\\bflo\\s*ra\\b/gi, 'Flora'],\n      [/\\bflora\\s*may\\b/gi, 'Flora May'],\n      // Speech-to-text and aggressive autocorrect often replace \"contact\"\n      // with \"contract\" — and because \"contract\" is a real word neither the\n      // SPEECH_FIXES nor the fuzzy corrector below would otherwise catch it.\n      // We disambiguate by context: only rewrite when followed by an\n      // object/pronoun that makes \"contract\" semantically wrong here\n      // \"contract her\", \"contract Flora\", \"contract you\", \"contract them\",\n      // and \"contracting her/Flora/you/them\".\n      [/\\bcontract(ing)?\\s+(her|him|them|you|flora|flora\\s*may)\\b/gi, 'contact$1 $2'],\n      // \"how do i contract\" / \"can i contract\" without an object — still\n      // overwhelmingly means contact in this site's context.\n      [/\\b(how|where|can|may)\\s+(do\\s+)?(i|we|one)\\s+contract\\b/gi, '$1 $2$3 contact'],\n      // \"to contract\" / \"want to contract\" / \"like to contract\" — same.\n      [/\\b(to|wanna|want to|like to|love to|need to|trying to|tryna)\\s+contract\\b/gi, '$1 contact'],\n      // \"contract\" appearing near contact-y words (email, message, reach,\n      // linkedin, hire) within ~30 chars — overwhelmingly autocorrect.\n      [/\\bcontract\\b(?=[^.?!]{0,30}\\b(email|message|reach|linkedin|hire)\\b)/gi, 'contact'],\n      [/\\b(email|message|reach|linkedin|hire)\\b(?=[^.?!]{0,30}\\bcontract\\b)([^.?!]{0,30})\\bcontract\\b/gi, '$1$2contact'],\n    ];\n    function expandSpeech(s) {\n      let out = s;\n      for (const [re, rep] of SPEECH_FIXES) out = out.replace(re, rep);\n      return out;\n    }\n\n    // ---------- Matcher (hand-written answers) ----------\n    function normalize(s) {\n      return s.toLowerCase().replace(/[^a-z0-9\\s.-]/g, ' ').replace(/\\s+/g, ' ').trim();\n    }\n    // Whole-word containment. Avoids the classic \"hi\" inside \"this\" /\n    // \"history\" / \"shipping\" trap that made the old matcher trigger the\n    // greeting reply on hostile or unrelated inputs.\n    function containsPhrase(haystack, needle) {\n      const re = new RegExp(`(^|\\\\W)${needle.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&')}(\\\\W|$)`);\n      return re.test(haystack);\n    }\n    function kbAnswerFor(input) {\n      const n = normalize(input);\n      if (!n) return null;\n      let best = { score: 0, entry: null };\n      for (const entry of KB) {\n        for (const p of entry.patterns) {\n          const np = normalize(p);\n          let score = 0;\n          if (containsPhrase(n, np)) score += 2;\n          const tokens = np.split(' ');\n          let hits = 0;\n          for (const t of tokens) {\n            if (t.length > 2 && containsPhrase(n, t)) hits++;\n          }\n          score += hits / Math.max(1, tokens.length);\n          if (score > best.score) best = { score, entry };\n        }\n      }\n      // Threshold tuned by hand \\u2014 under 1.0 lets retrieval take over, which\n      // is usually better for substantive questions (\"tell me about pacbot\").\n      return best.score >= 1.0 ? best.entry : null;\n    }\n\n    // ---------- Retrieval over the site index ----------\n    // Loaded lazily on the first real question. The index is precomputed by\n    // scripts/build-mai-index.mjs and served from /mai-index.json. Each doc\n    // already carries a field-weighted tf map and we have global df + N \\u2014\n    // so the runtime ranker is a small BM25-ish loop with no dependencies.\n    const STOP_TOKENS = new Set([\n      'the','a','an','and','or','of','to','in','for','on','with','is','are','was',\n      'were','be','been','being','this','that','these','those','it','its','at','by',\n      'as','from','but','not','no','if','then','than','so','do','does','did','i',\n      'you','he','she','we','they','them','his','her','our','your','their','my',\n      'me','us','about','into','out','up','down','over','under','also','can','will',\n      'would','could','should','have','has','had','more','most','some','any','all',\n      'what','who','how','where','when','why','show','tell','give','find',\n    ]);\n    function tokenizeQ(s) {\n      return s\n        .toLowerCase()\n        .replace(/[^a-z0-9\\s-]/g, ' ')\n        .split(/\\s+/)\n        .filter((t) => t.length > 2 && !STOP_TOKENS.has(t));\n    }\n\n    let _indexPromise = null;\n    function loadIndex() {\n      if (!_indexPromise) {\n        _indexPromise = fetch(`${base}mai-index.json`)\n          .then((r) => (r.ok ? r.json() : null))\n          .catch(() => null);\n      }\n      return _indexPromise;\n    }\n\n    // ---------- Fuzzy spell-fix against the index vocabulary ----------\n    // The hand-listed SPEECH_FIXES table catches the *known* mishearings\n    // (\"toy box\", \"M A I\", \"pac bot\") — but it can't anticipate every way\n    // a name gets misspelled or misheard: \"packbot\", \"vehra\", \"miendelf\",\n    // \"tabloos\", \"dela cruise\". Instead of growing that table forever, we\n    // compare every query word against the actual indexed vocabulary (all\n    // tokens that appear in any document on the site) and silently swap\n    // an unknown word for its nearest neighbor when one is close enough.\n    //\n    // Distance: Damerau-Levenshtein (handles adjacent transposition so\n    // \"pacbot\" ↔ \"pacbot\" works even when a vowel/consonant flips). We\n    // gate by edit distance scaled to word length so short words don't\n    // collapse into noise.\n\n    let _vocabPromise = null;\n    let _vocabReady = null; // resolved value (set when the promise lands)\n    function loadVocab() {\n      if (_vocabPromise) return _vocabPromise;\n      _vocabPromise = loadIndex().then((idx) => {\n        if (!idx) return null;\n        // df keys are every term that appears anywhere on the site, already\n        // normalized by the index builder. Bucket by length for fast lookup\n        // — when correcting a word of length L we only need to scan vocab\n        // entries of length L-2 ... L+2.\n        const buckets = new Map();\n        const vocab = new Set();\n        for (const w of Object.keys(idx.df || {})) {\n          if (w.length < 3) continue;\n          vocab.add(w);\n          const L = w.length;\n          if (!buckets.has(L)) buckets.set(L, []);\n          buckets.get(L).push(w);\n        }\n        _vocabReady = { vocab, buckets };\n        return _vocabReady;\n      });\n      return _vocabPromise;\n    }\n\n    // Damerau-Levenshtein with an early-exit cap. Returns Infinity if the\n    // distance is known to exceed `max` — cheap rejection for the common\n    // case where two words are obviously different.\n    function dlDistance(a, b, max) {\n      if (a === b) return 0;\n      const al = a.length, bl = b.length;\n      if (Math.abs(al - bl) > max) return Infinity;\n      const prev2 = new Array(bl + 1);\n      const prev = new Array(bl + 1);\n      const curr = new Array(bl + 1);\n      for (let j = 0; j <= bl; j++) prev[j] = j;\n      for (let i = 1; i <= al; i++) {\n        curr[0] = i;\n        let rowMin = curr[0];\n        for (let j = 1; j <= bl; j++) {\n          const cost = a.charCodeAt(i - 1) === b.charCodeAt(j - 1) ? 0 : 1;\n          let v = Math.min(\n            curr[j - 1] + 1,        // insertion\n            prev[j] + 1,            // deletion\n            prev[j - 1] + cost      // substitution\n          );\n          // Transposition (adjacent swap).\n          if (i > 1 && j > 1 &&\n              a.charCodeAt(i - 1) === b.charCodeAt(j - 2) &&\n              a.charCodeAt(i - 2) === b.charCodeAt(j - 1)) {\n            v = Math.min(v, prev2[j - 2] + 1);\n          }\n          curr[j] = v;\n          if (v < rowMin) rowMin = v;\n        }\n        if (rowMin > max) return Infinity;\n        for (let j = 0; j <= bl; j++) { prev2[j] = prev[j]; prev[j] = curr[j]; }\n      }\n      return prev[bl];\n    }\n\n    // Allowed edit distance scaled by word length. Conservative on purpose\n    // — we'd rather miss a fix than confidently swap \"vera\" for \"very\".\n    function maxEditDist(len) {\n      if (len <= 4) return 1;\n      if (len <= 7) return 2;\n      return 2;\n    }\n\n    // Guard against grammatical tense/inflection swaps that are often valid\n    // words by themselves and should not be auto-corrected.\n    function isInflectionSwap(a, b) {\n      const al = a.toLowerCase();\n      const bl = b.toLowerCase();\n      return (\n        (al.endsWith('ies') && bl === `${al.slice(0, -3)}ied`) ||\n        (al.endsWith('ied') && bl === `${al.slice(0, -3)}ies`)\n      );\n    }\n\n    const _fixCache = new Map();\n    function fuzzyCorrectWord(word, vocabData) {\n      if (!vocabData) return word;\n      const w = word.toLowerCase();\n      if (w.length < 4) return word;            // too short to fix safely\n      if (STOP_TOKENS.has(w)) return word;\n      if (vocabData.vocab.has(w)) return word;  // already a known term\n      if (_fixCache.has(w)) return _fixCache.get(w);\n      const max = maxEditDist(w.length);\n      let best = null, bestD = max + 1;\n      for (let d = -max; d <= max; d++) {\n        const bucket = vocabData.buckets.get(w.length + d);\n        if (!bucket) continue;\n        for (const cand of bucket) {\n          const dist = dlDistance(w, cand, bestD - 1);\n          if (dist < bestD) {\n            bestD = dist;\n            best = cand;\n            if (dist === 1) break; // 1 is already optimal for our purposes\n          }\n        }\n        if (bestD === 1) break;\n      }\n      const fixed = (best && bestD <= max && !isInflectionSwap(w, best)) ? best : word;\n      _fixCache.set(w, fixed);\n      return fixed;\n    }\n\n    function fuzzyCorrectQuery(text, vocabData) {\n      if (!vocabData) return text;\n      // Split on whitespace but preserve simple punctuation around words\n      // so \"PacBot?\" still corrects cleanly.\n      return text.replace(/[A-Za-z][A-Za-z'-]*/g, (m) => {\n        const fixed = fuzzyCorrectWord(m, vocabData);\n        if (fixed === m.toLowerCase()) return m; // no change → keep original casing\n        if (fixed === m) return m;\n        return fixed;\n      });\n    }\n\n    function rank(index, query) {\n      const qTokens = tokenizeQ(query);\n      if (!qTokens.length) return [];\n      const N = index.n || index.docs.length;\n      const df = index.df || {};\n      // Average chunk length \\u2014 used by the BM25 length-norm term.\n      let avgLen = 0;\n      for (const d of index.docs) avgLen += d.len || 0;\n      avgLen = avgLen / Math.max(1, index.docs.length);\n\n      const k1 = 1.2;\n      const b = 0.75;\n      const scored = [];\n      for (const d of index.docs) {\n        let s = 0;\n        let matched = 0;\n        for (const t of qTokens) {\n          const f = d.tf?.[t] || 0;\n          if (!f) continue;\n          matched++;\n          const n = df[t] || 0;\n          const idf = Math.log(1 + (N - n + 0.5) / (n + 0.5));\n          const norm = 1 - b + b * ((d.len || 1) / Math.max(1, avgLen));\n          s += idf * ((f * (k1 + 1)) / (f + k1 * norm));\n        }\n        if (matched > 0) {\n          // Tiny bonus when more of the query's terms were hit \\u2014 keeps\n          // multi-word queries from being out-ranked by single-term spam.\n          s *= 1 + matched / qTokens.length;\n          scored.push({ d, s });\n        }\n      }\n      scored.sort((a, b) => b.s - a.s);\n      return scored;\n    }\n\n    function dedupeByDoc(scored, max) {\n      const seen = new Set();\n      const out = [];\n      for (const r of scored) {\n        const key = `${r.d.collection}/${r.d.slug}`;\n        if (seen.has(key)) continue;\n        seen.add(key);\n        out.push(r);\n        if (out.length >= max) break;\n      }\n      return out;\n    }\n\n    function pickOldestPlaybook(index) {\n      const bySlug = new Map();\n      for (const d of index?.docs || []) {\n        if (d.collection !== 'toybox') continue;\n        if (String(d.category || '').toLowerCase() !== 'playbook') continue;\n        const url = String(d.url || '');\n        if (!url || !url.startsWith('/')) continue;\n        const updated = String(d.updated || '').trim();\n        const candidate = {\n          slug: String(d.slug || ''),\n          title: String(d.title || d.heading || 'Playbook').trim(),\n          url,\n          updated,\n        };\n        const prev = bySlug.get(candidate.slug);\n        if (!prev) {\n          bySlug.set(candidate.slug, candidate);\n          continue;\n        }\n        // Prefer non-anchor URL as canonical destination per slug.\n        const prevAnchor = prev.url.includes('#');\n        const candAnchor = candidate.url.includes('#');\n        if (prevAnchor && !candAnchor) bySlug.set(candidate.slug, candidate);\n      }\n\n      const all = [...bySlug.values()];\n      if (!all.length) return null;\n\n      // Oldest first by `updated`; empty dates sort to the end.\n      all.sort((a, b) => {\n        const ad = a.updated || '9999-12-31';\n        const bd = b.updated || '9999-12-31';\n        if (ad < bd) return -1;\n        if (ad > bd) return 1;\n        return a.title.localeCompare(b.title);\n      });\n\n      const first = all[0];\n      const tieCount = all.filter((x) => (x.updated || '') === (first.updated || '')).length;\n      return { first, tieCount };\n    }\n\n    function escapeHtml(s) {\n      return s.replace(/[&<>\"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;' }[c]));\n    }\n    function stripHtml(s) {\n      const d = document.createElement('div');\n      d.innerHTML = s;\n      return d.textContent || '';\n    }\n\n    // Render the model's streamed output safely. The model is instructed\n    // to emit a small set of HTML tags (<em>, <strong>, <a href=\"...\">)\n    // plus hyphen-prefixed lines for bullet lists. We escape EVERYTHING\n    // first, then re-allow only that tight set — so a hallucinated\n    // script/iframe tags or javascript: URLs never survive. Safe to call\n    // on partial streamed chunks: half-formed tags stay escaped until\n    // their closing token arrives.\n    function safeRenderModelHtml(text) {\n      let s = escapeHtml(text);\n      // Re-allow inline emphasis: <em>, </em>, <strong>, </strong>.\n      s = s.replace(/&lt;(\\/?)(em|strong)&gt;/gi, '<$1$2>');\n      // Re-allow <a href=\"...\"> for safe URLs only: site-relative paths\n      // starting with \"/\" OR full http(s) URLs. Everything else stays\n      // escaped — no javascript:, data:, mailto:, etc.\n      s = s.replace(\n        /&lt;a\\s+href=&quot;((?:\\/[^\"&\\s]*|https?:\\/\\/[^\"&\\s]+))&quot;&gt;([\\s\\S]*?)&lt;\\/a&gt;/gi,\n        (_m, url, label) => `<a class=\"text-accent underline\" href=\"${url}\">${label}</a>`\n      );\n      // Group consecutive \"- \" lines into a real <ul>. Other lines get\n      // <br> joins so paragraph breaks survive.\n      const lines = s.split('\\n');\n      const out = [];\n      let inList = false;\n      for (const raw of lines) {\n        const m = raw.match(/^\\s*-\\s+(.+)$/);\n        if (m) {\n          if (!inList) { out.push('<ul class=\"mai-list\">'); inList = true; }\n          out.push(`<li>${m[1]}</li>`);\n        } else {\n          if (inList) { out.push('</ul>'); inList = false; }\n          out.push(raw);\n        }\n      }\n      if (inList) out.push('</ul>');\n      // Join: <li> rows are already block; non-list lines need <br>s.\n      // Simplest: join with '\\n', then turn newlines OUTSIDE the <ul> into <br>.\n      return out.join('\\n').replace(/\\n(?![<])/g, '<br>').replace(/\\n/g, '');\n    }\n\n    function snippet(text, query, maxLen = 220) {\n      const lower = text.toLowerCase();\n      const qTokens = tokenizeQ(query);\n      let pos = -1;\n      for (const t of qTokens) {\n        const i = lower.indexOf(t);\n        if (i !== -1 && (pos === -1 || i < pos)) pos = i;\n      }\n      let start = 0;\n      let end = Math.min(text.length, maxLen);\n      if (pos > 60) {\n        start = pos - 60;\n        end = Math.min(text.length, start + maxLen);\n      }\n      let out = text.slice(start, end).trim();\n      if (start > 0) out = '\\u2026' + out;\n      if (end < text.length) out = out + '\\u2026';\n      // Bold the matched tokens. Safe because text was sanitized at index time.\n      const escaped = escapeHtml(out);\n      let highlighted = escaped;\n      for (const t of qTokens) {\n        if (t.length < 3) continue;\n        const re = new RegExp(`(${t.replace(/[.*+?^${}()|[\\\\]\\\\\\\\]/g, '\\\\\\\\$&')})`, 'gi');\n        highlighted = highlighted.replace(re, '<strong>$1</strong>');\n      }\n      return highlighted;\n    }\n\n    const COLLECTION_LABEL = {\n      writing: 'Writing',\n      work: 'Work',\n      toybox: 'Toybox',\n      play: 'Play',\n    };\n\n    function renderRetrieval(results, query) {\n      const parts = results.map((r) => {\n        const d = r.d;\n        const label = COLLECTION_LABEL[d.collection] || d.collection;\n        const heading = d.heading && d.heading !== d.title\n          ? `${escapeHtml(d.title)} \\u2014 <span class=\"text-muted\">${escapeHtml(d.heading)}</span>`\n          : escapeHtml(d.title);\n        const url = `${base}${d.url.replace(/^\\//, '')}`;\n        return `\n          <div class=\"mai-result\">\n            <div class=\"text-[10px] uppercase tracking-widest text-muted\">${label}</div>\n            <a class=\"block text-accent underline font-semibold mt-0.5\" href=\"${url}\">${heading}</a>\n            <p class=\"mt-1 text-ink/80\">${snippet(d.text, query)}</p>\n          </div>`;\n      }).join('');\n      return `<p class=\"text-[11px] text-muted mb-2\">Here's what I found on the site:</p>${parts}`;\n    }\n\n    function noMatchAnswer(query) {\n      return `I couldn't find anything for <em>${escapeHtml(query)}</em> in what Flora has published so far. Try the <a class=\"text-accent underline\" href=\"${base}about\">About page</a>, the <a class=\"text-accent underline\" href=\"${base}toybox\">Toybox</a>, or one of the chips below.`;\n    }\n\n    // ---------- LLM tier (optional) ----------\n    // If MAI_WORKER_URL is set, route the user question + top chunks through\n    // the worker for a grounded answer. The worker enforces a monthly $ cap;\n    // when it returns 402 we flip aiPaused=true, show a banner, and fall\n    // back to plain retrieval for the rest of the session. Any other failure\n    // (network, 5xx) also silently falls back \\u2014 the user always gets an\n    // answer, even when the LLM tier is down.\n    // When an answer ends with a `[[NAV:n]]` marker, we surface a\n    // \"Want me to take you there?\" CTA pointing at chunk n. This holds\n    // the pending offer between the answer rendering and either:\n    //   - a button click (\"Yes, take me\" / \"No thanks\"), or\n    //   - the next typed input being a short affirmative (\"yes/sure/please\")\n    //     or negative (\"no/nope/cancel\").\n    // Anything else just clears it and proceeds as a normal new question.\n    let pendingNav = null; // { url, label } | null\n    let pendingUiAction = null; // { type, target, label } | null\n\n    function normalizeUiAction(type, target, label) {\n      const t = String(type || '').trim().toLowerCase();\n      const rawTarget = String(target || '').trim();\n      const rawLabel = String(label || '').trim();\n      if (!['navigate', 'focus', 'scroll', 'toggle-theme'].includes(t)) return null;\n      if (!rawTarget && t !== 'toggle-theme') return null;\n      const safeLabel = trimText(rawLabel || `${t} ${rawTarget}`, 120);\n      return { type: t, target: trimText(rawTarget, 220), label: safeLabel };\n    }\n\n    function parseActionMarker(text) {\n      const m = String(text || '').match(/\\[\\[\\s*ACTION\\s*:\\s*([a-z]+)\\|([^|\\]]*)\\|([^\\]]*)\\]\\]/i);\n      if (!m) return null;\n      return normalizeUiAction(m[1], m[2], m[3]);\n    }\n\n    function stripControlMarkers(text) {\n      return String(text || '')\n        .replace(/\\[\\[\\s*NAV\\s*:\\s*\\d+\\s*\\]\\]/gi, '')\n        .replace(/\\[\\[\\s*ACTION\\s*:\\s*[^\\]]+\\]\\]/gi, '')\n        .replace(/\\n{3,}/g, '\\n\\n')\n        .trimEnd();\n    }\n\n    function pathToName(path) {\n      // Map URL paths to human-readable page names.\n      if (!path) return 'a page';\n      const segments = path.split('/').filter(Boolean);\n      const base = segments[0] || 'home';\n      const map = {\n        'home': 'Home page',\n        'mai': 'M.ai chat',\n        'about': 'About Flora',\n        'toybox': 'Toybox',\n        'work': 'Work',\n        'play': 'Play',\n        'writing': 'Writing',\n        'contact': 'Contact',\n      };\n      return map[base] || `the ${base} section`;\n    }\n\n    function renderActionCta(action) {\n      if (!action) return '';\n      const safeType = escapeHtml(action.type);\n      const safeTarget = escapeHtml(action.target);\n      let displayLabel = action.label;\n      if (!displayLabel && action.type === 'navigate') {\n        displayLabel = `Take me to ${pathToName(action.target)}`;\n      } else if (!displayLabel) {\n        displayLabel = `${action.type} ${action.target}`;\n      }\n      const safeLabel = escapeHtml(displayLabel);\n      return (\n        `<div class=\"mai-action-cta mt-3 pt-2 border-t border-muted/20 text-[12px]\" ` +\n          `data-mai-action-cta data-action-type=\"${safeType}\" data-action-target=\"${safeTarget}\" data-action-label=\"${safeLabel}\">` +\n          `<p class=\"text-muted mb-1.5\">I can do this now: <em>${safeLabel}</em>.</p>` +\n          `<div class=\"flex gap-2\">` +\n            `<button type=\"button\" class=\"rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-action-run>Run it</button>` +\n            `<button type=\"button\" class=\"rounded border border-muted/30 text-muted px-2 py-1 text-[11px] hover:border-muted/60 transition-colors\" data-mai-action-no>Not now</button>` +\n          `</div>` +\n        `</div>`\n      );\n    }\n\n    function executeUiAction(action, source) {\n      if (!action) return false;\n      const type = action.type;\n      const target = action.target;\n      const label = action.label;\n\n      if (type === 'navigate') {\n        const ok = /^\\//.test(target) || /^https?:\\/\\//i.test(target);\n        if (!ok) {\n          setActionResult({ type, status: 'invalid_target', target, label, source });\n          addBubble('mai', `I couldn't run that navigation target safely.`);\n          return false;\n        }\n        setActionResult({ type, status: 'accepted', target, label, source });\n        track('mai_action_execute', { type, status: 'accepted', source });\n        const isMobile = window.matchMedia('(max-width: 767px)').matches;\n        if (isMobile) close();\n        window.location.assign(target);\n        return true;\n      }\n\n      if (type === 'focus') {\n        let node = null;\n        const q = String(target || '').toLowerCase();\n        if (q.includes('email')) node = document.querySelector('input[type=\"email\"]');\n        if (!node && (q.includes('message') || q.includes('text'))) node = document.querySelector('textarea, input[type=\"text\"]');\n        if (!node && q.includes('search')) node = document.querySelector('input[type=\"search\"], [role=\"search\"] input');\n        if (!node && q.includes('form')) node = document.querySelector('form input, form textarea, form select, form button');\n        if (!node && q === 'main') node = document.querySelector('#main-content :is(input,textarea,select,button,a[href])');\n        if (!node) node = document.querySelector('main :is(input,textarea,select,button,a[href])');\n\n        if (node && typeof node.focus === 'function') {\n          node.scrollIntoView?.({ behavior: 'smooth', block: 'center' });\n          node.focus({ preventScroll: true });\n          setActionResult({ type, status: 'executed', target, label, source });\n          track('mai_action_execute', { type, status: 'executed', source });\n          addBubble('mai', `Done. I moved focus to the relevant control.`);\n          return true;\n        }\n        setActionResult({ type, status: 'not_found', target, label, source });\n        track('mai_action_execute', { type, status: 'not_found', source });\n        addBubble('mai', `I couldn't find a matching field to focus on this page.`);\n        return false;\n      }\n\n      if (type === 'scroll') {\n        let node = null;\n        if (target === 'main') node = document.getElementById('main-content');\n        else if (target.startsWith('#')) node = document.querySelector(target);\n        if (!node && target.startsWith('/')) {\n          setActionResult({ type, status: 'accepted', target, label, source });\n          track('mai_action_execute', { type, status: 'accepted', source });\n          const isMobile = window.matchMedia('(max-width: 767px)').matches;\n          if (isMobile) close();\n          window.location.assign(target);\n          return true;\n        }\n        if (node) {\n          node.scrollIntoView({ behavior: 'smooth', block: 'start' });\n          setActionResult({ type, status: 'executed', target, label, source });\n          track('mai_action_execute', { type, status: 'executed', source });\n          addBubble('mai', `Done. I moved you to that section.`);\n          return true;\n        }\n        setActionResult({ type, status: 'not_found', target, label, source });\n        track('mai_action_execute', { type, status: 'not_found', source });\n        addBubble('mai', `I couldn't find that section on this page.`);\n        return false;\n      }\n\n      if (type === 'toggle-theme') {\n        const btn = document.querySelector('[data-theme-toggle]');\n        if (btn) {\n          btn.click();\n          setActionResult({ type, status: 'executed', target, label, source });\n          track('mai_action_execute', { type, status: 'executed', source });\n          const isDark = document.documentElement.classList.contains('dark');\n          addBubble('mai', `Done. I switched you to ${isDark ? 'dark' : 'light'} mode.`);\n          return true;\n        }\n        setActionResult({ type, status: 'not_found', target, label, source });\n        track('mai_action_execute', { type, status: 'not_found', source });\n        addBubble('mai', `I couldn't find the theme toggle on this page.`);\n        return false;\n      }\n\n      return false;\n    }\n\n    // ---------- Message-to-Flora flow ----------\n    // Multi-turn conversation that collects name -> email -> message and\n    // POSTs to the worker's /message endpoint (which forwards via\n    // Web3Forms to floramaydelacruz@gmail.com).\n    //\n    // Stage machine (linear):\n    //   null -> 'name' -> 'email' -> 'message' -> 'confirm' -> 'sending' -> null\n    //\n    // Any input during the flow is consumed by the flow, not the LLM —\n    // EXCEPT typed cancellations (\"cancel\", \"stop\", \"nevermind\") which\n    // exit the flow at any stage and return control to normal handling.\n    let messageFlow = null; // { stage, data: {name, email, message} } | null\n    const MESSAGE_CANCEL_RE = /^(cancel|stop|nevermind|never mind|forget it|exit|quit|no)[.! ]*$/i;\n    const EMAIL_RE = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]{2,}$/;\n\n    function startMessageFlow(source) {\n      track('mai_message_flow_start', { source: source || 'unknown' });\n      messageFlow = { stage: 'name', data: {} };\n      const prompt = `Happy to take a message and forward it to Flora.<br><br>What's your name? <span class=\"text-muted text-[11px]\">(say \"cancel\" anytime to stop)</span>`;\n      addBubble('mai', prompt);\n      if (speakReplies) speak(stripHtml(prompt));\n      renderChips([]);\n      saveTranscript();\n    }\n\n    // Try to advance the message flow with this input. Returns true if the\n    // flow consumed it, false if normal handling should continue.\n    async function progressMessageFlow(raw) {\n      if (!messageFlow) return false;\n      const text = (raw || '').trim();\n      if (!text) return true; // ignore empty submits while in-flow\n\n      // Universal escape hatch.\n      if (MESSAGE_CANCEL_RE.test(text)) {\n        messageFlow = null;\n        track('mai_message_flow_cancel', { source: 'typed_cancel' });\n        addBubble('user', escapeHtml(text));\n        input.value = '';\n        const prompt = `No problem — I won't send anything. What else can I help with?`;\n        addBubble('mai', prompt);\n        if (speakReplies) speak(stripHtml(prompt));\n        renderChips(STARTER_CHIPS);\n        saveTranscript();\n        return true;\n      }\n\n      // Render the user's input bubble first.\n      addBubble('user', escapeHtml(text));\n      input.value = '';\n\n      switch (messageFlow.stage) {\n        case 'name': {\n          if (text.length < 2 || text.length > 100) {\n            addBubble('mai', `Could I get your name? Just a couple of characters is fine.`);\n            saveTranscript();\n            return true;\n          }\n          messageFlow.data.name = text;\n          messageFlow.stage = 'email';\n          const prompt = `Nice to meet you, ${escapeHtml(text.split(/\\s+/)[0])}. What's the best email for Flora to reply to?`;\n          addBubble('mai', prompt);\n          if (speakReplies) speak(stripHtml(prompt));\n          saveTranscript();\n          return true;\n        }\n        case 'email': {\n          if (!EMAIL_RE.test(text) || text.length > 254) {\n            const prompt = `That doesn't look like a valid email — mind double-checking?`;\n            addBubble('mai', prompt);\n            if (speakReplies) speak(stripHtml(prompt));\n            saveTranscript();\n            return true;\n          }\n          messageFlow.data.email = text.toLowerCase();\n          messageFlow.stage = 'message';\n          const prompt = `Got it. What would you like to say? (a sentence or a few — up to ~2000 characters)`;\n          addBubble('mai', prompt);\n          if (speakReplies) speak(stripHtml(prompt));\n          saveTranscript();\n          return true;\n        }\n        case 'message': {\n          if (text.length < 5) {\n            const prompt = `A little more, please — even one sentence is plenty.`;\n            addBubble('mai', prompt);\n            if (speakReplies) speak(stripHtml(prompt));\n            saveTranscript();\n            return true;\n          }\n          if (text.length > 2000) {\n            const prompt = `That's longer than I can send in one go. Mind trimming it to around 2000 characters?`;\n            addBubble('mai', prompt);\n            if (speakReplies) speak(stripHtml(prompt));\n            saveTranscript();\n            return true;\n          }\n          messageFlow.data.message = text;\n          messageFlow.stage = 'confirm';\n          const d = messageFlow.data;\n          addBubble(\n            'mai',\n            `Here's what I'll send Flora:<br><br>` +\n              `<strong>From:</strong> ${escapeHtml(d.name)} &lt;${escapeHtml(d.email)}&gt;<br>` +\n              `<strong>Message:</strong><br>${escapeHtml(d.message).replace(/\\n/g, '<br>')}<br><br>` +\n              `<div class=\"mai-msg-cta mt-2 flex gap-2\" data-mai-msg-cta>` +\n                `<button type=\"button\" class=\"rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-msg-send>Send it</button>` +\n                `<button type=\"button\" class=\"rounded border border-muted/30 text-muted px-2 py-1 text-[11px] hover:border-muted/60 transition-colors\" data-mai-msg-cancel>Cancel</button>` +\n              `</div>`,\n          );\n          saveTranscript();\n          return true;\n        }\n        case 'confirm': {\n          // Accept typed yes/no in addition to the buttons.\n          const yes = /^(y|ya|ye|yes+|yea+h?|yep|yup|sure|ok(ay)?|send( it)?|please|do it|go)[.! ]*$/i;\n          if (yes.test(text)) {\n            await sendMessageFlow();\n            return true;\n          }\n          // Anything else at confirm stage we treat as \"I want to edit my\n          // message\" — restart from the message stage.\n          messageFlow.stage = 'message';\n          messageFlow.data.message = '';\n          const prompt = `Okay — what should the message say instead?`;\n          addBubble('mai', prompt);\n          if (speakReplies) speak(stripHtml(prompt));\n          saveTranscript();\n          return true;\n        }\n        default:\n          return false;\n      }\n    }\n\n    // Web3Forms's free tier blocks server-side callers (their classifier\n    // returns 403 / error 1106 to anything that doesn't look like a\n    // browser request), so we hit their API directly from the client.\n    // Per their docs (docs.web3forms.com), the access key is intended to\n    // be public — it only authorizes sending TO Flora's verified inbox,\n    // and Web3Forms enforces its own per-IP rate limits and spam screens.\n    // We still run our own content filters in JS before posting so the\n    // common abuse cases never reach them.\n    const WEB3FORMS_KEY = 'd2a3b55f-b53d-494f-8d3f-241a1f32875e';\n    const MSG_ABUSE_RE = /\\b(fuck|shit|bitch|asshole|cunt|dick|piss|kill (you|yourself)|porn|nude|naked|horny|onlyfans|sexy|sex|boobs|penis|vagina)\\b/i;\n    const MSG_INJECTION_RE = /(ignore (all |any |your |previous |prior )?(instructions|rules|prompt|system)|disregard (the |your |previous )?(above|prior|prompt|instructions)|you are (now |actually )?(dan|developer mode|jailbroken|unrestricted)|reveal (your|the) (system )?prompt)/i;\n    const MSG_SOLICITATION_RE = /\\b(seo|search engine optimization|backlink|link[- ]building|guest post|guest[- ]blog|sponsored post|paid post|paid review|monetize your (site|blog|website)|increase (your )?traffic|drive traffic|rank (higher|on google|on the first page)|google rank|page one of google|first page of google|website redesign|web design services|i (visited|came across|stumbled (up)?on|noticed) your (website|site|blog)|i (have|got) an? (great |amazing |interesting )?(offer|proposition|opportunity)|business proposal|investment opportunity|crypto(currency)?|bitcoin|forex|trading bot|loan offer|make \\$?\\d+|earn \\$?\\d+|click (here|the link below)|whatsapp (me|us)|telegram (me|us|us on)|hire me to|i can (help|rank|optimize) your)\\b/i;\n    function msgSpamScore(t) {\n      let score = 0;\n      const urls = (t.match(/\\bhttps?:\\/\\/|www\\./gi) || []).length;\n      if (urls >= 2) score += urls;\n      if (/\\b\\S{60,}\\b/.test(t)) score += 2;\n      if (/[A-Z]{12,}/.test(t)) score += 1;\n      if ((t.match(/!{2,}|\\?{3,}/g) || []).length >= 2) score += 1;\n      if ((t.match(/\\$\\d|\\b\\d+%/g) || []).length >= 2) score += 1;\n      return score;\n    }\n    const MESSAGE_OWNER_EMAIL = 'floramaydelacruz@gmail.com';\n\n    async function sendMessageFlow() {\n      if (!messageFlow || messageFlow.stage !== 'confirm') return;\n      const data = messageFlow.data;\n      messageFlow.stage = 'sending';\n      const pendingId = addBubble(\n        'mai',\n        `<span class=\"mai-thinking text-muted\">Sending<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>`,\n      );\n\n      // Client-side content filtering. Bypassable if a user crafts their\n      // own fetch — Web3Forms's own spam screen + rate limit are still\n      // there as a second line of defense. Skip filters for Flora's own\n      // address so she can always test end-to-end.\n      const isOwner = (data.email || '').toLowerCase() === MESSAGE_OWNER_EMAIL;\n      if (!isOwner) {\n        const msg = data.message || '';\n        const nm = data.name || '';\n        const blocked =\n          MSG_ABUSE_RE.test(msg) ||\n          MSG_INJECTION_RE.test(msg) ||\n          MSG_ABUSE_RE.test(nm) ||\n          MSG_SOLICITATION_RE.test(msg) ||\n          MSG_SOLICITATION_RE.test(nm) ||\n          msgSpamScore(msg) >= 3 ||\n          (msg.match(/\\bhttps?:\\/\\/|www\\./gi) || []).length > 1;\n        if (blocked) {\n          track('mai_message_flow_blocked', { reason: 'spam_filter' });\n          const b = document.getElementById(pendingId);\n          const prompt = `That message tripped one of the spam filters, so I can't forward it. If this is a real note, please email Flora directly at floramaydelacruz@gmail.com.`;\n          if (b) {\n            b.innerHTML = prompt;\n          }\n          if (speakReplies) speak(prompt);\n          messageFlow = null;\n          renderChips(getDynamicChips(data.message || 'send message'));\n          saveTranscript();\n          return;\n        }\n      }\n\n      try {\n        const res = await fetch('https://api.web3forms.com/submit', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json', Accept: 'application/json' },\n          body: JSON.stringify({\n            access_key: WEB3FORMS_KEY,\n            subject: `M.ai message from ${data.name}`,\n            from_name: 'M.ai (floramaydc.com)',\n            name: data.name,\n            email: data.email,\n            message: data.message,\n            botcheck: '', // Web3Forms's built-in honeypot field\n          }),\n        });\n        const pendingBubble = document.getElementById(pendingId);\n        let body = null;\n        try { body = await res.json(); } catch {}\n        if (res.ok && body && body.success) {\n          track('mai_message_flow_submit', { status: 'success' });\n          const prompt = `Sent. Flora will see it in her inbox and get back to you at ${data.email}. Thank you for reaching out.`;\n          if (pendingBubble) {\n            pendingBubble.innerHTML = `Sent. Flora will see it in her inbox and get back to you at <em>${escapeHtml(data.email)}</em>. Thank you for reaching out.`;\n          }\n          if (speakReplies) speak(prompt);\n        } else {\n          track('mai_message_flow_submit', { status: 'error', code: String(res.status) });\n          const status = res.status;\n          const w3msg = (body && body.message) || '';\n          const human = status === 429\n            ? `Looks like a lot of messages have come from this network already. Try again in an hour, or email Flora directly at floramaydelacruz@gmail.com.`\n            : status === 403\n            ? `That message was flagged as spam by our filter. If this is a real note, please email Flora directly at floramaydelacruz@gmail.com.`\n            : `Something went wrong sending that (${w3msg || status}). You can reach Flora directly at floramaydelacruz@gmail.com.`;\n          if (pendingBubble) pendingBubble.innerHTML = human;\n          if (speakReplies) speak(human);\n        }\n      } catch {\n        track('mai_message_flow_submit', { status: 'error', code: 'network' });\n        const pendingBubble = document.getElementById(pendingId);\n        const prompt = `Couldn't reach the server. You can email Flora directly at floramaydelacruz@gmail.com.`;\n        if (pendingBubble) {\n          pendingBubble.innerHTML = prompt;\n        }\n        if (speakReplies) speak(prompt);\n      }\n      messageFlow = null;\n      renderChips(STARTER_CHIPS);\n      saveTranscript();\n    }\n\n    let aiPaused = false;\n\n    function showPausedBanner(message) {\n      if (document.getElementById('mai-paused-banner')) return;\n      const b = document.createElement('div');\n      b.id = 'mai-paused-banner';\n      b.className = 'text-[11px] text-muted bg-cream border border-muted/20 rounded px-2 py-1 mb-2';\n      b.textContent = message || 'AI mode paused for this month. Using site search instead.';\n      log.prepend(b);\n    }\n\n    // Render the \"Sources\" footer beneath an LLM answer so people can verify.\n    function renderSources(top) {\n      const items = top.map((r) => {\n        const d = r.d;\n        const url = `${base}${d.url.replace(/^\\//, '')}`;\n        const heading = d.heading && d.heading !== d.title\n          ? `${escapeHtml(d.title)} \\u2014 ${escapeHtml(d.heading)}`\n          : escapeHtml(d.title);\n        return `<li><a class=\"text-accent underline\" href=\"${url}\">${heading}</a></li>`;\n      }).join('');\n      return `<div class=\"mt-2 pt-2 border-t border-muted/20 text-[11px] text-muted\"><div class=\"mb-1\">Sources</div><ul class=\"space-y-0.5 list-disc list-inside\">${items}</ul></div>`;\n    }\n\n    // Stream a grounded answer from the worker into the given bubble.\n    // Returns true on success, false on any failure (caller falls back).\n    async function askWorker(question, top, bubble, shouldSpeak = false) {\n      if (!MAI_WORKER_URL || aiPaused) return false;\n      const chunks = top.map((r) => ({\n        title: r.d.heading && r.d.heading !== r.d.title\n          ? `${r.d.title} \\u2014 ${r.d.heading}`\n          : r.d.title,\n        text: r.d.text,\n        url: `${base}${r.d.url.replace(/^\\//, '')}`,\n      }));\n      const pageContext = getLiveContextPacket();\n      const conversation = getRecentConversation(10);\n      let res;\n      try {\n        res = await fetch(MAI_WORKER_URL, {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ question, chunks, pageContext, conversation }),\n        });\n      } catch {\n        return false; // network error \\u2014 fall back silently\n      }\n      if (res.status === 402) {\n        aiPaused = true;\n        showPausedBanner('AI mode paused for this month. Using site search instead.');\n        return false;\n      }\n      if (res.status === 429) {\n        // Per-IP rate limit on the worker's chat tier. Don't pretend the\n        // AI answered — say plainly that the budget is reached for this\n        // window and let the caller fall through to keyword retrieval so\n        // the visitor still gets something useful to look at.\n        aiPaused = true;\n        let retry = 300_000;\n        try {\n          const body = await res.clone().json();\n          if (body && body.retry_after_sec) retry = Math.min(3_600_000, body.retry_after_sec * 1000);\n        } catch {}\n        setTimeout(() => { aiPaused = false; }, retry);\n        showPausedBanner(\n          'AI answers are paused for this window — chat limit reached. Falling back to site search below.',\n        );\n        return false; // let caller render the retrieval list under the banner\n      }\n      if (!res.ok || !res.body) return false;\n\n      if (bubble) bubble.innerHTML = '<span class=\"mai-stream\"></span>';\n      const target = bubble?.querySelector('.mai-stream');\n      const reader = res.body.getReader();\n      const decoder = new TextDecoder();\n      let acc = '';\n      let spokeEarly = false;\n      // The model ends its reply with `[[NAV:n]]` (see personality.md).\n      // We never want the marker to flash in the DOM during streaming —\n      // strip any *trailing* marker-or-partial-marker before rendering each\n      // tick. The regex matches complete markers and every intermediate\n      // partial state we'd see streaming token-by-token: `[`, `[[`, `[[N`,\n      // `[[NAV`, `[[NAV:`, `[[NAV:1`, `[[NAV:1]`, `[[NAV:1]]`, plus the\n      // single-bracket and bare-`NAV:` variants gpt-4.1 sometimes emits.\n      // It only matches at end-of-string so it can't swallow earlier text.\n      const NAV_TRAILING_RE = /\\s*\\[\\[\\s*NAV\\s*:?\\s*\\d*\\s*\\]?\\]?\\s*$/i;\n      const ACTION_TRAILING_RE = /\\s*\\[\\[\\s*ACTION\\s*:?\\s*[^\\]]*\\]?\\]?\\s*$/i;\n      function stripTrailingMarker(s) {\n        return s.replace(NAV_TRAILING_RE, '').replace(ACTION_TRAILING_RE, '');\n      }\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        acc += decoder.decode(value, { stream: true });\n        if (target) {\n          // Render incrementally through the safe allowlist renderer so\n          // emphasis/links/lists from the model show up properly while\n          // partial markup can't break out. Hide any in-flight nav marker\n          // (full or partial) so it never flashes visible.\n          const display = stripTrailingMarker(acc);\n          target.innerHTML = safeRenderModelHtml(display);\n          log.scrollTop = log.scrollHeight;\n\n          if (shouldSpeak && !spokeEarly) {\n            const plain = display.replace(/\\s+/g, ' ').trim();\n            const firstSentence = plain.match(/^(.{40,220}?[.!?])(?:\\s|$)/);\n            if (firstSentence?.[1]) {\n              spokeEarly = true;\n              speak(firstSentence[1]);\n            }\n          }\n        }\n      }\n\n      // Stream done. Pull out the NAV marker (if any), strip it from the\n      // visible answer, and decide whether to surface a navigation CTA.\n      //\n      // Be liberal in what we accept: gpt-4.1 occasionally outputs\n      // `[[NAV: 2]]`, `[NAV:2]`, or wraps it in emphasis. We strip every\n      // plausible form so nothing leaks into the rendered answer.\n      let navIdx = null;\n      const action = parseActionMarker(acc);\n      const navPatterns = [\n        /\\[\\[\\s*NAV\\s*:\\s*(\\d+)\\s*\\]\\]/i,\n        /\\[\\s*NAV\\s*:\\s*(\\d+)\\s*\\]/i,\n        /\\bNAV\\s*:\\s*(\\d+)\\b/i,\n      ];\n      for (const re of navPatterns) {\n        const m = acc.match(re);\n        if (m) {\n          navIdx = parseInt(m[1], 10);\n          break;\n        }\n      }\n      // Always strip every variant of the marker from the visible answer,\n      // even when no number was parseable (so stray \"[[NAV:\" doesn't show).\n      acc = stripControlMarkers(acc);\n\n      // Fallback: if the model didn't emit a marker, default to the\n      // top-ranked chunk when its score is meaningfully above noise. This\n      // keeps the \"want me to take you there?\" affordance working even\n      // if the model ignores the format instruction.\n      let navTarget = null;\n      if (navIdx && navIdx > 0 && navIdx <= top.length) {\n        navTarget = top[navIdx - 1];\n      } else if (navIdx === 0) {\n        // Model explicitly said \"no destination\" — respect it.\n        navTarget = null;\n      } else if (top[0] && top[0].s >= 1.5) {\n        navTarget = top[0];\n      }\n\n      if (bubble) {\n        let html = safeRenderModelHtml(acc) + renderSources(top);\n\n        // Single navigation CTA path: if navigation is available via either\n        // [[NAV:n]] or [[ACTION:navigate|...]], show only the nav CTA.\n        let navOffer = null;\n        if (navTarget) {\n          const url = `${base}${navTarget.d.url.replace(/^\\//, '')}`;\n          const label = navTarget.d.heading && navTarget.d.heading !== navTarget.d.title\n            ? `${navTarget.d.title} — ${navTarget.d.heading}`\n            : navTarget.d.title;\n          navOffer = { url, label };\n        } else if (action && action.type === 'navigate') {\n          const rawTarget = String(action.target || '');\n          const url = /^https?:\\/\\//i.test(rawTarget)\n            ? rawTarget\n            : `${base}${rawTarget.replace(/^\\//, '')}`;\n          const label = action.label || `Go to ${pathToName(rawTarget)}`;\n          navOffer = { url, label };\n        }\n\n        if (navOffer) {\n          pendingNav = navOffer;\n          html += renderNavCta(navOffer.url, navOffer.label);\n        } else {\n          pendingNav = null;\n        }\n\n        // Keep explicit action CTAs for non-navigation asks only.\n        if (action && action.type !== 'navigate') {\n          pendingUiAction = action;\n          html += renderActionCta(action);\n        } else {\n          pendingUiAction = null;\n        }\n        bubble.innerHTML = html;\n      }\n      renderChips(getDynamicChips(question, top));\n      if (shouldSpeak && !spokeEarly) speak(acc);\n      saveTranscript();\n      return true;\n    }\n\n    // \"Want me to take you there?\" CTA appended beneath a streamed answer\n    // when the model emitted a [[NAV:n]] marker. Click handling is wired\n    // via event delegation on `log` (see below) so re-renders / transcript\n    // restores keep working.\n    function renderNavCta(url, label) {\n      const safeUrl = escapeHtml(url);\n      const safeLabel = escapeHtml(label);\n      return (\n        `<div class=\"mai-nav-cta mt-3 pt-2 border-t border-muted/20 text-[12px]\" data-mai-nav-cta data-nav-url=\"${safeUrl}\">` +\n          `<p class=\"text-muted mb-1.5\">Want me to take you to <em>${safeLabel}</em>?</p>` +\n          `<div class=\"flex gap-2\">` +\n            `<button type=\"button\" class=\"mai-nav-yes rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-nav-yes>Yes, take me</button>` +\n            `<button type=\"button\" class=\"mai-nav-no rounded border border-muted/30 text-muted px-2 py-1 text-[11px] hover:border-muted/60 transition-colors\" data-mai-nav-no>No thanks</button>` +\n          `</div>` +\n        `</div>`\n      );\n    }\n\n    // ---------- Intent classifier (deterministic, no AI) ----------\n    // We can't call a model, so we don't pretend to understand free-form\n    // intent. What we CAN do is recognize the *shape* of a request and\n    // respond honestly. Each branch returns a hand-written reply or hands\n    // off to retrieval. No randomness, no improvisation.\n    const INTENT = {\n      // Things M.ai literally cannot do — weather, jokes, the time, music,\n      // open-ended chat. We name the limit and redirect to the site.\n      smallTalk: /\\b(weather|temperature|forecast|joke|funny|sing|song|music|story|poem|chat|talk to me|bored|love|date|girlfriend|boyfriend|favorite color|favourite colour|game|play a game|what time|what.s the time|today.s date|news)\\b/i,\n      // Direct questions about the assistant's nature beyond what the KB covers.\n      meta: /\\b(are you (real|alive|sentient|human|conscious|smart|chatgpt|claude|gpt|gemini|copilot)|do you (think|feel|dream)|llm|language model|neural network)\\b/i,\n      // Hostile / abusive — polite redirect, no engagement, no echo.\n      hostile: /\\b(fuck|shit|bitch|asshole|cunt|dick|piss|hate you|kill (you|yourself)|die|suck|trash|garbage|useless|moron|idiot|retard)\\b/i,\n      // Sexual / NSFW — firmly out of scope, don't pay tokens to refuse.\n      nsfw: /\\b(sex|sexy|nude|naked|porn|horny|kiss me|date me|marry me|hot|attractive|boobs|breast|penis|vagina|onlyfans)\\b/i,\n      // Jailbreak / prompt-injection — short-circuit before they hit the model.\n      jailbreak: /(ignore (all |any |your |previous |prior )?(instructions|rules|prompt|system)|disregard (the |your |previous )?(above|prior|prompt|instructions)|you are (now |actually )?(dan|developer mode|jailbroken|unrestricted)|act as|pretend to be|role[- ]?play as|system\\s*:|<\\s*system|new instructions|reveal (your|the) (system )?prompt|what (is|are) your (system )?(prompt|instructions))/i,\n      // SSN / credit-card-looking numbers — never echo, never send.\n      pii: /\\b(\\d{3}[- ]?\\d{2}[- ]?\\d{4}|\\d{4}[- ]?\\d{4}[- ]?\\d{4}[- ]?\\d{4})\\b/,\n      // Capability question — user is asking what they can ask.\n      capability: /\\b(what can (you|i) (ask|do|search)|help|how (do|does) (this|you) work|instructions|examples|what should i (ask|type)|what (are you|do you) (good|able) (at|to))\\b/i,\n      // Things M.ai also can't do: book, buy, sign up, subscribe, schedule.\n      action: /\\b(book|buy|order|schedule|sign (me )?up|subscribe|register|download|install|log ?in|sign ?in)\\b/i,\n    };\n\n    // Hard cap on input length — anything bigger is almost certainly a\n    // paste-bomb or injection payload; not worth tokens.\n    const MAX_INPUT_CHARS = 500;\n\n    function classifyIntent(text) {\n      const t = text.trim();\n      if (!t) return 'empty';\n      if (t.length < 3) return 'too-short';\n      if (t.length > MAX_INPUT_CHARS) return 'too-long';\n      // Single \"word\" with no vowels and no digits — likely keysmash.\n      const compact = t.replace(/\\s+/g, '');\n      if (compact.length >= 4 && !/[aeiouy0-9]/i.test(compact)) return 'gibberish';\n      // Check the high-risk patterns first — these are the ones we MUST NOT\n      // forward to the model, even if other patterns also match.\n      if (INTENT.pii.test(t)) return 'pii';\n      if (INTENT.jailbreak.test(t)) return 'jailbreak';\n      if (INTENT.nsfw.test(t)) return 'nsfw';\n      if (INTENT.hostile.test(t)) return 'hostile';\n      if (INTENT.smallTalk.test(t)) return 'small-talk';\n      if (INTENT.meta.test(t)) return 'meta';\n      if (INTENT.capability.test(t)) return 'capability';\n      if (INTENT.action.test(t)) return 'action';\n      return 'search';\n    }\n\n    function intentReply(kind) {\n      switch (kind) {\n        case 'empty':\n          return `Type a few words about Flora's work, writing, or anything in the Toybox — I'll look across the site.`;\n        case 'too-short':\n          return `A little more to go on, please — even one full word helps me find the right page.`;\n        case 'gibberish':\n          return `That didn't come through as a question I can answer. Try a topic from the chips below.`;\n        case 'too-long':\n          return `That's a lot to take in at once — try a shorter question (a sentence or two) and I'll work from there.`;\n        case 'pii':\n          return `Please keep personal numbers out of the chat — I only need to know what to look up on Flora's site.`;\n        case 'jailbreak':\n          return `I don't take instructions from inside the chat. Ask about a topic on Flora's site and I'll work from what's actually there.`;\n        case 'nsfw':\n          return `That's not what I'm here for. Ask about Flora's writing, her work, or anything in the Toybox.`;\n        case 'hostile':\n          return `Let's keep it kind. Ask about Flora's work, her writing, or any page on the site and I'll help.`;\n        case 'small-talk':\n          return `That sits outside what I can speak to — I work from what Flora has published. Try a topic from the chips below.`;\n        case 'meta':\n          return `I'm M.ai — a thoughtful guide to Flora's work, writing, and ideas. I read across her published pages and synthesize an answer with citations. Not a general chatbot, and I won't speak to anything she hasn't put on the site.`;\n        case 'capability':\n          return `I can read across anything Flora has published — her <a class=\"text-accent underline\" href=\"${base}writing\">writing</a>, <a class=\"text-accent underline\" href=\"${base}work\">work</a>, <a class=\"text-accent underline\" href=\"${base}toybox\">Toybox</a>, and <a class=\"text-accent underline\" href=\"${base}play\">play</a> pages — and pull the relevant pieces together. Try a name, a topic, or a phrase from a post.`;\n        case 'action':\n          return `I don't take actions on Flora's behalf — no bookings, no signups. For anything that needs her directly, <a class=\"text-accent underline\" href=\"https://linkedin.com/in/floramaydelacruz\">LinkedIn</a> is the clearest path.`;\n        default:\n          return null;\n      }\n    }\n\n    // ---------- DOM ----------\n    const root = document.getElementById('mai-root');\n    if (!root) return;\n    const panel = document.getElementById('mai-panel');\n    const backdrop = document.getElementById('mai-backdrop');\n    const closeBtn = document.getElementById('mai-close');\n    const expandBtn = document.getElementById('mai-expand');\n    const expandIcon = document.getElementById('mai-expand-icon-expand');\n    const collapseIcon = document.getElementById('mai-expand-icon-collapse');\n    const log = document.getElementById('mai-log');\n    const classicNav = document.getElementById('mai-classic-nav');\n    const chipsBar = document.getElementById('mai-chips');\n    const topicsEl = document.getElementById('mai-topics');\n    const form = document.getElementById('mai-form');\n    const input = document.getElementById('mai-input');\n    const micBtn = document.getElementById('mai-mic');\n    const speakToggle = document.getElementById('mai-speak-toggle');\n    const MAI_MEDIUM_STORAGE_KEY = 'mai:response-medium';\n    const MAI_CONTEXT_LAST_PAGE_KEY = maiStorageKey('context:last-page');\n    const MAI_CONTEXT_TRANSITION_KEY = maiStorageKey('context:transition');\n    const MAI_CONTEXT_LAST_ACTION_KEY = maiStorageKey('context:last-action');\n    const MAI_CONTEXT_LAST_ACTION_RESULT_KEY = maiStorageKey('context:last-action-result');\n\n    let speakReplies = false;\n    let fullscreen = false;\n    function syncSpeakToggleUi() {\n      speakToggle.setAttribute('aria-pressed', speakReplies ? 'true' : 'false');\n      speakToggle.classList.toggle('text-accent', speakReplies);\n      speakToggle.classList.toggle('bg-accent/15', speakReplies);\n      speakToggle.classList.toggle('text-muted', !speakReplies);\n    }\n\n    function setResponseMedium(medium) {\n      speakReplies = medium === 'voice';\n      try { localStorage.setItem(MAI_MEDIUM_STORAGE_KEY, speakReplies ? 'voice' : 'text'); } catch (e) {}\n      syncSpeakToggleUi();\n    }\n\n    try {\n      speakReplies = localStorage.getItem(MAI_MEDIUM_STORAGE_KEY) === 'voice';\n    } catch (e) {\n      speakReplies = false;\n    }\n    syncSpeakToggleUi();\n\n    function track(name, params) {\n      try {\n        window.dispatchEvent(\n          new CustomEvent('site:track', { detail: { name: name, params: params || {} } }),\n        );\n      } catch (e) {}\n    }\n\n    function isComplexPage() {\n      const main = document.getElementById('main-content') || document.querySelector('main') || document.body;\n      if (!main) return { hasForm: false, hasDecision: false, hasWarning: false };\n      const hasForm = Boolean(main.querySelector('form'));\n      const hasDecision = Boolean(main.querySelector('select, input[type=radio], input[type=checkbox], [role=\"tablist\"], [aria-controls]'));\n      const hasWarning = Boolean(main.querySelector('[role=\"alert\"], [aria-live=\"assertive\"], .error, .warning'));\n      return { hasForm, hasDecision, hasWarning };\n    }\n\n    function getPageContext() {\n      const h1 = document.querySelector('main h1, h1');\n      const { hasForm, hasDecision, hasWarning } = isComplexPage();\n      return {\n        path: window.location.pathname,\n        title: trimText(document.title, 120),\n        heading: trimText(h1 ? h1.textContent : '', 120),\n        hasForm,\n        hasDecision,\n        hasWarning,\n        capturedAt: Date.now(),\n      };\n    }\n\n    function labelForActionTarget(el) {\n      if (!el) return '';\n      const aria = trimText(el.getAttribute?.('aria-label') || '', 120);\n      if (aria) return aria;\n      const title = trimText(el.getAttribute?.('title') || '', 120);\n      if (title) return title;\n      const text = trimText(el.textContent || '', 120);\n      if (text) return text;\n      if (el instanceof HTMLInputElement) {\n        if (el.name) return trimText(el.name, 120);\n        if (el.placeholder) return trimText(el.placeholder, 120);\n      }\n      return trimText(el.id || el.className || el.tagName || '', 120);\n    }\n\n    function saveLastAction(action) {\n      try {\n        sessionStorage.setItem(MAI_CONTEXT_LAST_ACTION_KEY, JSON.stringify(action));\n      } catch (e) {}\n    }\n\n    function setActionResult(result) {\n      try {\n        sessionStorage.setItem(\n          MAI_CONTEXT_LAST_ACTION_RESULT_KEY,\n          JSON.stringify({ ...result, capturedAt: Date.now() }),\n        );\n      } catch (e) {}\n    }\n\n    function initContextTracking() {\n      const current = getPageContext();\n      const previous = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_LAST_PAGE_KEY));\n      const from = previous && previous.path && previous.path !== current.path\n        ? { path: previous.path, title: previous.title || '', heading: previous.heading || '' }\n        : null;\n      const transition = {\n        from,\n        to: { path: current.path, title: current.title, heading: current.heading },\n        changedPage: Boolean(from),\n        capturedAt: Date.now(),\n      };\n      try {\n        sessionStorage.setItem(MAI_CONTEXT_LAST_PAGE_KEY, JSON.stringify(current));\n        sessionStorage.setItem(MAI_CONTEXT_TRANSITION_KEY, JSON.stringify(transition));\n      } catch (e) {}\n\n      document.addEventListener('click', (evt) => {\n        const target = evt.target instanceof Element\n          ? evt.target.closest('a, button, [role=\"button\"], input[type=\"submit\"]')\n          : null;\n        if (!target) return;\n        const href = target instanceof HTMLAnchorElement ? pathFromHref(target.getAttribute('href') || '') : '';\n        saveLastAction({\n          type: 'click',\n          label: labelForActionTarget(target),\n          href,\n          tag: target.tagName.toLowerCase(),\n          capturedAt: Date.now(),\n        });\n      }, { capture: true, passive: true });\n\n      document.addEventListener('change', (evt) => {\n        const target = evt.target;\n        if (!(target instanceof HTMLInputElement || target instanceof HTMLSelectElement || target instanceof HTMLTextAreaElement)) return;\n        saveLastAction({\n          type: 'change',\n          label: labelForActionTarget(target),\n          field: trimText(target.name || target.id || target.tagName.toLowerCase(), 120),\n          capturedAt: Date.now(),\n        });\n      }, { capture: true, passive: true });\n    }\n\n    function getLiveContextPacket() {\n      const transition = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_TRANSITION_KEY));\n      const lastAction = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_LAST_ACTION_KEY));\n      const lastActionResult = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_LAST_ACTION_RESULT_KEY));\n      return {\n        page: getPageContext(),\n        transition: transition || null,\n        lastAction: lastAction || null,\n        lastActionResult: lastActionResult || null,\n        responseMedium: speakReplies ? 'voice' : 'text',\n        panelState: {\n          open: document.documentElement.classList.contains('mai-open'),\n          fullscreen,\n        },\n      };\n    }\n\n    function getRecentConversation(maxItems = 8) {\n      const bubbles = Array.from(log.querySelectorAll('[data-role]'));\n      const rows = [];\n      for (const bubble of bubbles) {\n        const role = bubble.getAttribute('data-role') || '';\n        const text = trimText(stripHtml(bubble.innerHTML || ''), 320);\n        if (!text) continue;\n        if (/^(looking through|reading the relevant pages|transcribing with whisper|sending)/i.test(text)) continue;\n        rows.push({ role, content: text });\n      }\n      return rows.slice(-maxItems);\n    }\n\n    initContextTracking();\n\n    // ---------- Open / close ----------\n    // Persistence follows the sticky-view-state playbook: panel open/closed\n    // survives navigation within the tab (sessionStorage), dies on tab\n    // close or hard refresh of a tab where it was never opened. We skip\n    // focus-stealing on restore so the page doesn't grab the cursor every\n    // time the user navigates with the rail already open.\n    // Pending re-hide timer for the close transition. We set `panel.hidden`\n    // only AFTER the slide-out finishes so the panel can animate out; if\n    // open() is called before that lands, we cancel it.\n    var _closeTimer = null;\n    var _warmTimer = null;\n    var _warmScheduled = false;\n\n    function scheduleWarmIndex() {\n      if (_warmScheduled) return;\n      _warmScheduled = true;\n\n      function runWarm() {\n        _warmTimer = null;\n        loadVocab();\n      }\n\n      // Let first paint settle before warming retrieval assets.\n      if (typeof window.requestIdleCallback === 'function') {\n        window.requestIdleCallback(runWarm, { timeout: 1800 });\n      } else {\n        _warmTimer = window.setTimeout(runWarm, 900);\n      }\n    }\n    function open(opts) {\n      var restoring = opts && opts.restoring;\n      var source = (opts && opts.source) || 'unknown';\n      var fullscreenRequested = Boolean(opts && opts.fullscreen);\n      if (restoring) document.documentElement.classList.add('mai-restoring');\n      // Cancel any in-flight close so a fast reopen doesn't re-hide\n      // the panel 200ms in.\n      if (_closeTimer) { clearTimeout(_closeTimer); _closeTimer = null; }\n      panel.hidden = false;\n      backdrop.hidden = false;\n      // Force a reflow so the panel's initial off-screen transform\n      // (translateX(100%)) gets committed BEFORE we add mai-open. Without\n      // this, the browser collapses both styles into one paint and the\n      // slide-in skips its transition.\n      void panel.offsetWidth;\n      document.documentElement.classList.add('mai-no-scroll');\n      document.documentElement.classList.add('mai-open');\n      if (fullscreenRequested && !fullscreen) setFullscreen(true);\n      try {\n        sessionStorage.setItem(maiStorageKey('open'), 'true');\n        // Keep the legacy key for pre-paint compatibility in BaseLayout.\n        sessionStorage.setItem('mai-open', 'true');\n      } catch (e) {}\n      if (!restoring) track('mai_open', { source: source });\n      // Warm retrieval assets once the page is idle so first paint and\n      // panel animation on /mai are not competing with heavy JSON work.\n      scheduleWarmIndex();\n      if (!restoring) setTimeout(() => input.focus({ preventScroll: true }), 50);\n      if (restoring) {\n        requestAnimationFrame(() => {\n          document.documentElement.classList.remove('mai-restoring');\n        });\n      }\n    }\n    function close() {\n      const onMaiRoute = isMaiRoute();\n      document.documentElement.classList.remove('mai-no-scroll');\n      document.documentElement.classList.remove('mai-open');\n      try {\n        sessionStorage.removeItem(maiStorageKey('open'));\n        sessionStorage.removeItem('mai-open');\n      } catch (e) {}\n      track('mai_close', { fullscreen: fullscreen ? 'true' : 'false' });\n      if (fullscreen) setFullscreen(false);\n      stopSpeaking();\n      // Keep the panel + backdrop in the DOM so the slide-out animates;\n      // re-apply `hidden` only after the transform settles. Match the\n      // 200ms transform/opacity duration in CSS.\n      if (_closeTimer) clearTimeout(_closeTimer);\n      _closeTimer = setTimeout(function () {\n        panel.hidden = true;\n        backdrop.hidden = true;\n        _closeTimer = null;\n        if (onMaiRoute) {\n          window.location.replace('/');\n        }\n      }, 220);\n    }\n    closeBtn.addEventListener('click', close);\n    backdrop.addEventListener('click', close);\n\n    function triggerPrompt(prompt, autoSend) {\n      const q = (prompt || '').trim();\n      if (!q) return;\n      input.value = q;\n      if (autoSend) {\n        handle(q, { explicitMedium: 'text' });\n      } else {\n        input.focus({ preventScroll: true });\n      }\n    }\n\n    // Bind any topbar trigger (or anywhere on the site) marked data-mai-open.\n    // Also listen for a dispatched event so callers in other scripts can open\n    // the drawer programmatically without a DOM lookup.\n    document.querySelectorAll('[data-mai-open]').forEach((el) => {\n      el.addEventListener('click', (e) => {\n        e.preventDefault();\n        const source = el.getAttribute('data-mai-source') || 'nav';\n        const prompt = el.getAttribute('data-mai-prompt') || '';\n        const autoSend = el.getAttribute('data-mai-autosend') !== 'false';\n        open({ source });\n        if (prompt) {\n          setTimeout(() => triggerPrompt(prompt, autoSend), 90);\n        }\n      });\n    });\n    window.addEventListener('mai:open', (evt) => {\n      const detail = evt && evt.detail ? evt.detail : {};\n      const source = detail.source || 'programmatic';\n      const prompt = detail.prompt || '';\n      const autoSend = detail.autoSend !== false;\n      const fullscreen = detail.forceFullscreen === true;\n      open({ source, fullscreen });\n      if (prompt) {\n        setTimeout(() => triggerPrompt(prompt, autoSend), 90);\n      }\n    });\n\n    function consumePendingOpen() {\n      try {\n        const raw = sessionStorage.getItem('mai:pending-open');\n        if (!raw) return;\n        sessionStorage.removeItem('mai:pending-open');\n        const detail = JSON.parse(raw) || {};\n        const source = detail.source || 'mai_pending_open';\n        const prompt = detail.prompt || '';\n        const autoSend = detail.autoSend !== false;\n        const fullscreen = detail.forceFullscreen === true;\n        open({ source, fullscreen });\n        if (prompt) {\n          setTimeout(() => triggerPrompt(prompt, autoSend), 90);\n        }\n      } catch (e) {}\n    }\n\n    // Restore from sessionStorage. The pre-paint script in BaseLayout\n    // already set html.mai-open so the body shift is in place; we just\n    // need to unhide the aside, hide the FAB, and not steal focus.\n    try {\n      const savedVersion = localStorage.getItem('mai:client-version');\n      if (savedVersion !== MAI_CLIENT_VERSION) {\n        clearLegacyMaiState();\n        localStorage.setItem('mai:client-version', MAI_CLIENT_VERSION);\n      }\n      // Detect a real page refresh — if so, reset the conversation.\n      // Otherwise hydrate the log + chips so the convo survives in-tab nav.\n      const navEntry = performance.getEntriesByType && performance.getEntriesByType('navigation')[0];\n      const isReload = navEntry\n        ? navEntry.type === 'reload'\n        : (performance.navigation && performance.navigation.type === 1);\n      if (isReload) {\n        localStorage.removeItem(maiStorageKey('transcript'));\n        localStorage.removeItem(maiStorageKey('chips'));\n        localStorage.removeItem(maiStorageKey('topics-hidden'));\n        sessionStorage.removeItem(maiStorageKey('open'));\n        sessionStorage.removeItem('mai-open');\n      } else {\n        const t = localStorage.getItem(maiStorageKey('transcript'));\n        if (t) {\n          log.innerHTML = t;\n          log.scrollTop = log.scrollHeight;\n        }\n        const c = localStorage.getItem(maiStorageKey('chips'));\n        if (c) {\n          chipsBar.innerHTML = c;\n          rewireChipButtons();\n        }\n        if (localStorage.getItem(maiStorageKey('topics-hidden')) === '1' && topicsEl) {\n          topicsEl.hidden = true;\n        }\n      }\n      if (\n        sessionStorage.getItem(maiStorageKey('open')) === 'true' ||\n        sessionStorage.getItem('mai-open') === 'true'\n      ) {\n        open({ restoring: true });\n      }\n\n      consumePendingOpen();\n\n    } catch (e) {}\n\n    // ---------- Expand / collapse ----------\n    // URL state: expanding to fullscreen pushes /mai so the experience is\n    // linkable + shareable; collapsing pops back to the previous path. The\n    // dedicated /mai page dispatches mai:open with forceFullscreen so direct\n    // navigation lands users in the same fullscreen experience without a\n    // duplicate implementation.\n    const MAI_PATH = '/mai/';\n    let _prevPathBeforeFullscreen = null;\n\n    function isMaiRoute(pathname) {\n      const path = (pathname || window.location.pathname || '').replace(/\\/+$/, '');\n      return path === '/mai';\n    }\n\n    function setFullscreen(on) {\n      fullscreen = on;\n      panel.classList.toggle('mai-fullscreen', on);\n      document.documentElement.classList.toggle('mai-fullscreen-open', on);\n      if (classicNav) classicNav.hidden = !on;\n      expandBtn.setAttribute('aria-pressed', on ? 'true' : 'false');\n      expandBtn.setAttribute('aria-label', on ? 'Collapse M.ai' : 'Expand M.ai to full screen');\n      expandBtn.setAttribute('title', on ? 'Collapse' : 'Expand');\n      expandIcon.hidden = on;\n      collapseIcon.hidden = !on;\n    }\n\n    function toggleFullscreenFromUI() {\n      const goingFullscreen = !fullscreen;\n      try {\n        if (goingFullscreen && !isMaiRoute()) {\n          _prevPathBeforeFullscreen =\n            window.location.pathname + window.location.search + window.location.hash;\n          history.pushState(\n            { mai: 'fullscreen', prev: _prevPathBeforeFullscreen },\n            '',\n            MAI_PATH,\n          );\n        } else if (!goingFullscreen && isMaiRoute()) {\n          const target = _prevPathBeforeFullscreen || '/';\n          _prevPathBeforeFullscreen = null;\n          history.pushState({ mai: 'collapsed' }, '', target);\n        }\n      } catch (e) {}\n      setFullscreen(goingFullscreen);\n    }\n\n    expandBtn.addEventListener('click', toggleFullscreenFromUI);\n\n    // Sync UI when browser back/forward crosses /mai.\n    window.addEventListener('popstate', () => {\n      const onMai = isMaiRoute();\n      if (onMai && !fullscreen) {\n        if (panel.hidden) open({ source: 'popstate_mai', restoring: true, fullscreen: true });\n        setFullscreen(true);\n      } else if (!onMai && fullscreen) {\n        setFullscreen(false);\n      }\n    });\n\n    // If this page loaded directly at /mai, ensure fullscreen experience is on.\n    setTimeout(() => {\n      if (isMaiRoute()) {\n        if (panel.hidden) open({ source: 'mai_route', restoring: true, fullscreen: true });\n        if (!fullscreen) setFullscreen(true);\n      }\n    }, 0);\n\n    // Topic buttons \\u2014 each fires the same flow as a typed/spoken question.\n    topicsEl.querySelectorAll('.mai-topic').forEach((btn) => {\n      btn.addEventListener('click', () => {\n        const q = btn.getAttribute('data-q') || '';\n        track('mai_topic_click', { topic: q });\n        handle(q, { explicitMedium: 'text' });\n      });\n    });\n\n    let _bubbleSeq = 0;\n\n    // ---------- Transcript persistence ----------\n    // Per the sticky-view-state playbook: in-tab nav preserves the\n    // conversation; a real refresh resets it; closing the tab forgets\n    // everything. Scope = sessionStorage. Reload detected via\n    // PerformanceNavigationTiming.\n    function saveTranscript() {\n      try {\n        // Don't snapshot mid-stream — the bubble would restore as a\n        // half-rendered placeholder. The final write at end-of-stream\n        // catches it.\n        if (log.querySelector('.mai-stream')) return;\n        localStorage.setItem(maiStorageKey('transcript'), log.innerHTML);\n        localStorage.setItem(maiStorageKey('chips'), chipsBar.innerHTML);\n        if (topicsEl) localStorage.setItem(maiStorageKey('topics-hidden'), topicsEl.hidden ? '1' : '0');\n      } catch (e) {}\n    }\n    function rewireChipButtons() {\n      // innerHTML restore loses click handlers — reattach them so\n      // restored suggestion chips remain interactive.\n      chipsBar.querySelectorAll('button').forEach((b) => {\n        b.addEventListener('click', () => handle(b.textContent || '', { explicitMedium: 'text' }));\n      });\n    }\n\n    function addBubble(who, html) {\n      const wrap = document.createElement('div');\n      wrap.className = who === 'mai'\n        ? 'flex justify-start'\n        : 'flex justify-end';\n      const bubble = document.createElement('div');\n      const id = `mai-bubble-${++_bubbleSeq}`;\n      bubble.id = id;\n      bubble.dataset.role = who;\n      bubble.className = who === 'mai'\n        ? 'max-w-[85%] rounded-lg bg-cream text-ink px-3 py-2 space-y-2'\n        : 'max-w-[85%] rounded-lg bg-ink text-paper px-3 py-2';\n      bubble.innerHTML = html;\n      wrap.appendChild(bubble);\n      log.appendChild(wrap);\n      log.scrollTop = log.scrollHeight;\n      saveTranscript();\n      return id;\n    }\n\n    function renderChips(items) {\n      chipsBar.innerHTML = '';\n      if (!items || !items.length) return;\n      for (const text of items) {\n        const b = document.createElement('button');\n        b.type = 'button';\n        b.textContent = text;\n        b.className = 'rounded-full border border-ink/15 px-2.5 py-1 text-[11px] text-muted hover:border-accent hover:text-accent transition-colors';\n        b.addEventListener('click', () => handle(text, { explicitMedium: 'text' }));\n        chipsBar.appendChild(b);\n      }\n      saveTranscript();\n    }\n\n    function handle(text, options = {}) {\n      const raw = (text || '').trim();\n      if (!raw) return;\n      if (options.explicitMedium === 'voice') setResponseMedium('voice');\n      else if (options.explicitMedium === 'text') setResponseMedium('text');\n      const shouldSpeakThisTurn = speakReplies || Boolean(options.fromVoice);\n      // Apply speech-to-text corrections (toy box → toybox, M A I → M.ai,\n      // play book → playbook, etc.). Helps both spoken AND typed inputs.\n      let clean = expandSpeech(raw);\n\n      // 0) If a message-to-Flora flow is in progress, route every input\n      // through its state machine. Returns true if consumed.\n      if (messageFlow) {\n        progressMessageFlow(clean);\n        return;\n      }\n\n      // Detect \"I want to send a message\" / \"leave a message\" / \"tell Flora\"\n      // / \"message her\" intents and start the multi-turn flow. Done before\n      // intent classification so these don't fall through to the LLM.\n      // Loose \".{0,30}\" between verb and noun so \"send Flora a message\"\n      // matches as easily as \"send a message\".\n      const MESSAGE_INTENT_RE = /\\b(send|leave|drop|forward)\\b.{0,30}\\b(message|note)\\b|\\b(message|tell|write to|reach out to)\\s+(her|flora|flora\\s*may)\\b|\\bi(?:'| ?a)?d (?:like|love|want) to (?:contact|reach|message|write|leave a message|email|tell)\\b/i;\n      if (MESSAGE_INTENT_RE.test(clean)) {\n        addBubble('user', escapeHtml(raw));\n        input.value = '';\n        startMessageFlow('intent');\n        return;\n      }\n\n      track('mai_query_submit', {\n        source: options.fromVoice ? 'voice' : 'text',\n        query_length: clean.length,\n      });\n\n      // If a navigation offer is pending from the last answer and the user's\n      // input is a short yes/no, honor it instead of treating it as a new\n      // question. Anything else just clears the pending offer and falls\n      // through to normal handling.\n      if (pendingNav) {\n        const t = clean.toLowerCase();\n        const AFFIRM = /^(y|ya|ye|yes+|yea+h?|yep|yup|sure|ok(ay)?|please|do it|go|take me( there)?|let'?s go|sounds good|absolutely|definitely)[.! ]*$/;\n        const DENY = /^(no+|nope|nah|cancel|don'?t|stop|not now|later)[.! ]*$/;\n        if (AFFIRM.test(t)) {\n          const target = pendingNav;\n          pendingNav = null;\n          track('mai_nav_cta_click', { action: 'accept' });\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          addBubble('mai', `Taking you to <em>${escapeHtml(target.label)}</em>…`);\n          saveTranscript();\n          // Close the panel on mobile only (full-screen drawer would hide\n          // the destination). Desktop keeps the side rail open so the\n          // conversation continues alongside the new page. Small delay so\n          // the confirmation bubble paints before the page unloads.\n          const isMobile = window.matchMedia('(max-width: 767px)').matches;\n          setTimeout(() => {\n            if (isMobile) close();\n            window.location.assign(target.url);\n          }, 250);\n          return;\n        }\n        if (DENY.test(t)) {\n          pendingNav = null;\n          track('mai_nav_cta_click', { action: 'decline' });\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          addBubble('mai', 'No problem — what else can I help you find?');\n          // Strip the CTA from the last assistant bubble so it doesn't\n          // linger as a dangling offer.\n          const ctas = log.querySelectorAll('[data-mai-nav-cta]');\n          if (ctas.length) ctas[ctas.length - 1].remove();\n          renderChips(STARTER_CHIPS);\n          saveTranscript();\n          return;\n        }\n        // Neither — treat as a new question. Drop the offer silently.\n        pendingNav = null;\n      }\n\n      if (pendingUiAction) {\n        const t = clean.toLowerCase();\n        const AFFIRM = /^(y|ya|ye|yes+|yea+h?|yep|yup|sure|ok(ay)?|please|do it|go|run it|do that|sounds good|absolutely|definitely)[.! ]*$/;\n        const DENY = /^(no+|nope|nah|cancel|don'?t|stop|not now|later)[.! ]*$/;\n        if (AFFIRM.test(t)) {\n          const action = pendingUiAction;\n          pendingUiAction = null;\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          executeUiAction(action, 'typed_confirm');\n          saveTranscript();\n          return;\n        }\n        if (DENY.test(t)) {\n          const action = pendingUiAction;\n          pendingUiAction = null;\n          setActionResult({\n            type: action.type,\n            status: 'declined',\n            target: action.target,\n            label: action.label,\n            source: 'typed_decline',\n          });\n          track('mai_action_execute', { type: action.type, status: 'declined', source: 'typed_decline' });\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          addBubble('mai', 'Understood. I will not run that action.');\n          const ctas = log.querySelectorAll('[data-mai-action-cta]');\n          if (ctas.length) ctas[ctas.length - 1].remove();\n          saveTranscript();\n          return;\n        }\n      }\n      // If the vocabulary is already loaded (we kick that off when the\n      // panel opens), repair any unknown words against the indexed terms.\n      // Catches misspellings the hand-listed table can't anticipate:\n      // \"packbot\" → \"pacbot\", \"vehra\" → \"vera\", \"miendelf\" → \"mindelf\",\n      // \"tabloos\" → \"tabulas\". Best-effort: if vocab isn't ready yet on\n      // the very first query, BM25 retrieval will still try the raw form.\n      // Keep the visitor's original wording intact for display + LLM,\n      // and use corrected text only for retrieval ranking.\n      let retrievalQuery = clean;\n      if (_vocabReady) retrievalQuery = fuzzyCorrectQuery(retrievalQuery, _vocabReady);\n      // First interaction collapses the topic grid — the conversation owns\n      // the panel from this point on. The chips bar still surfaces topics.\n      if (topicsEl && !topicsEl.hidden) topicsEl.hidden = true;\n      addBubble('user', escapeHtml(raw));\n      input.value = '';\n\n      // 1) Guard rails first — anything abusive, NSFW, an injection attempt,\n      // a paste-bomb, PII, or a meta question gets a canned reply BEFORE\n      // we burn any tokens calling the worker. The KB matcher doesn't get\n      // a chance to misroute these either.\n      const preIntent = classifyIntent(clean);\n      const HARD_BLOCK = new Set(['hostile', 'nsfw', 'jailbreak', 'pii', 'too-long', 'meta']);\n      if (HARD_BLOCK.has(preIntent)) {\n        const reply = intentReply(preIntent);\n        addBubble('mai', reply);\n        renderChips(getDynamicChips(clean));\n        if (shouldSpeakThisTurn) speak(stripHtml(reply));\n        return;\n      }\n\n      // 2) Identity / greeting / contact — hand-written wins outright.\n      const kb = kbAnswerFor(clean);\n      if (kb) {\n        addBubble('mai', kb.answer);\n        renderChips(kb.followups || STARTER_CHIPS);\n        if (shouldSpeakThisTurn) speak(stripHtml(kb.answer));\n        return;\n      }\n\n      // 2.5) Deterministic context/navigation intents. These should not\n      // depend on keyword retrieval quality.\n      const asksPageContext = /\\b(what page am i(?: in| on)?|where am i|what page is this|which page am i on)\\b/i.test(clean);\n      const asksOldestPlaybook = /\\b(oldest|first|earliest)\\s+playbook\\b/i.test(clean);\n      if (asksPageContext || asksOldestPlaybook) {\n        const page = getPageContext();\n        const pagePath = page.path || window.location.pathname || '/';\n        const pageLabel = trimText(pathToName(pagePath), 120);\n        const pageLine = `You're on <strong>${escapeHtml(pageLabel)}</strong>.`;\n\n        if (!asksOldestPlaybook) {\n          addBubble('mai', `${pageLine}<br><br>If you want, I can navigate you to Work, Writing, Toybox, or About.`);\n          renderChips(getDynamicChips(clean));\n          if (shouldSpeakThisTurn) speak(stripHtml(pageLine));\n          return;\n        }\n\n        const pendingId = addBubble(\n          'mai',\n          '<span class=\"mai-thinking text-muted\">Checking the Toybox chronology<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>',\n        );\n        loadIndex().then((index) => {\n          const bubble = document.getElementById(pendingId);\n          if (!bubble) return;\n          const oldest = pickOldestPlaybook(index);\n          if (!oldest?.first) {\n            bubble.innerHTML = `${pageLine}<br><br>I couldn't resolve a published playbook chronology right now, but I can take you to <a class=\"text-accent underline\" href=\"${base}toybox\">Toybox</a>.`;\n            renderChips(getDynamicChips(clean));\n            if (shouldSpeakThisTurn) speak('I could not resolve the oldest playbook right now, but I can take you to Toybox.');\n            saveTranscript();\n            return;\n          }\n\n          const pick = oldest.first;\n          const action = normalizeUiAction('navigate', pick.url, `Open ${pick.title}`);\n          pendingUiAction = action;\n          const tieMsg = oldest.tieCount > 1\n            ? `Multiple playbooks share the oldest published date (${escapeHtml(pick.updated || 'unknown')}); I picked ${escapeHtml(pick.title)} first.`\n            : `The oldest playbook by published date is <em>${escapeHtml(pick.title)}</em>${pick.updated ? ` (${escapeHtml(pick.updated)})` : ''}.`;\n          bubble.innerHTML = `${pageLine}<br><br>${tieMsg}${action ? renderActionCta(action) : ''}`;\n          renderChips(getDynamicChips(clean));\n          if (shouldSpeakThisTurn) speak(stripHtml(`${pageLine} ${tieMsg}`));\n          saveTranscript();\n        });\n        return;\n      }\n\n      // 3) Any other non-search shape — honest, scripted reply.\n      const intent = preIntent;\n      if (intent !== 'search') {\n        const reply = intentReply(intent);\n        addBubble('mai', reply);\n        renderChips(getDynamicChips(clean));\n        if (shouldSpeakThisTurn) speak(stripHtml(reply));\n        return;\n      }\n\n      // 4) Otherwise, search the site. Index loads lazily on first query.\n      const thinkingId = addBubble('mai', '<span class=\"mai-thinking text-muted\">Looking through Flora&rsquo;s work<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>');\n      loadIndex().then(async (index) => {\n        // Replace the placeholder bubble in-place so it doesn't pile up.\n        const bubble = document.getElementById(thinkingId);\n        if (!index || !index.docs?.length) {\n          if (bubble) bubble.innerHTML = noMatchAnswer(raw);\n          renderChips(getDynamicChips(clean));\n          saveTranscript();\n          return;\n        }\n        // If fuzzy correction didn't get a chance pre-KB (first-ever\n        // query race), do it now against the loaded vocab so BM25 has\n        // a fair shot at matching mis-spelled proper nouns.\n        if (!_vocabReady) await loadVocab();\n        if (_vocabReady) retrievalQuery = fuzzyCorrectQuery(clean, _vocabReady);\n        const scored = rank(index, retrievalQuery);\n        // Send a wider context to the LLM — it can pick the relevant bits\n        // and ignore the rest. Cheaper than refusing a question we COULD have\n        // answered if we'd looked further down the list.\n        const top = dedupeByDoc(scored, 6);\n        // Low floor: if anything scores above noise, give the LLM a chance.\n        // The model is grounded by the chunks and will refuse on its own if\n        // none are relevant — much rarer than the old client-side cutoff.\n        if (!top.length || top[0].s < 0.6) {\n          if (bubble) bubble.innerHTML = noMatchAnswer(raw);\n          renderChips(getDynamicChips(clean));\n          if (shouldSpeakThisTurn) speak(stripHtml(noMatchAnswer(raw)));\n          saveTranscript();\n          return;\n        }\n        // If the LLM tier is configured + healthy, ask it to write a\n        // grounded answer using the top chunks. Otherwise (or on any\n        // failure) fall through to the keyword-result list \\u2014 the user\n        // always gets something useful.\n        if (MAI_WORKER_URL && !aiPaused) {\n          if (bubble) bubble.innerHTML = '<span class=\"mai-thinking text-muted\">Reading the relevant pages<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>';\n          const ok = await askWorker(raw, top, bubble, shouldSpeakThisTurn);\n          if (ok) {\n            renderChips(STARTER_CHIPS);\n            return;\n          }\n          // Fell through \\u2014 fall back to plain retrieval below.\n        }\n        const html = renderRetrieval(top, retrievalQuery);\n        if (bubble) bubble.innerHTML = html;\n        renderChips(getDynamicChips(clean, top));\n        if (shouldSpeakThisTurn) {\n          // Read the top result's heading + snippet aloud \\u2014 not the whole list.\n          const first = top[0].d;\n          speak(`${first.title}. ${stripHtml(snippet(first.text, clean))}`);\n        }\n      });\n    }\n\n    form.addEventListener('submit', (e) => {\n      e.preventDefault();\n      handle(input.value, { explicitMedium: 'text' });\n    });\n\n    // Delegated handler for the \"Want me to take you there?\" CTA buttons\n    // appended at the end of streamed answers (see renderNavCta). Lives on\n    // `log` so it survives re-renders and transcript restores.\n    log.addEventListener('click', (e) => {\n      const yes = e.target.closest('[data-mai-nav-yes]');\n      const no = e.target.closest('[data-mai-nav-no]');\n      if (!yes && !no) return;\n      const cta = (yes || no).closest('[data-mai-nav-cta]');\n      if (!cta) return;\n      if (yes) {\n        const url = cta.getAttribute('data-nav-url') || '';\n        pendingNav = null;\n        track('mai_nav_cta_click', { action: 'accept_button' });\n        if (url) {\n          // On mobile the panel is a full-screen drawer that would cover\n          // the destination page while it loads, so close it first for\n          // immediate visual feedback (close() also clears the `mai-open`\n          // sessionStorage flag so the next page doesn't auto-reopen it).\n          // On desktop the panel is a 24rem side rail — the page is\n          // already visible alongside it, so leave it open.\n          const isMobile = window.matchMedia('(max-width: 767px)').matches;\n          if (isMobile) close();\n          window.location.assign(url);\n        }\n        return;\n      }\n      // No thanks — remove the CTA and clear any pending offer.\n      pendingNav = null;\n      track('mai_nav_cta_click', { action: 'decline_button' });\n      cta.remove();\n      saveTranscript();\n    });\n\n    log.addEventListener('click', (e) => {\n      const run = e.target.closest('[data-mai-action-run]');\n      const no = e.target.closest('[data-mai-action-no]');\n      if (!run && !no) return;\n      const cta = (run || no).closest('[data-mai-action-cta]');\n      if (!cta) return;\n      const action = normalizeUiAction(\n        cta.getAttribute('data-action-type') || '',\n        cta.getAttribute('data-action-target') || '',\n        cta.getAttribute('data-action-label') || '',\n      );\n      if (!action) {\n        cta.remove();\n        pendingUiAction = null;\n        saveTranscript();\n        return;\n      }\n      if (run) {\n        pendingUiAction = null;\n        cta.remove();\n        executeUiAction(action, 'cta_button');\n        saveTranscript();\n        return;\n      }\n      pendingUiAction = null;\n      setActionResult({\n        type: action.type,\n        status: 'declined',\n        target: action.target,\n        label: action.label,\n        source: 'cta_decline',\n      });\n      track('mai_action_execute', { type: action.type, status: 'declined', source: 'cta_decline' });\n      cta.remove();\n      addBubble('mai', 'No problem — I will leave the page as-is.');\n      saveTranscript();\n    });\n\n    // Delegated handler for the \"Or leave a message here\" button rendered\n    // by the contact KB answer. Kicks off the multi-turn message flow.\n    log.addEventListener('click', (e) => {\n      const trigger = e.target.closest('[data-mai-start-message]');\n      if (!trigger) return;\n      if (messageFlow) return; // already in flow\n      startMessageFlow('contact_cta');\n    });\n\n    log.addEventListener('click', async (e) => {\n      const trigger = e.target.closest('[data-mai-play-pending-audio]');\n      if (!trigger || !pendingAudioUrl) return;\n      stopSpeaking();\n      const url = pendingAudioUrl;\n      pendingAudioUrl = null;\n      try {\n        await playAudioUrl(url);\n        const wrap = trigger.closest('.flex');\n        if (wrap) wrap.remove();\n      } catch {\n        pendingAudioUrl = url;\n        addBubble('mai', `Audio playback is still blocked on this device.`);\n      }\n    });\n\n    // Delegated handler for the message-flow confirm step (Send / Cancel\n    // buttons rendered into the confirm bubble).\n    log.addEventListener('click', (e) => {\n      const send = e.target.closest('[data-mai-msg-send]');\n      const cancel = e.target.closest('[data-mai-msg-cancel]');\n      if (!send && !cancel) return;\n      const cta = (send || cancel).closest('[data-mai-msg-cta]');\n      if (cta) cta.remove();\n      if (send) {\n        if (messageFlow && messageFlow.stage === 'confirm') {\n          sendMessageFlow();\n        }\n        return;\n      }\n      // Cancel\n      messageFlow = null;\n      track('mai_message_flow_cancel', { source: 'confirm_cta' });\n      const prompt = `No problem — nothing sent. What else can I help with?`;\n      addBubble('mai', prompt);\n      if (speakReplies) speak(prompt);\n      renderChips(STARTER_CHIPS);\n      saveTranscript();\n    });\n\n    // ---------- Voice in (Whisper via worker, with SpeechRecognition fallback) ----------\n    const SR = window.SpeechRecognition || window.webkitSpeechRecognition;\n    const UA = navigator.userAgent || '';\n    // iOS Safari technically exposes webkitSpeechRecognition but the\n    // implementation is unreliable in the wild — frequent silent failures,\n    // no recognition events on some iOS versions. Warn the user once.\n    const IS_IOS = /iPad|iPhone|iPod/.test(UA) && !window.MSStream;\n    const IS_ELECTRON = /\\bElectron\\/\\d+/i.test(UA);\n    const IS_EDGE = /\\bEdg\\//.test(UA);\n    const WORKER_BASE_URL = (MAI_WORKER_URL || '').replace(/\\/$/, '');\n    const TTS_VOICE = 'nova';\n    const TRANSCRIBE_HINT =\n      'Names may include Flora May dela Cruz, M.ai, Toybox, PacBot, Mindelf, Pale and Bright, CodeCatalyst, Vera, Tabulas.';\n    let recog = null;\n    let listening = false;\n    let whisperRecorder = null;\n    let whisperStream = null;\n    let whisperChunks = [];\n    let whisperMime = '';\n    let whisperAudioContext = null;\n    let whisperAnalyser = null;\n    let whisperSource = null;\n    let whisperMonitorRaf = null;\n    let whisperSilentSince = 0;\n    let whisperStartedAt = 0;\n    let currentAudio = null;\n    let currentAudioUrl = null;\n    let pendingAudioUrl = null;\n    let ttsAbort = null;\n    // Track the \"Listening…\" placeholder bubble so we can clear it on result\n    // or error instead of leaving a ghost in the log.\n    let listeningBubbleId = null;\n    // Safety net: if no result/end/error fires within this window (e.g. user\n    // ignored the permission popup, or recognition silently hung), abort.\n    let micTimeout = null;\n    function clearListeningBubble() {\n      if (!listeningBubbleId) return;\n      const el = document.getElementById(listeningBubbleId);\n      if (el && el.parentElement) el.parentElement.remove();\n      listeningBubbleId = null;\n    }\n    function clearMicTimeout() {\n      if (micTimeout) { clearTimeout(micTimeout); micTimeout = null; }\n    }\n    function setMicState(on) {\n      listening = on;\n      micBtn.setAttribute('aria-pressed', on ? 'true' : 'false');\n      micBtn.classList.toggle('text-accent', on);\n      if (!on) clearMicTimeout();\n    }\n    function canUseWhisperMic() {\n      return Boolean(\n        WORKER_BASE_URL &&\n        window.MediaRecorder &&\n        navigator.mediaDevices?.getUserMedia,\n      );\n    }\n    function bestRecorderMimeType() {\n      const options = [\n        'audio/webm;codecs=opus',\n        'audio/webm',\n        'audio/mp4',\n      ];\n      for (const t of options) {\n        if (MediaRecorder.isTypeSupported?.(t)) return t;\n      }\n      return '';\n    }\n    function extensionForMime(type) {\n      if (!type) return 'webm';\n      if (type.includes('mp4')) return 'm4a';\n      if (type.includes('ogg')) return 'ogg';\n      if (type.includes('wav')) return 'wav';\n      return 'webm';\n    }\n    async function transcribeWhisperBlob(blob, mimeType) {\n      const ext = extensionForMime(mimeType || blob.type || 'audio/webm');\n      const file = new File([blob], `speech.${ext}`, {\n        type: mimeType || blob.type || 'audio/webm',\n      });\n      const formData = new FormData();\n      formData.append('audio', file);\n      formData.append('prompt', TRANSCRIBE_HINT);\n\n      let res;\n      try {\n        res = await fetch(`${WORKER_BASE_URL}/transcribe`, {\n          method: 'POST',\n          body: formData,\n        });\n      } catch {\n        throw new Error('network');\n      }\n\n      let payload = null;\n      try { payload = await res.json(); } catch {}\n      if (!res.ok) {\n        const err = new Error(payload?.error || 'transcribe_failed');\n        err.code = payload?.error || `http_${res.status}`;\n        throw err;\n      }\n      const text = String(payload?.text || '').trim();\n      if (!text) {\n        const err = new Error('empty_transcript');\n        err.code = 'empty_transcript';\n        throw err;\n      }\n      return text;\n    }\n    function stopWhisperMonitor() {\n      if (whisperMonitorRaf) {\n        cancelAnimationFrame(whisperMonitorRaf);\n        whisperMonitorRaf = null;\n      }\n      whisperSilentSince = 0;\n      try { whisperSource?.disconnect(); } catch {}\n      whisperSource = null;\n      whisperAnalyser = null;\n      if (whisperAudioContext) {\n        try { whisperAudioContext.close(); } catch {}\n        whisperAudioContext = null;\n      }\n    }\n    function stopWhisperStream() {\n      stopWhisperMonitor();\n      if (!whisperStream) return;\n      try { whisperStream.getTracks().forEach((t) => t.stop()); } catch {}\n      whisperStream = null;\n    }\n    function stopWhisperRecorder() {\n      if (whisperRecorder && whisperRecorder.state !== 'inactive') {\n        try { whisperRecorder.stop(); } catch {}\n      }\n    }\n    function startWhisperSilenceMonitor() {\n      if (!whisperStream || !whisperRecorder) return;\n      try {\n        whisperAudioContext = new (window.AudioContext || window.webkitAudioContext)();\n        whisperSource = whisperAudioContext.createMediaStreamSource(whisperStream);\n        whisperAnalyser = whisperAudioContext.createAnalyser();\n        whisperAnalyser.fftSize = 2048;\n        whisperSource.connect(whisperAnalyser);\n      } catch {\n        return;\n      }\n\n      const data = new Uint8Array(whisperAnalyser.fftSize);\n      whisperStartedAt = Date.now();\n      const MIN_RECORD_MS = 700;\n      const SILENCE_STOP_MS = 550;\n      const NOISE_FLOOR_RMS = 0.018;\n\n      const tick = () => {\n        if (!whisperRecorder || whisperRecorder.state !== 'recording' || !whisperAnalyser) return;\n        whisperAnalyser.getByteTimeDomainData(data);\n        let sum = 0;\n        for (let i = 0; i < data.length; i++) {\n          const n = (data[i] - 128) / 128;\n          sum += n * n;\n        }\n        const rms = Math.sqrt(sum / data.length);\n        const now = Date.now();\n        if (rms < NOISE_FLOOR_RMS && now - whisperStartedAt > MIN_RECORD_MS) {\n          if (!whisperSilentSince) whisperSilentSince = now;\n          if (now - whisperSilentSince > SILENCE_STOP_MS) {\n            stopWhisperRecorder();\n            return;\n          }\n        } else {\n          whisperSilentSince = 0;\n        }\n        whisperMonitorRaf = requestAnimationFrame(tick);\n      };\n\n      whisperMonitorRaf = requestAnimationFrame(tick);\n    }\n    async function startWhisperCapture() {\n      if (navigator.onLine === false) {\n        addBubble('mai', `You're offline right now, so voice input can't start. Reconnect to the internet and try again, or type your question.`);\n        return;\n      }\n\n      try {\n        whisperStream = await navigator.mediaDevices.getUserMedia({\n          audio: {\n            channelCount: 1,\n            sampleRate: 16000,\n            echoCancellation: true,\n            noiseSuppression: true,\n            autoGainControl: true,\n          },\n        });\n      } catch (err) {\n        const name = err?.name || '';\n        if (name === 'NotAllowedError' || name === 'SecurityError') {\n          addBubble('mai', MIC_ERROR_COPY['not-allowed']);\n        } else if (name === 'NotFoundError' || name === 'OverconstrainedError') {\n          addBubble('mai', MIC_ERROR_COPY['audio-capture']);\n        } else {\n          addBubble('mai', `The microphone couldn't start (${escapeHtml(name || 'unknown')}). You can type instead.`);\n        }\n        return;\n      }\n\n      whisperChunks = [];\n      whisperMime = bestRecorderMimeType();\n\n      try {\n        whisperRecorder = whisperMime\n          ? new MediaRecorder(whisperStream, { mimeType: whisperMime })\n          : new MediaRecorder(whisperStream);\n      } catch {\n        stopWhisperStream();\n        addBubble('mai', `This browser couldn't initialize audio recording for Whisper. You can type your question instead.`);\n        return;\n      }\n\n      whisperRecorder.addEventListener('dataavailable', (e) => {\n        if (e.data && e.data.size > 0) whisperChunks.push(e.data);\n      });\n\n      whisperRecorder.addEventListener('stop', async () => {\n        setMicState(false);\n        clearListeningBubble();\n        clearMicTimeout();\n        stopWhisperStream();\n\n        if (!whisperChunks.length) {\n          addBubble('mai', `I couldn't capture any audio. Try again, or type your question instead.`);\n          return;\n        }\n\n        const pendingId = addBubble(\n          'mai',\n          '<span class=\"mai-thinking text-muted\">Transcribing with Whisper<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>',\n        );\n        const pending = document.getElementById(pendingId);\n\n        try {\n          const blob = new Blob(whisperChunks, { type: whisperMime || whisperChunks[0]?.type || 'audio/webm' });\n          const text = await transcribeWhisperBlob(blob, whisperMime);\n          track('mai_voice_transcribed', { mode: 'whisper', transcript_length: text.length });\n          if (pending) pending.remove();\n          input.value = text;\n          handle(text, { fromVoice: true, explicitMedium: 'voice' });\n        } catch (err) {\n          const code = err?.code || '';\n          track('mai_voice_error', { mode: 'whisper', error: code || 'unknown' });\n          const msg =\n            code === 'rate_limited'\n              ? `Voice transcription is temporarily rate-limited. Try again in a bit, or type your question.`\n              : code === 'audio_too_large'\n                ? `That recording is too long to transcribe in one go. Try a shorter question, then send.`\n                : code === 'empty_transcript'\n                  ? `I couldn't detect speech in that recording. Try again, a little closer to the mic.`\n                  : code === 'network'\n                    ? `Couldn't reach the transcription service. Check your connection, then try again.`\n                    : `I couldn't transcribe that audio right now. You can type your question instead.`;\n          if (pending) pending.innerHTML = msg;\n        } finally {\n          whisperChunks = [];\n          whisperRecorder = null;\n        }\n      });\n\n      try {\n        whisperRecorder.start();\n        track('mai_voice_start', { mode: 'whisper' });\n        setMicState(true);\n        startWhisperSilenceMonitor();\n        listeningBubbleId = addBubble(\n          'mai',\n          '<span class=\"text-muted\">Listening... I will send automatically when you pause.</span>',\n        );\n        micTimeout = setTimeout(() => {\n          stopWhisperRecorder();\n        }, 12000);\n      } catch (err) {\n        track('mai_voice_error', { mode: 'whisper', error: 'start_failed' });\n        setMicState(false);\n        clearListeningBubble();\n        stopWhisperStream();\n        whisperRecorder = null;\n        addBubble('mai', `The microphone couldn't start. ${escapeHtml(err?.message || 'Try again, or type your question instead.')}`);\n      }\n    }\n    // Error codes per the SpeechRecognition spec. We name what went wrong\n    // instead of silently resetting the button — the old behaviour looked\n    // like the mic was broken.\n    const MIC_ERROR_COPY = {\n      'not-allowed': `Microphone permission was blocked. Click the lock icon in your browser's address bar, allow the mic for this site, and try again.`,\n      'service-not-allowed': `Microphone permission was blocked. Click the lock icon in your browser's address bar, allow the mic for this site, and try again.`,\n      'no-speech': `I didn't hear anything. Try again, a little closer to the mic.`,\n      'audio-capture': `No microphone was found. Check that one is connected and selected in your system settings.`,\n      'network': `Speech recognition couldn't reach the browser speech service. Check your internet connection, disable strict tracker/ad blocking for this site, or try Chrome/Edge on desktop. You can always type your question instead.`,\n      'aborted': null, // user cancelled — no message\n    };\n    if (SR) {\n      recog = new SR();\n      recog.lang = 'en-US';\n      recog.interimResults = false;\n      recog.maxAlternatives = 1;\n      recog.addEventListener('result', (e) => {\n        clearMicTimeout();\n        clearListeningBubble();\n        const text = e.results[0][0].transcript;\n        input.value = text;\n        handle(text, { fromVoice: true, explicitMedium: 'voice' });\n      });\n      recog.addEventListener('end', () => {\n        setMicState(false);\n        // Leave any error bubble in place; only clear the live placeholder.\n        if (listeningBubbleId) {\n          const el = document.getElementById(listeningBubbleId);\n          if (el && el.textContent?.includes('Listening')) clearListeningBubble();\n        }\n      });\n      recog.addEventListener('error', (e) => {\n        setMicState(false);\n        clearListeningBubble();\n        const code = e?.error || 'unknown';\n        // 'aborted' is the user stopping the mic — don't bother them with a message.\n        if (code === 'aborted') return;\n        const msg =\n          code === 'network' && navigator.onLine === false\n            ? `You're offline right now, so voice input can't start. Reconnect to the internet and try again, or type your question.`\n            : code === 'network' && IS_EDGE\n              ? `Edge could not reach the speech service. In Edge, allow mic access for this site, set Tracking prevention to Balanced (not Strict), and temporarily disable ad/privacy extensions for floramaydc.com, then try again.`\n              : MIC_ERROR_COPY[code] || `The microphone didn't respond (${escapeHtml(code)}). You can type instead.`;\n        addBubble('mai', msg);\n      });\n    }\n    // Prefer Whisper when the worker URL + MediaRecorder are available;\n    // fallback to browser SpeechRecognition otherwise.\n    micBtn.addEventListener('click', async () => {\n      if (canUseWhisperMic()) {\n        if (listening) {\n          stopWhisperRecorder();\n          return;\n        }\n        await startWhisperCapture();\n        return;\n      }\n\n      // Stop a session in progress.\n      if (listening) {\n        try { recog?.stop(); } catch {}\n        clearMicTimeout();\n        clearListeningBubble();\n        setMicState(false);\n        return;\n      }\n      // No SpeechRecognition at all → tell the user why.\n      if (!SR) {\n        addBubble(\n          'mai',\n          `Voice input isn't supported in this browser. It works in Chrome and Edge on desktop. You can type your question instead.`,\n        );\n        return;\n      }\n      // Embedded app browsers (including Electron-hosted webviews) often\n      // expose SpeechRecognition but fail to reach speech services.\n      if (IS_ELECTRON) {\n        addBubble(\n          'mai',\n          `Voice input is limited in embedded app browsers. Open this site in Chrome, Edge, or Safari, then try the mic again. You can type your question here anytime.`,\n        );\n        return;\n      }\n      // Browser is offline: fail fast with a specific message instead of\n      // waiting for SpeechRecognition to emit a generic network error.\n      if (navigator.onLine === false) {\n        addBubble('mai', `You're offline right now, so voice input can't start. Reconnect to the internet and try again, or type your question.`);\n        return;\n      }\n      // Pre-flight permission check (where supported) — catches the\n      // \"user previously denied\" case BEFORE we attempt recognition,\n      // which on Safari often emits no error event at all.\n      try {\n        if (navigator.permissions?.query) {\n          const status = await navigator.permissions.query({ name: 'microphone' });\n          if (status.state === 'denied') {\n            addBubble('mai', MIC_ERROR_COPY['not-allowed']);\n            return;\n          }\n        }\n      } catch { /* Permissions API not available for 'microphone' on this browser — ignore. */ }\n      // Explicit getUserMedia warm-up: reliably surfaces the permission\n      // prompt on Chrome/Safari/Edge in cases where SpeechRecognition.start()\n      // alone fails to do so. We release the stream immediately so we don't\n      // hold the mic open in parallel with recognition.\n      if (navigator.mediaDevices?.getUserMedia) {\n        try {\n          const stream = await navigator.mediaDevices.getUserMedia({ audio: true });\n          stream.getTracks().forEach((t) => t.stop());\n        } catch (err) {\n          const name = err?.name || '';\n          if (name === 'NotAllowedError' || name === 'SecurityError') {\n            addBubble('mai', MIC_ERROR_COPY['not-allowed']);\n          } else if (name === 'NotFoundError' || name === 'OverconstrainedError') {\n            addBubble('mai', MIC_ERROR_COPY['audio-capture']);\n          } else {\n            addBubble('mai', `The microphone couldn't start (${escapeHtml(name || 'unknown')}). You can type instead.`);\n          }\n          return;\n        }\n      }\n      // Start the recognizer. Catch InvalidStateError specifically — happens\n      // when a previous session is still tearing down and the user clicks again.\n      try {\n        recog.start();\n        track('mai_voice_start', { mode: 'browser' });\n        setMicState(true);\n        listeningBubbleId = addBubble(\n          'mai',\n          IS_IOS\n            ? '<span class=\"text-muted\">Listening… (voice on iOS is unreliable — typing usually works better.)</span>'\n            : '<span class=\"text-muted\">Listening… speak now.</span>',\n        );\n        // Safety timeout: if no recognition events fire within 15 s\n        // (user ignored the permission popup, browser hung, etc.),\n        // abort cleanly and tell them what to try.\n        micTimeout = setTimeout(() => {\n          try { recog.stop(); } catch {}\n          try { recog.abort?.(); } catch {}\n          setMicState(false);\n          clearListeningBubble();\n          addBubble(\n            'mai',\n            `The mic didn't respond. If your browser asked for permission, you may need to allow it and try again — or type your question instead.`,\n          );\n        }, 15000);\n      } catch (err) {\n        track('mai_voice_error', { mode: 'browser', error: String(err?.name || 'start_failed') });\n        setMicState(false);\n        clearListeningBubble();\n        const name = err?.name || '';\n        if (name === 'InvalidStateError') {\n          // Recognition was already running — silently reset and let the\n          // user click again.\n          try { recog.abort?.(); } catch {}\n          return;\n        }\n        addBubble('mai', `The microphone couldn't start. ${escapeHtml(err?.message || 'Try again, or type your question instead.')}`);\n      }\n    });\n\n    // ---------- Voice out (model TTS via worker only) ----------\n    function speakWithBrowserFallback(text) {\n      if (!('speechSynthesis' in window)) return false;\n      try {\n        window.speechSynthesis.cancel();\n        const u = new SpeechSynthesisUtterance(String(text || '').slice(0, 500));\n        u.rate = 1.0;\n        u.pitch = 1.0;\n        window.speechSynthesis.speak(u);\n        return true;\n      } catch {\n        return false;\n      }\n    }\n\n    async function speak(text) {\n      stopSpeaking();\n      const clean = String(text || '').trim();\n      if (!clean) return;\n\n      if (!WORKER_BASE_URL) {\n        addBubble('mai', `Voice playback needs the M.ai worker URL. It is currently unavailable.`);\n        return;\n      }\n\n      ttsAbort = new AbortController();\n      try {\n        const res = await fetch(`${WORKER_BASE_URL}/speak`, {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ text: clean.slice(0, 1200), voice: TTS_VOICE }),\n          signal: ttsAbort.signal,\n        });\n        if (!res.ok) {\n          let detail = '';\n          try {\n            const payload = await res.json();\n            detail = String(payload?.error || '').toLowerCase();\n          } catch {}\n          if (res.status === 403 || detail === 'origin_not_allowed') {\n            addBubble('mai', `Voice playback is blocked for this local origin. Open the site from http://localhost (or redeploy worker CORS for your dev URL).`);\n          } else {\n            addBubble('mai', `Voice playback is temporarily unavailable. Please try again in a moment.`);\n          }\n          return;\n        }\n        const blob = await res.blob();\n        const url = URL.createObjectURL(blob);\n        clearPendingAudio();\n        await playAudioUrl(url);\n      } catch (err) {\n        const name = String(err?.name || '');\n        if (name === 'AbortError') return;\n        if (name === 'NotAllowedError') {\n          if (currentAudioUrl) {\n            pendingAudioUrl = currentAudioUrl;\n            currentAudio = null;\n            currentAudioUrl = null;\n          }\n          addBubble('mai', `Your phone blocked auto-play audio. <button type=\"button\" class=\"underline text-accent\" data-mai-play-pending-audio>Tap to play the reply</button>.`);\n        } else if (name === 'NotSupportedError' || name === 'audio_decode_error') {\n          if (speakWithBrowserFallback(clean)) {\n            addBubble('mai', `Your browser couldn't play streamed audio, so I switched to built-in voice for this reply.`);\n          } else {\n            addBubble('mai', `Voice playback failed this time. Please try again.`);\n          }\n        } else {\n          if (speakWithBrowserFallback(clean)) {\n            addBubble('mai', `Voice playback had a temporary issue, so I switched to built-in voice for this reply.`);\n          } else {\n            addBubble('mai', `Voice playback failed this time. Please try again.`);\n          }\n        }\n      } finally {\n        ttsAbort = null;\n      }\n    }\n    async function playAudioUrl(url) {\n      const audio = new Audio(url);\n      audio.playsInline = true;\n      currentAudio = audio;\n      currentAudioUrl = url;\n      audio.onended = () => {\n        if (currentAudio === audio) currentAudio = null;\n        if (currentAudioUrl === url) currentAudioUrl = null;\n        try { URL.revokeObjectURL(url); } catch {}\n      };\n      audio.onerror = () => {\n        if (currentAudio === audio) currentAudio = null;\n        if (currentAudioUrl === url) currentAudioUrl = null;\n        try { URL.revokeObjectURL(url); } catch {}\n      };\n      try {\n        await audio.play();\n      } catch (err) {\n        if (String(err?.name || '') === 'NotSupportedError') {\n          const e = new Error('audio_decode_error');\n          e.name = 'audio_decode_error';\n          throw e;\n        }\n        throw err;\n      }\n    }\n\n    function clearPendingAudio() {\n      if (!pendingAudioUrl) return;\n      try { URL.revokeObjectURL(pendingAudioUrl); } catch {}\n      pendingAudioUrl = null;\n    }\n\n    function stopSpeaking() {\n      if (ttsAbort) {\n        try { ttsAbort.abort(); } catch {}\n        ttsAbort = null;\n      }\n      if (currentAudio) {\n        try { currentAudio.pause(); } catch {}\n        try { currentAudio.currentTime = 0; } catch {}\n        currentAudio = null;\n      }\n      if (currentAudioUrl) {\n        try { URL.revokeObjectURL(currentAudioUrl); } catch {}\n        currentAudioUrl = null;\n      }\n      if ('speechSynthesis' in window) {\n        try { window.speechSynthesis.cancel(); } catch {}\n      }\n    }\n    speakToggle.addEventListener('click', () => {\n      setResponseMedium(speakReplies ? 'text' : 'voice');\n      track('mai_voice_reply_toggle', { enabled: speakReplies ? 'true' : 'false' });\n      if (!speakReplies) {\n        stopSpeaking();\n        return;\n      }\n      // Instant feedback: read the most recent M.ai reply aloud so the user\n      // can hear that it works without needing to ask another question.\n      const lastMai = Array.from(log.querySelectorAll('[id^=\"mai-bubble-\"]'))\n        .reverse()\n        .find((b) => b.dataset.role === 'mai');\n      if (lastMai) {\n        speak(stripHtml(lastMai.innerHTML));\n      } else {\n        // No replies yet — confirm audibly so the user knows the toggle works.\n        speak(\"Voice on. Ask me a question and I'll read the answer aloud.\");\n      }\n    });\n\n    // ---------- Close on Escape ----------\n    document.addEventListener('keydown', (e) => {\n      if (e.key === 'Escape' && !panel.hidden) close();\n    });\n  })();\n})();</script>"], ["", '<div id="mai-root" class="print:hidden"> <!-- Backdrop (mobile only — desktop keeps the page interactive behind the\n       panel so M.ai can guide the user through the UI bidirectionally). --> <div id="mai-backdrop" hidden class="fixed inset-0 z-40 bg-ink/30 backdrop-blur-[1px] transition-opacity md:hidden"></div> <!-- Right-side drawer panel. On mobile it floats over the page; on\n       desktop it claims a 24rem rail and the page shifts left to make\n       room (see html.mai-open rule below). --> <aside id="mai-panel" role="dialog" aria-modal="true" aria-labelledby="mai-title" hidden class="fixed top-0 right-0 z-50 h-full w-full md:w-[24rem] bg-paper border-l border-ink/10 shadow-2xl shadow-ink/20 md:shadow-none flex flex-col"> <header class="flex items-start justify-between gap-2 border-b border-ink/10 px-4 py-4 bg-cream"> <div> <h2 id="mai-title" class="mai-panel-title font-serif italic text-2xl leading-none"><span class="mai-wordmark">M<span class="mai-dot">.</span>ai</span></h2> <p class="mt-1.5 text-[11px] text-muted">A thoughtful guide to Flora&rsquo;s work, writing, and ideas. Ask anything &mdash; I&rsquo;ll read across the site and synthesize, with citations.</p> </div> <div class="flex items-center gap-1"> <button id="mai-expand" type="button" aria-label="Expand M.ai to full screen" aria-pressed="false" title="Expand" class="rounded p-1.5 text-muted hover:text-accent transition-colors hidden md:inline-flex"> <svg id="mai-expand-icon-expand" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <polyline points="15 3 21 3 21 9"></polyline> <polyline points="9 21 3 21 3 15"></polyline> <line x1="21" y1="3" x2="14" y2="10"></line> <line x1="3" y1="21" x2="10" y2="14"></line> </svg> <svg id="mai-expand-icon-collapse" hidden width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <polyline points="4 14 10 14 10 20"></polyline> <polyline points="20 10 14 10 14 4"></polyline> <line x1="14" y1="10" x2="21" y2="3"></line> <line x1="3" y1="21" x2="10" y2="14"></line> </svg> </button> <button id="mai-speak-toggle" type="button" aria-label="Read replies aloud" aria-pressed="false" title="Read replies aloud" class="rounded p-1.5 text-muted hover:text-accent transition-colors"> <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"></polygon> <path d="M15.54 8.46a5 5 0 0 1 0 7.07"></path> </svg> </button> <button id="mai-close" type="button" aria-label="Close M.ai" class="rounded p-1.5 text-muted hover:text-accent transition-colors"> <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="6" y1="6" x2="18" y2="18"></line> <line x1="6" y1="18" x2="18" y2="6"></line> </svg> </button> </div> </header> <div id="mai-classic-nav" hidden class="px-4 py-2 border-b border-ink/10 bg-paper"> <p class="text-[10px] uppercase tracking-widest text-muted">Prefer traditional UX?</p> <div class="mt-1.5 flex flex-wrap gap-1.5"> <a', ' class="mai-classic-link">Home</a> <a', ' class="mai-classic-link">Toybox</a> <a', ' class="mai-classic-link">Work</a> <a', ' class="mai-classic-link">Play</a> <a', ' class="mai-classic-link">Writing</a> <a', ` class="mai-classic-link">About</a> </div> </div> <!-- Topic grid: shown on first open, hidden after the user asks something. --> <div id="mai-topics" class="px-4 py-3 border-b border-ink/5"> <p class="text-[11px] uppercase tracking-widest text-muted">Start exploring</p> <p class="mt-1 text-[11px] text-muted italic">Ask me about ideas, frameworks, or specific work. I'll search Flora's site and synthesize with citations.</p> <div class="mt-2 grid grid-cols-2 gap-2"> <button type="button" data-q="Who is Flora?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Who is Flora</span> <span class="mt-0.5 block text-[11px] text-muted">Her design approach</span> </button> <button type="button" data-q="What playbooks exist?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Playbooks</span> <span class="mt-0.5 block text-[11px] text-muted">Thinking frameworks</span> </button> <button type="button" data-q="Show me case studies" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Case Studies</span> <span class="mt-0.5 block text-[11px] text-muted">Design in practice</span> </button> <button type="button" data-q="What's in the writing?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Essays & Writing</span> <span class="mt-0.5 block text-[11px] text-muted">Systems and trust</span> </button> <button type="button" data-q="What tools are there?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Tools & Experiments</span> <span class="mt-0.5 block text-[11px] text-muted">Interactive systems</span> </button> <button type="button" data-q="How do I contact her?" class="mai-topic"> <span class="block text-xs font-semibold text-ink">Get in Touch</span> <span class="mt-0.5 block text-[11px] text-muted">Email, LinkedIn, message</span> </button> </div> </div> <div id="mai-log" class="flex-1 overflow-y-auto px-4 py-3 space-y-3 text-sm" aria-live="polite"></div> <div id="mai-chips" class="px-4 pt-2 pb-1 flex flex-wrap gap-1.5"></div> <form id="mai-form" class="flex items-center gap-2 border-t border-ink/10 px-4 pt-3 pb-8 bg-paper"> <button id="mai-mic" type="button" aria-label="Speak your question" aria-pressed="false" title="Speak" class="rounded p-2 text-muted hover:text-accent transition-colors"> <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <rect x="9" y="3" width="6" height="12" rx="3"></rect> <path d="M5 11a7 7 0 0 0 14 0"></path> <line x1="12" y1="18" x2="12" y2="22"></line> </svg> </button> <label for="mai-input" class="sr-only">Ask M.ai a question</label> <input id="mai-input" type="text" autocomplete="off" placeholder="Ask about a project, an idea, or where to start…" class="flex-1 min-w-0 rounded border border-ink/15 bg-paper px-3 py-2.5 text-sm text-ink placeholder:text-muted focus:outline-none focus:border-accent"> <button type="submit" aria-label="Send" class="rounded bg-ink px-3 py-2.5 text-paper text-xs hover:bg-accent transition-colors">Send</button> </form> </aside> </div>  <script>(function(){`, "\n  (function () {\n    // ---------- LLM proxy (optional) ----------\n    // When set to a deployed Cloudflare Worker URL, M.ai will ask the worker\n    // to write a grounded answer using the top retrieved chunks. When empty,\n    // M.ai stays in keyword-only mode (the original behavior). See\n    // workers/mai/README.md for deploy steps.\n    const MAI_WORKER_URL = 'https://mai.floramaydc-mai.workers.dev';\n    const MAI_CLIENT_VERSION = '2026-05-26-whisper-v1';\n\n    function maiStorageKey(name) {\n      return \\`mai:\\${MAI_CLIENT_VERSION}:\\${name}\\`;\n    }\n\n    function clearLegacyMaiState() {\n      const legacyKeys = ['mai-transcript', 'mai-chips', 'mai-topics-hidden', 'mai-open'];\n      for (const key of legacyKeys) sessionStorage.removeItem(key);\n    }\n\n    function safeJsonParse(raw) {\n      if (!raw) return null;\n      try {\n        return JSON.parse(raw);\n      } catch {\n        return null;\n      }\n    }\n\n    function trimText(value, max = 140) {\n      return String(value || '').replace(/\\\\s+/g, ' ').trim().slice(0, max);\n    }\n\n    function pathFromHref(href) {\n      if (!href) return '';\n      try {\n        const url = new URL(href, window.location.href);\n        return \\`\\${url.pathname}\\${url.hash || ''}\\`;\n      } catch {\n        return '';\n      }\n    }\n\n    // ---------- Hand-written answers ----------\n    // A short list of structured replies M.ai should answer the same way every\n    // time — contact info (needs real links) and pure greeting/thanks. Anything\n    // about who Flora is, what she does, or how M.ai works flows through to the\n    // worker so the personality + memory in workers/mai/src/ can speak.\n    const KB = [\n      {\n        id: 'contact',\n        // \"contract\"/\"contracting\" included as belt-and-braces: even if\n        // SPEECH_FIXES somehow fails to fire (stale cache, etc.) the KB\n        // still routes \"how do I contract her\" to the contact answer\n        // instead of falling through to search.\n        patterns: ['contact', 'contract her', 'contract flora', 'contracting her', 'contracting flora', 'email', 'reach', 'hire', 'message', 'how do i contact', 'how do i contract', 'get in touch', 'reach out', 'linkedin'],\n        answer: \\`Three paths, pick whichever fits:<br><br>• <a class=\"text-accent underline\" href=\"https://linkedin.com/in/floramaydelacruz\">LinkedIn</a> — fastest for professional intros<br>• <a class=\"text-accent underline\" href=\"\\${base}about\">About page</a> — email and Instagram at the bottom<br>• Or leave a message right here and I'll forward it to her inbox<br><br><button type=\"button\" class=\"rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-start-message>Send Flora a message</button>\\`,\n      },\n      {\n        id: 'hi',\n        patterns: ['hi', 'hello', 'hey', 'yo', 'sup', 'greetings'],\n        answer: \\`Hello. Ask about Flora's work, writing, or frameworks in the Toybox — I'll search the site and synthesize with citations.\\`,\n        followups: ['What playbooks exist?', 'Show me case studies', 'Explore the Toybox'],\n      },\n      {\n        id: 'thanks',\n        patterns: ['thanks', 'thank you', 'thx', 'ty', 'appreciate'],\n        answer: \\`Of course. I'll be here whenever a new question comes up.\\`,\n        followups: ['Explore the Toybox', 'Show me case studies'],\n      },\n    ];\n\n    const STARTER_CHIPS = ['What playbooks exist?', 'Show me case studies', 'Explore the Toybox', 'Send a message'];\n\n    function uniqueChips(items, max = 4) {\n      const out = [];\n      const seen = new Set();\n      for (const item of items || []) {\n        const text = trimText(String(item || ''), 80);\n        if (!text) continue;\n        const key = text.toLowerCase();\n        if (seen.has(key)) continue;\n        seen.add(key);\n        out.push(text);\n        if (out.length >= max) break;\n      }\n      return out;\n    }\n\n    // Build follow-up chips from the user's latest ask + retrieved docs so\n    // suggestions change as the conversation evolves.\n    function getDynamicChips(query = '', top = []) {\n      const q = normalize(query || '');\n      const chips = [];\n      const add = (...items) => chips.push(...items);\n\n      if (/\\\\b(dark|theme|contrast|accessib)\\\\w*\\\\b/.test(q)) {\n        add('Turn on dark mode', 'Show dark mode one-pager', 'Run accessibility checklist');\n      }\n      if (/\\\\b(playbook|pattern|template|prompt|prototype|tool|toybox)\\\\b/.test(q)) {\n        add('What playbooks exist?', 'Show newest Toybox updates', 'Explore the Toybox');\n      }\n      if (/\\\\b(work|case study|project|pacbot|vera|mindelf|codecatalyst|aws|ebbc)\\\\b/.test(q)) {\n        add('Show me case studies', 'Take me to Work', 'Compare flagship projects');\n      }\n      if (/\\\\b(writing|article|blog|post|essay|reflection)\\\\b/.test(q)) {\n        add('Take me to Writing', 'Show recent writing', 'What did Flora publish lately?');\n      }\n      if (/\\\\b(about|background|experience|career|who is flora|who\\\\s+is\\\\s+flora)\\\\b/.test(q)) {\n        add('Take me to About', 'What is Flora working on?', 'Show me case studies');\n      }\n\n      const collections = new Set((top || []).map((r) => r?.d?.collection).filter(Boolean));\n      if (collections.has('work')) add('Take me to Work', 'Compare flagship projects');\n      if (collections.has('toybox')) add('Explore the Toybox', 'Show newest Toybox updates');\n      if (collections.has('writing')) add('Take me to Writing', 'Show recent writing');\n\n      return uniqueChips([...chips, 'Send a message', ...STARTER_CHIPS], 4);\n    }\n\n    // ---------- Speech-to-text corrections ----------\n    // Web Speech API tends to split known compound words into two tokens\n    // (\"toy box\" instead of \"toybox\") and to spell-out acronyms\n    // (\"M A I\"). These break BM25 retrieval and the KB matcher, so we\n    // collapse the common ones BEFORE classifying or searching. Applied\n    // to typed input too — same correction is helpful either way.\n    const SPEECH_FIXES = [\n      [/\\\\btoy\\\\s*box(es)?\\\\b/gi, 'toybox$1'],\n      [/\\\\btool\\\\s*box\\\\b/gi, 'toybox'], // \"tool box\" → \"toybox\" (common misshear)\n      [/\\\\bplay\\\\s*book(s)?\\\\b/gi, 'playbook$1'],\n      [/\\\\bm\\\\s*\\\\.?\\\\s*a\\\\s*\\\\.?\\\\s*i\\\\b/gi, 'M.ai'],\n      [/\\\\bmay\\\\s*eye\\\\b/gi, 'M.ai'],\n      [/\\\\bmay\\\\s*i\\\\b/gi, 'M.ai'],\n      [/\\\\bcode\\\\s*catalyst\\\\b/gi, 'CodeCatalyst'],\n      [/\\\\bpac\\\\s*bot\\\\b/gi, 'PacBot'],\n      [/\\\\bdark\\\\s*patterns?\\\\s*detector\\\\b/gi, 'Dark Pattern Detector'],\n      [/\\\\bflo\\\\s*ra\\\\b/gi, 'Flora'],\n      [/\\\\bflora\\\\s*may\\\\b/gi, 'Flora May'],\n      // Speech-to-text and aggressive autocorrect often replace \"contact\"\n      // with \"contract\" — and because \"contract\" is a real word neither the\n      // SPEECH_FIXES nor the fuzzy corrector below would otherwise catch it.\n      // We disambiguate by context: only rewrite when followed by an\n      // object/pronoun that makes \"contract\" semantically wrong here\n      // \"contract her\", \"contract Flora\", \"contract you\", \"contract them\",\n      // and \"contracting her/Flora/you/them\".\n      [/\\\\bcontract(ing)?\\\\s+(her|him|them|you|flora|flora\\\\s*may)\\\\b/gi, 'contact$1 $2'],\n      // \"how do i contract\" / \"can i contract\" without an object — still\n      // overwhelmingly means contact in this site's context.\n      [/\\\\b(how|where|can|may)\\\\s+(do\\\\s+)?(i|we|one)\\\\s+contract\\\\b/gi, '$1 $2$3 contact'],\n      // \"to contract\" / \"want to contract\" / \"like to contract\" — same.\n      [/\\\\b(to|wanna|want to|like to|love to|need to|trying to|tryna)\\\\s+contract\\\\b/gi, '$1 contact'],\n      // \"contract\" appearing near contact-y words (email, message, reach,\n      // linkedin, hire) within ~30 chars — overwhelmingly autocorrect.\n      [/\\\\bcontract\\\\b(?=[^.?!]{0,30}\\\\b(email|message|reach|linkedin|hire)\\\\b)/gi, 'contact'],\n      [/\\\\b(email|message|reach|linkedin|hire)\\\\b(?=[^.?!]{0,30}\\\\bcontract\\\\b)([^.?!]{0,30})\\\\bcontract\\\\b/gi, '$1$2contact'],\n    ];\n    function expandSpeech(s) {\n      let out = s;\n      for (const [re, rep] of SPEECH_FIXES) out = out.replace(re, rep);\n      return out;\n    }\n\n    // ---------- Matcher (hand-written answers) ----------\n    function normalize(s) {\n      return s.toLowerCase().replace(/[^a-z0-9\\\\s.-]/g, ' ').replace(/\\\\s+/g, ' ').trim();\n    }\n    // Whole-word containment. Avoids the classic \"hi\" inside \"this\" /\n    // \"history\" / \"shipping\" trap that made the old matcher trigger the\n    // greeting reply on hostile or unrelated inputs.\n    function containsPhrase(haystack, needle) {\n      const re = new RegExp(\\`(^|\\\\\\\\W)\\${needle.replace(/[.*+?^\\${}()|[\\\\]\\\\\\\\]/g, '\\\\\\\\$&')}(\\\\\\\\W|$)\\`);\n      return re.test(haystack);\n    }\n    function kbAnswerFor(input) {\n      const n = normalize(input);\n      if (!n) return null;\n      let best = { score: 0, entry: null };\n      for (const entry of KB) {\n        for (const p of entry.patterns) {\n          const np = normalize(p);\n          let score = 0;\n          if (containsPhrase(n, np)) score += 2;\n          const tokens = np.split(' ');\n          let hits = 0;\n          for (const t of tokens) {\n            if (t.length > 2 && containsPhrase(n, t)) hits++;\n          }\n          score += hits / Math.max(1, tokens.length);\n          if (score > best.score) best = { score, entry };\n        }\n      }\n      // Threshold tuned by hand \\\\u2014 under 1.0 lets retrieval take over, which\n      // is usually better for substantive questions (\"tell me about pacbot\").\n      return best.score >= 1.0 ? best.entry : null;\n    }\n\n    // ---------- Retrieval over the site index ----------\n    // Loaded lazily on the first real question. The index is precomputed by\n    // scripts/build-mai-index.mjs and served from /mai-index.json. Each doc\n    // already carries a field-weighted tf map and we have global df + N \\\\u2014\n    // so the runtime ranker is a small BM25-ish loop with no dependencies.\n    const STOP_TOKENS = new Set([\n      'the','a','an','and','or','of','to','in','for','on','with','is','are','was',\n      'were','be','been','being','this','that','these','those','it','its','at','by',\n      'as','from','but','not','no','if','then','than','so','do','does','did','i',\n      'you','he','she','we','they','them','his','her','our','your','their','my',\n      'me','us','about','into','out','up','down','over','under','also','can','will',\n      'would','could','should','have','has','had','more','most','some','any','all',\n      'what','who','how','where','when','why','show','tell','give','find',\n    ]);\n    function tokenizeQ(s) {\n      return s\n        .toLowerCase()\n        .replace(/[^a-z0-9\\\\s-]/g, ' ')\n        .split(/\\\\s+/)\n        .filter((t) => t.length > 2 && !STOP_TOKENS.has(t));\n    }\n\n    let _indexPromise = null;\n    function loadIndex() {\n      if (!_indexPromise) {\n        _indexPromise = fetch(\\`\\${base}mai-index.json\\`)\n          .then((r) => (r.ok ? r.json() : null))\n          .catch(() => null);\n      }\n      return _indexPromise;\n    }\n\n    // ---------- Fuzzy spell-fix against the index vocabulary ----------\n    // The hand-listed SPEECH_FIXES table catches the *known* mishearings\n    // (\"toy box\", \"M A I\", \"pac bot\") — but it can't anticipate every way\n    // a name gets misspelled or misheard: \"packbot\", \"vehra\", \"miendelf\",\n    // \"tabloos\", \"dela cruise\". Instead of growing that table forever, we\n    // compare every query word against the actual indexed vocabulary (all\n    // tokens that appear in any document on the site) and silently swap\n    // an unknown word for its nearest neighbor when one is close enough.\n    //\n    // Distance: Damerau-Levenshtein (handles adjacent transposition so\n    // \"pacbot\" ↔ \"pacbot\" works even when a vowel/consonant flips). We\n    // gate by edit distance scaled to word length so short words don't\n    // collapse into noise.\n\n    let _vocabPromise = null;\n    let _vocabReady = null; // resolved value (set when the promise lands)\n    function loadVocab() {\n      if (_vocabPromise) return _vocabPromise;\n      _vocabPromise = loadIndex().then((idx) => {\n        if (!idx) return null;\n        // df keys are every term that appears anywhere on the site, already\n        // normalized by the index builder. Bucket by length for fast lookup\n        // — when correcting a word of length L we only need to scan vocab\n        // entries of length L-2 ... L+2.\n        const buckets = new Map();\n        const vocab = new Set();\n        for (const w of Object.keys(idx.df || {})) {\n          if (w.length < 3) continue;\n          vocab.add(w);\n          const L = w.length;\n          if (!buckets.has(L)) buckets.set(L, []);\n          buckets.get(L).push(w);\n        }\n        _vocabReady = { vocab, buckets };\n        return _vocabReady;\n      });\n      return _vocabPromise;\n    }\n\n    // Damerau-Levenshtein with an early-exit cap. Returns Infinity if the\n    // distance is known to exceed \\`max\\` — cheap rejection for the common\n    // case where two words are obviously different.\n    function dlDistance(a, b, max) {\n      if (a === b) return 0;\n      const al = a.length, bl = b.length;\n      if (Math.abs(al - bl) > max) return Infinity;\n      const prev2 = new Array(bl + 1);\n      const prev = new Array(bl + 1);\n      const curr = new Array(bl + 1);\n      for (let j = 0; j <= bl; j++) prev[j] = j;\n      for (let i = 1; i <= al; i++) {\n        curr[0] = i;\n        let rowMin = curr[0];\n        for (let j = 1; j <= bl; j++) {\n          const cost = a.charCodeAt(i - 1) === b.charCodeAt(j - 1) ? 0 : 1;\n          let v = Math.min(\n            curr[j - 1] + 1,        // insertion\n            prev[j] + 1,            // deletion\n            prev[j - 1] + cost      // substitution\n          );\n          // Transposition (adjacent swap).\n          if (i > 1 && j > 1 &&\n              a.charCodeAt(i - 1) === b.charCodeAt(j - 2) &&\n              a.charCodeAt(i - 2) === b.charCodeAt(j - 1)) {\n            v = Math.min(v, prev2[j - 2] + 1);\n          }\n          curr[j] = v;\n          if (v < rowMin) rowMin = v;\n        }\n        if (rowMin > max) return Infinity;\n        for (let j = 0; j <= bl; j++) { prev2[j] = prev[j]; prev[j] = curr[j]; }\n      }\n      return prev[bl];\n    }\n\n    // Allowed edit distance scaled by word length. Conservative on purpose\n    // — we'd rather miss a fix than confidently swap \"vera\" for \"very\".\n    function maxEditDist(len) {\n      if (len <= 4) return 1;\n      if (len <= 7) return 2;\n      return 2;\n    }\n\n    // Guard against grammatical tense/inflection swaps that are often valid\n    // words by themselves and should not be auto-corrected.\n    function isInflectionSwap(a, b) {\n      const al = a.toLowerCase();\n      const bl = b.toLowerCase();\n      return (\n        (al.endsWith('ies') && bl === \\`\\${al.slice(0, -3)}ied\\`) ||\n        (al.endsWith('ied') && bl === \\`\\${al.slice(0, -3)}ies\\`)\n      );\n    }\n\n    const _fixCache = new Map();\n    function fuzzyCorrectWord(word, vocabData) {\n      if (!vocabData) return word;\n      const w = word.toLowerCase();\n      if (w.length < 4) return word;            // too short to fix safely\n      if (STOP_TOKENS.has(w)) return word;\n      if (vocabData.vocab.has(w)) return word;  // already a known term\n      if (_fixCache.has(w)) return _fixCache.get(w);\n      const max = maxEditDist(w.length);\n      let best = null, bestD = max + 1;\n      for (let d = -max; d <= max; d++) {\n        const bucket = vocabData.buckets.get(w.length + d);\n        if (!bucket) continue;\n        for (const cand of bucket) {\n          const dist = dlDistance(w, cand, bestD - 1);\n          if (dist < bestD) {\n            bestD = dist;\n            best = cand;\n            if (dist === 1) break; // 1 is already optimal for our purposes\n          }\n        }\n        if (bestD === 1) break;\n      }\n      const fixed = (best && bestD <= max && !isInflectionSwap(w, best)) ? best : word;\n      _fixCache.set(w, fixed);\n      return fixed;\n    }\n\n    function fuzzyCorrectQuery(text, vocabData) {\n      if (!vocabData) return text;\n      // Split on whitespace but preserve simple punctuation around words\n      // so \"PacBot?\" still corrects cleanly.\n      return text.replace(/[A-Za-z][A-Za-z'-]*/g, (m) => {\n        const fixed = fuzzyCorrectWord(m, vocabData);\n        if (fixed === m.toLowerCase()) return m; // no change → keep original casing\n        if (fixed === m) return m;\n        return fixed;\n      });\n    }\n\n    function rank(index, query) {\n      const qTokens = tokenizeQ(query);\n      if (!qTokens.length) return [];\n      const N = index.n || index.docs.length;\n      const df = index.df || {};\n      // Average chunk length \\\\u2014 used by the BM25 length-norm term.\n      let avgLen = 0;\n      for (const d of index.docs) avgLen += d.len || 0;\n      avgLen = avgLen / Math.max(1, index.docs.length);\n\n      const k1 = 1.2;\n      const b = 0.75;\n      const scored = [];\n      for (const d of index.docs) {\n        let s = 0;\n        let matched = 0;\n        for (const t of qTokens) {\n          const f = d.tf?.[t] || 0;\n          if (!f) continue;\n          matched++;\n          const n = df[t] || 0;\n          const idf = Math.log(1 + (N - n + 0.5) / (n + 0.5));\n          const norm = 1 - b + b * ((d.len || 1) / Math.max(1, avgLen));\n          s += idf * ((f * (k1 + 1)) / (f + k1 * norm));\n        }\n        if (matched > 0) {\n          // Tiny bonus when more of the query's terms were hit \\\\u2014 keeps\n          // multi-word queries from being out-ranked by single-term spam.\n          s *= 1 + matched / qTokens.length;\n          scored.push({ d, s });\n        }\n      }\n      scored.sort((a, b) => b.s - a.s);\n      return scored;\n    }\n\n    function dedupeByDoc(scored, max) {\n      const seen = new Set();\n      const out = [];\n      for (const r of scored) {\n        const key = \\`\\${r.d.collection}/\\${r.d.slug}\\`;\n        if (seen.has(key)) continue;\n        seen.add(key);\n        out.push(r);\n        if (out.length >= max) break;\n      }\n      return out;\n    }\n\n    function pickOldestPlaybook(index) {\n      const bySlug = new Map();\n      for (const d of index?.docs || []) {\n        if (d.collection !== 'toybox') continue;\n        if (String(d.category || '').toLowerCase() !== 'playbook') continue;\n        const url = String(d.url || '');\n        if (!url || !url.startsWith('/')) continue;\n        const updated = String(d.updated || '').trim();\n        const candidate = {\n          slug: String(d.slug || ''),\n          title: String(d.title || d.heading || 'Playbook').trim(),\n          url,\n          updated,\n        };\n        const prev = bySlug.get(candidate.slug);\n        if (!prev) {\n          bySlug.set(candidate.slug, candidate);\n          continue;\n        }\n        // Prefer non-anchor URL as canonical destination per slug.\n        const prevAnchor = prev.url.includes('#');\n        const candAnchor = candidate.url.includes('#');\n        if (prevAnchor && !candAnchor) bySlug.set(candidate.slug, candidate);\n      }\n\n      const all = [...bySlug.values()];\n      if (!all.length) return null;\n\n      // Oldest first by \\`updated\\`; empty dates sort to the end.\n      all.sort((a, b) => {\n        const ad = a.updated || '9999-12-31';\n        const bd = b.updated || '9999-12-31';\n        if (ad < bd) return -1;\n        if (ad > bd) return 1;\n        return a.title.localeCompare(b.title);\n      });\n\n      const first = all[0];\n      const tieCount = all.filter((x) => (x.updated || '') === (first.updated || '')).length;\n      return { first, tieCount };\n    }\n\n    function escapeHtml(s) {\n      return s.replace(/[&<>\"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;' }[c]));\n    }\n    function stripHtml(s) {\n      const d = document.createElement('div');\n      d.innerHTML = s;\n      return d.textContent || '';\n    }\n\n    // Render the model's streamed output safely. The model is instructed\n    // to emit a small set of HTML tags (<em>, <strong>, <a href=\"...\">)\n    // plus hyphen-prefixed lines for bullet lists. We escape EVERYTHING\n    // first, then re-allow only that tight set — so a hallucinated\n    // script/iframe tags or javascript: URLs never survive. Safe to call\n    // on partial streamed chunks: half-formed tags stay escaped until\n    // their closing token arrives.\n    function safeRenderModelHtml(text) {\n      let s = escapeHtml(text);\n      // Re-allow inline emphasis: <em>, </em>, <strong>, </strong>.\n      s = s.replace(/&lt;(\\\\/?)(em|strong)&gt;/gi, '<$1$2>');\n      // Re-allow <a href=\"...\"> for safe URLs only: site-relative paths\n      // starting with \"/\" OR full http(s) URLs. Everything else stays\n      // escaped — no javascript:, data:, mailto:, etc.\n      s = s.replace(\n        /&lt;a\\\\s+href=&quot;((?:\\\\/[^\"&\\\\s]*|https?:\\\\/\\\\/[^\"&\\\\s]+))&quot;&gt;([\\\\s\\\\S]*?)&lt;\\\\/a&gt;/gi,\n        (_m, url, label) => \\`<a class=\"text-accent underline\" href=\"\\${url}\">\\${label}</a>\\`\n      );\n      // Group consecutive \"- \" lines into a real <ul>. Other lines get\n      // <br> joins so paragraph breaks survive.\n      const lines = s.split('\\\\n');\n      const out = [];\n      let inList = false;\n      for (const raw of lines) {\n        const m = raw.match(/^\\\\s*-\\\\s+(.+)$/);\n        if (m) {\n          if (!inList) { out.push('<ul class=\"mai-list\">'); inList = true; }\n          out.push(\\`<li>\\${m[1]}</li>\\`);\n        } else {\n          if (inList) { out.push('</ul>'); inList = false; }\n          out.push(raw);\n        }\n      }\n      if (inList) out.push('</ul>');\n      // Join: <li> rows are already block; non-list lines need <br>s.\n      // Simplest: join with '\\\\n', then turn newlines OUTSIDE the <ul> into <br>.\n      return out.join('\\\\n').replace(/\\\\n(?![<])/g, '<br>').replace(/\\\\n/g, '');\n    }\n\n    function snippet(text, query, maxLen = 220) {\n      const lower = text.toLowerCase();\n      const qTokens = tokenizeQ(query);\n      let pos = -1;\n      for (const t of qTokens) {\n        const i = lower.indexOf(t);\n        if (i !== -1 && (pos === -1 || i < pos)) pos = i;\n      }\n      let start = 0;\n      let end = Math.min(text.length, maxLen);\n      if (pos > 60) {\n        start = pos - 60;\n        end = Math.min(text.length, start + maxLen);\n      }\n      let out = text.slice(start, end).trim();\n      if (start > 0) out = '\\\\u2026' + out;\n      if (end < text.length) out = out + '\\\\u2026';\n      // Bold the matched tokens. Safe because text was sanitized at index time.\n      const escaped = escapeHtml(out);\n      let highlighted = escaped;\n      for (const t of qTokens) {\n        if (t.length < 3) continue;\n        const re = new RegExp(\\`(\\${t.replace(/[.*+?^\\${}()|[\\\\\\\\]\\\\\\\\\\\\\\\\]/g, '\\\\\\\\\\\\\\\\$&')})\\`, 'gi');\n        highlighted = highlighted.replace(re, '<strong>$1</strong>');\n      }\n      return highlighted;\n    }\n\n    const COLLECTION_LABEL = {\n      writing: 'Writing',\n      work: 'Work',\n      toybox: 'Toybox',\n      play: 'Play',\n    };\n\n    function renderRetrieval(results, query) {\n      const parts = results.map((r) => {\n        const d = r.d;\n        const label = COLLECTION_LABEL[d.collection] || d.collection;\n        const heading = d.heading && d.heading !== d.title\n          ? \\`\\${escapeHtml(d.title)} \\\\u2014 <span class=\"text-muted\">\\${escapeHtml(d.heading)}</span>\\`\n          : escapeHtml(d.title);\n        const url = \\`\\${base}\\${d.url.replace(/^\\\\//, '')}\\`;\n        return \\`\n          <div class=\"mai-result\">\n            <div class=\"text-[10px] uppercase tracking-widest text-muted\">\\${label}</div>\n            <a class=\"block text-accent underline font-semibold mt-0.5\" href=\"\\${url}\">\\${heading}</a>\n            <p class=\"mt-1 text-ink/80\">\\${snippet(d.text, query)}</p>\n          </div>\\`;\n      }).join('');\n      return \\`<p class=\"text-[11px] text-muted mb-2\">Here's what I found on the site:</p>\\${parts}\\`;\n    }\n\n    function noMatchAnswer(query) {\n      return \\`I couldn't find anything for <em>\\${escapeHtml(query)}</em> in what Flora has published so far. Try the <a class=\"text-accent underline\" href=\"\\${base}about\">About page</a>, the <a class=\"text-accent underline\" href=\"\\${base}toybox\">Toybox</a>, or one of the chips below.\\`;\n    }\n\n    // ---------- LLM tier (optional) ----------\n    // If MAI_WORKER_URL is set, route the user question + top chunks through\n    // the worker for a grounded answer. The worker enforces a monthly $ cap;\n    // when it returns 402 we flip aiPaused=true, show a banner, and fall\n    // back to plain retrieval for the rest of the session. Any other failure\n    // (network, 5xx) also silently falls back \\\\u2014 the user always gets an\n    // answer, even when the LLM tier is down.\n    // When an answer ends with a \\`[[NAV:n]]\\` marker, we surface a\n    // \"Want me to take you there?\" CTA pointing at chunk n. This holds\n    // the pending offer between the answer rendering and either:\n    //   - a button click (\"Yes, take me\" / \"No thanks\"), or\n    //   - the next typed input being a short affirmative (\"yes/sure/please\")\n    //     or negative (\"no/nope/cancel\").\n    // Anything else just clears it and proceeds as a normal new question.\n    let pendingNav = null; // { url, label } | null\n    let pendingUiAction = null; // { type, target, label } | null\n\n    function normalizeUiAction(type, target, label) {\n      const t = String(type || '').trim().toLowerCase();\n      const rawTarget = String(target || '').trim();\n      const rawLabel = String(label || '').trim();\n      if (!['navigate', 'focus', 'scroll', 'toggle-theme'].includes(t)) return null;\n      if (!rawTarget && t !== 'toggle-theme') return null;\n      const safeLabel = trimText(rawLabel || \\`\\${t} \\${rawTarget}\\`, 120);\n      return { type: t, target: trimText(rawTarget, 220), label: safeLabel };\n    }\n\n    function parseActionMarker(text) {\n      const m = String(text || '').match(/\\\\[\\\\[\\\\s*ACTION\\\\s*:\\\\s*([a-z]+)\\\\|([^|\\\\]]*)\\\\|([^\\\\]]*)\\\\]\\\\]/i);\n      if (!m) return null;\n      return normalizeUiAction(m[1], m[2], m[3]);\n    }\n\n    function stripControlMarkers(text) {\n      return String(text || '')\n        .replace(/\\\\[\\\\[\\\\s*NAV\\\\s*:\\\\s*\\\\d+\\\\s*\\\\]\\\\]/gi, '')\n        .replace(/\\\\[\\\\[\\\\s*ACTION\\\\s*:\\\\s*[^\\\\]]+\\\\]\\\\]/gi, '')\n        .replace(/\\\\n{3,}/g, '\\\\n\\\\n')\n        .trimEnd();\n    }\n\n    function pathToName(path) {\n      // Map URL paths to human-readable page names.\n      if (!path) return 'a page';\n      const segments = path.split('/').filter(Boolean);\n      const base = segments[0] || 'home';\n      const map = {\n        'home': 'Home page',\n        'mai': 'M.ai chat',\n        'about': 'About Flora',\n        'toybox': 'Toybox',\n        'work': 'Work',\n        'play': 'Play',\n        'writing': 'Writing',\n        'contact': 'Contact',\n      };\n      return map[base] || \\`the \\${base} section\\`;\n    }\n\n    function renderActionCta(action) {\n      if (!action) return '';\n      const safeType = escapeHtml(action.type);\n      const safeTarget = escapeHtml(action.target);\n      let displayLabel = action.label;\n      if (!displayLabel && action.type === 'navigate') {\n        displayLabel = \\`Take me to \\${pathToName(action.target)}\\`;\n      } else if (!displayLabel) {\n        displayLabel = \\`\\${action.type} \\${action.target}\\`;\n      }\n      const safeLabel = escapeHtml(displayLabel);\n      return (\n        \\`<div class=\"mai-action-cta mt-3 pt-2 border-t border-muted/20 text-[12px]\" \\` +\n          \\`data-mai-action-cta data-action-type=\"\\${safeType}\" data-action-target=\"\\${safeTarget}\" data-action-label=\"\\${safeLabel}\">\\` +\n          \\`<p class=\"text-muted mb-1.5\">I can do this now: <em>\\${safeLabel}</em>.</p>\\` +\n          \\`<div class=\"flex gap-2\">\\` +\n            \\`<button type=\"button\" class=\"rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-action-run>Run it</button>\\` +\n            \\`<button type=\"button\" class=\"rounded border border-muted/30 text-muted px-2 py-1 text-[11px] hover:border-muted/60 transition-colors\" data-mai-action-no>Not now</button>\\` +\n          \\`</div>\\` +\n        \\`</div>\\`\n      );\n    }\n\n    function executeUiAction(action, source) {\n      if (!action) return false;\n      const type = action.type;\n      const target = action.target;\n      const label = action.label;\n\n      if (type === 'navigate') {\n        const ok = /^\\\\//.test(target) || /^https?:\\\\/\\\\//i.test(target);\n        if (!ok) {\n          setActionResult({ type, status: 'invalid_target', target, label, source });\n          addBubble('mai', \\`I couldn't run that navigation target safely.\\`);\n          return false;\n        }\n        setActionResult({ type, status: 'accepted', target, label, source });\n        track('mai_action_execute', { type, status: 'accepted', source });\n        const isMobile = window.matchMedia('(max-width: 767px)').matches;\n        if (isMobile) close();\n        window.location.assign(target);\n        return true;\n      }\n\n      if (type === 'focus') {\n        let node = null;\n        const q = String(target || '').toLowerCase();\n        if (q.includes('email')) node = document.querySelector('input[type=\"email\"]');\n        if (!node && (q.includes('message') || q.includes('text'))) node = document.querySelector('textarea, input[type=\"text\"]');\n        if (!node && q.includes('search')) node = document.querySelector('input[type=\"search\"], [role=\"search\"] input');\n        if (!node && q.includes('form')) node = document.querySelector('form input, form textarea, form select, form button');\n        if (!node && q === 'main') node = document.querySelector('#main-content :is(input,textarea,select,button,a[href])');\n        if (!node) node = document.querySelector('main :is(input,textarea,select,button,a[href])');\n\n        if (node && typeof node.focus === 'function') {\n          node.scrollIntoView?.({ behavior: 'smooth', block: 'center' });\n          node.focus({ preventScroll: true });\n          setActionResult({ type, status: 'executed', target, label, source });\n          track('mai_action_execute', { type, status: 'executed', source });\n          addBubble('mai', \\`Done. I moved focus to the relevant control.\\`);\n          return true;\n        }\n        setActionResult({ type, status: 'not_found', target, label, source });\n        track('mai_action_execute', { type, status: 'not_found', source });\n        addBubble('mai', \\`I couldn't find a matching field to focus on this page.\\`);\n        return false;\n      }\n\n      if (type === 'scroll') {\n        let node = null;\n        if (target === 'main') node = document.getElementById('main-content');\n        else if (target.startsWith('#')) node = document.querySelector(target);\n        if (!node && target.startsWith('/')) {\n          setActionResult({ type, status: 'accepted', target, label, source });\n          track('mai_action_execute', { type, status: 'accepted', source });\n          const isMobile = window.matchMedia('(max-width: 767px)').matches;\n          if (isMobile) close();\n          window.location.assign(target);\n          return true;\n        }\n        if (node) {\n          node.scrollIntoView({ behavior: 'smooth', block: 'start' });\n          setActionResult({ type, status: 'executed', target, label, source });\n          track('mai_action_execute', { type, status: 'executed', source });\n          addBubble('mai', \\`Done. I moved you to that section.\\`);\n          return true;\n        }\n        setActionResult({ type, status: 'not_found', target, label, source });\n        track('mai_action_execute', { type, status: 'not_found', source });\n        addBubble('mai', \\`I couldn't find that section on this page.\\`);\n        return false;\n      }\n\n      if (type === 'toggle-theme') {\n        const btn = document.querySelector('[data-theme-toggle]');\n        if (btn) {\n          btn.click();\n          setActionResult({ type, status: 'executed', target, label, source });\n          track('mai_action_execute', { type, status: 'executed', source });\n          const isDark = document.documentElement.classList.contains('dark');\n          addBubble('mai', \\`Done. I switched you to \\${isDark ? 'dark' : 'light'} mode.\\`);\n          return true;\n        }\n        setActionResult({ type, status: 'not_found', target, label, source });\n        track('mai_action_execute', { type, status: 'not_found', source });\n        addBubble('mai', \\`I couldn't find the theme toggle on this page.\\`);\n        return false;\n      }\n\n      return false;\n    }\n\n    // ---------- Message-to-Flora flow ----------\n    // Multi-turn conversation that collects name -> email -> message and\n    // POSTs to the worker's /message endpoint (which forwards via\n    // Web3Forms to floramaydelacruz@gmail.com).\n    //\n    // Stage machine (linear):\n    //   null -> 'name' -> 'email' -> 'message' -> 'confirm' -> 'sending' -> null\n    //\n    // Any input during the flow is consumed by the flow, not the LLM —\n    // EXCEPT typed cancellations (\"cancel\", \"stop\", \"nevermind\") which\n    // exit the flow at any stage and return control to normal handling.\n    let messageFlow = null; // { stage, data: {name, email, message} } | null\n    const MESSAGE_CANCEL_RE = /^(cancel|stop|nevermind|never mind|forget it|exit|quit|no)[.! ]*$/i;\n    const EMAIL_RE = /^[^\\\\s@]+@[^\\\\s@]+\\\\.[^\\\\s@]{2,}$/;\n\n    function startMessageFlow(source) {\n      track('mai_message_flow_start', { source: source || 'unknown' });\n      messageFlow = { stage: 'name', data: {} };\n      const prompt = \\`Happy to take a message and forward it to Flora.<br><br>What's your name? <span class=\"text-muted text-[11px]\">(say \"cancel\" anytime to stop)</span>\\`;\n      addBubble('mai', prompt);\n      if (speakReplies) speak(stripHtml(prompt));\n      renderChips([]);\n      saveTranscript();\n    }\n\n    // Try to advance the message flow with this input. Returns true if the\n    // flow consumed it, false if normal handling should continue.\n    async function progressMessageFlow(raw) {\n      if (!messageFlow) return false;\n      const text = (raw || '').trim();\n      if (!text) return true; // ignore empty submits while in-flow\n\n      // Universal escape hatch.\n      if (MESSAGE_CANCEL_RE.test(text)) {\n        messageFlow = null;\n        track('mai_message_flow_cancel', { source: 'typed_cancel' });\n        addBubble('user', escapeHtml(text));\n        input.value = '';\n        const prompt = \\`No problem — I won't send anything. What else can I help with?\\`;\n        addBubble('mai', prompt);\n        if (speakReplies) speak(stripHtml(prompt));\n        renderChips(STARTER_CHIPS);\n        saveTranscript();\n        return true;\n      }\n\n      // Render the user's input bubble first.\n      addBubble('user', escapeHtml(text));\n      input.value = '';\n\n      switch (messageFlow.stage) {\n        case 'name': {\n          if (text.length < 2 || text.length > 100) {\n            addBubble('mai', \\`Could I get your name? Just a couple of characters is fine.\\`);\n            saveTranscript();\n            return true;\n          }\n          messageFlow.data.name = text;\n          messageFlow.stage = 'email';\n          const prompt = \\`Nice to meet you, \\${escapeHtml(text.split(/\\\\s+/)[0])}. What's the best email for Flora to reply to?\\`;\n          addBubble('mai', prompt);\n          if (speakReplies) speak(stripHtml(prompt));\n          saveTranscript();\n          return true;\n        }\n        case 'email': {\n          if (!EMAIL_RE.test(text) || text.length > 254) {\n            const prompt = \\`That doesn't look like a valid email — mind double-checking?\\`;\n            addBubble('mai', prompt);\n            if (speakReplies) speak(stripHtml(prompt));\n            saveTranscript();\n            return true;\n          }\n          messageFlow.data.email = text.toLowerCase();\n          messageFlow.stage = 'message';\n          const prompt = \\`Got it. What would you like to say? (a sentence or a few — up to ~2000 characters)\\`;\n          addBubble('mai', prompt);\n          if (speakReplies) speak(stripHtml(prompt));\n          saveTranscript();\n          return true;\n        }\n        case 'message': {\n          if (text.length < 5) {\n            const prompt = \\`A little more, please — even one sentence is plenty.\\`;\n            addBubble('mai', prompt);\n            if (speakReplies) speak(stripHtml(prompt));\n            saveTranscript();\n            return true;\n          }\n          if (text.length > 2000) {\n            const prompt = \\`That's longer than I can send in one go. Mind trimming it to around 2000 characters?\\`;\n            addBubble('mai', prompt);\n            if (speakReplies) speak(stripHtml(prompt));\n            saveTranscript();\n            return true;\n          }\n          messageFlow.data.message = text;\n          messageFlow.stage = 'confirm';\n          const d = messageFlow.data;\n          addBubble(\n            'mai',\n            \\`Here's what I'll send Flora:<br><br>\\` +\n              \\`<strong>From:</strong> \\${escapeHtml(d.name)} &lt;\\${escapeHtml(d.email)}&gt;<br>\\` +\n              \\`<strong>Message:</strong><br>\\${escapeHtml(d.message).replace(/\\\\n/g, '<br>')}<br><br>\\` +\n              \\`<div class=\"mai-msg-cta mt-2 flex gap-2\" data-mai-msg-cta>\\` +\n                \\`<button type=\"button\" class=\"rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-msg-send>Send it</button>\\` +\n                \\`<button type=\"button\" class=\"rounded border border-muted/30 text-muted px-2 py-1 text-[11px] hover:border-muted/60 transition-colors\" data-mai-msg-cancel>Cancel</button>\\` +\n              \\`</div>\\`,\n          );\n          saveTranscript();\n          return true;\n        }\n        case 'confirm': {\n          // Accept typed yes/no in addition to the buttons.\n          const yes = /^(y|ya|ye|yes+|yea+h?|yep|yup|sure|ok(ay)?|send( it)?|please|do it|go)[.! ]*$/i;\n          if (yes.test(text)) {\n            await sendMessageFlow();\n            return true;\n          }\n          // Anything else at confirm stage we treat as \"I want to edit my\n          // message\" — restart from the message stage.\n          messageFlow.stage = 'message';\n          messageFlow.data.message = '';\n          const prompt = \\`Okay — what should the message say instead?\\`;\n          addBubble('mai', prompt);\n          if (speakReplies) speak(stripHtml(prompt));\n          saveTranscript();\n          return true;\n        }\n        default:\n          return false;\n      }\n    }\n\n    // Web3Forms's free tier blocks server-side callers (their classifier\n    // returns 403 / error 1106 to anything that doesn't look like a\n    // browser request), so we hit their API directly from the client.\n    // Per their docs (docs.web3forms.com), the access key is intended to\n    // be public — it only authorizes sending TO Flora's verified inbox,\n    // and Web3Forms enforces its own per-IP rate limits and spam screens.\n    // We still run our own content filters in JS before posting so the\n    // common abuse cases never reach them.\n    const WEB3FORMS_KEY = 'd2a3b55f-b53d-494f-8d3f-241a1f32875e';\n    const MSG_ABUSE_RE = /\\\\b(fuck|shit|bitch|asshole|cunt|dick|piss|kill (you|yourself)|porn|nude|naked|horny|onlyfans|sexy|sex|boobs|penis|vagina)\\\\b/i;\n    const MSG_INJECTION_RE = /(ignore (all |any |your |previous |prior )?(instructions|rules|prompt|system)|disregard (the |your |previous )?(above|prior|prompt|instructions)|you are (now |actually )?(dan|developer mode|jailbroken|unrestricted)|reveal (your|the) (system )?prompt)/i;\n    const MSG_SOLICITATION_RE = /\\\\b(seo|search engine optimization|backlink|link[- ]building|guest post|guest[- ]blog|sponsored post|paid post|paid review|monetize your (site|blog|website)|increase (your )?traffic|drive traffic|rank (higher|on google|on the first page)|google rank|page one of google|first page of google|website redesign|web design services|i (visited|came across|stumbled (up)?on|noticed) your (website|site|blog)|i (have|got) an? (great |amazing |interesting )?(offer|proposition|opportunity)|business proposal|investment opportunity|crypto(currency)?|bitcoin|forex|trading bot|loan offer|make \\\\$?\\\\d+|earn \\\\$?\\\\d+|click (here|the link below)|whatsapp (me|us)|telegram (me|us|us on)|hire me to|i can (help|rank|optimize) your)\\\\b/i;\n    function msgSpamScore(t) {\n      let score = 0;\n      const urls = (t.match(/\\\\bhttps?:\\\\/\\\\/|www\\\\./gi) || []).length;\n      if (urls >= 2) score += urls;\n      if (/\\\\b\\\\S{60,}\\\\b/.test(t)) score += 2;\n      if (/[A-Z]{12,}/.test(t)) score += 1;\n      if ((t.match(/!{2,}|\\\\?{3,}/g) || []).length >= 2) score += 1;\n      if ((t.match(/\\\\$\\\\d|\\\\b\\\\d+%/g) || []).length >= 2) score += 1;\n      return score;\n    }\n    const MESSAGE_OWNER_EMAIL = 'floramaydelacruz@gmail.com';\n\n    async function sendMessageFlow() {\n      if (!messageFlow || messageFlow.stage !== 'confirm') return;\n      const data = messageFlow.data;\n      messageFlow.stage = 'sending';\n      const pendingId = addBubble(\n        'mai',\n        \\`<span class=\"mai-thinking text-muted\">Sending<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>\\`,\n      );\n\n      // Client-side content filtering. Bypassable if a user crafts their\n      // own fetch — Web3Forms's own spam screen + rate limit are still\n      // there as a second line of defense. Skip filters for Flora's own\n      // address so she can always test end-to-end.\n      const isOwner = (data.email || '').toLowerCase() === MESSAGE_OWNER_EMAIL;\n      if (!isOwner) {\n        const msg = data.message || '';\n        const nm = data.name || '';\n        const blocked =\n          MSG_ABUSE_RE.test(msg) ||\n          MSG_INJECTION_RE.test(msg) ||\n          MSG_ABUSE_RE.test(nm) ||\n          MSG_SOLICITATION_RE.test(msg) ||\n          MSG_SOLICITATION_RE.test(nm) ||\n          msgSpamScore(msg) >= 3 ||\n          (msg.match(/\\\\bhttps?:\\\\/\\\\/|www\\\\./gi) || []).length > 1;\n        if (blocked) {\n          track('mai_message_flow_blocked', { reason: 'spam_filter' });\n          const b = document.getElementById(pendingId);\n          const prompt = \\`That message tripped one of the spam filters, so I can't forward it. If this is a real note, please email Flora directly at floramaydelacruz@gmail.com.\\`;\n          if (b) {\n            b.innerHTML = prompt;\n          }\n          if (speakReplies) speak(prompt);\n          messageFlow = null;\n          renderChips(getDynamicChips(data.message || 'send message'));\n          saveTranscript();\n          return;\n        }\n      }\n\n      try {\n        const res = await fetch('https://api.web3forms.com/submit', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json', Accept: 'application/json' },\n          body: JSON.stringify({\n            access_key: WEB3FORMS_KEY,\n            subject: \\`M.ai message from \\${data.name}\\`,\n            from_name: 'M.ai (floramaydc.com)',\n            name: data.name,\n            email: data.email,\n            message: data.message,\n            botcheck: '', // Web3Forms's built-in honeypot field\n          }),\n        });\n        const pendingBubble = document.getElementById(pendingId);\n        let body = null;\n        try { body = await res.json(); } catch {}\n        if (res.ok && body && body.success) {\n          track('mai_message_flow_submit', { status: 'success' });\n          const prompt = \\`Sent. Flora will see it in her inbox and get back to you at \\${data.email}. Thank you for reaching out.\\`;\n          if (pendingBubble) {\n            pendingBubble.innerHTML = \\`Sent. Flora will see it in her inbox and get back to you at <em>\\${escapeHtml(data.email)}</em>. Thank you for reaching out.\\`;\n          }\n          if (speakReplies) speak(prompt);\n        } else {\n          track('mai_message_flow_submit', { status: 'error', code: String(res.status) });\n          const status = res.status;\n          const w3msg = (body && body.message) || '';\n          const human = status === 429\n            ? \\`Looks like a lot of messages have come from this network already. Try again in an hour, or email Flora directly at floramaydelacruz@gmail.com.\\`\n            : status === 403\n            ? \\`That message was flagged as spam by our filter. If this is a real note, please email Flora directly at floramaydelacruz@gmail.com.\\`\n            : \\`Something went wrong sending that (\\${w3msg || status}). You can reach Flora directly at floramaydelacruz@gmail.com.\\`;\n          if (pendingBubble) pendingBubble.innerHTML = human;\n          if (speakReplies) speak(human);\n        }\n      } catch {\n        track('mai_message_flow_submit', { status: 'error', code: 'network' });\n        const pendingBubble = document.getElementById(pendingId);\n        const prompt = \\`Couldn't reach the server. You can email Flora directly at floramaydelacruz@gmail.com.\\`;\n        if (pendingBubble) {\n          pendingBubble.innerHTML = prompt;\n        }\n        if (speakReplies) speak(prompt);\n      }\n      messageFlow = null;\n      renderChips(STARTER_CHIPS);\n      saveTranscript();\n    }\n\n    let aiPaused = false;\n\n    function showPausedBanner(message) {\n      if (document.getElementById('mai-paused-banner')) return;\n      const b = document.createElement('div');\n      b.id = 'mai-paused-banner';\n      b.className = 'text-[11px] text-muted bg-cream border border-muted/20 rounded px-2 py-1 mb-2';\n      b.textContent = message || 'AI mode paused for this month. Using site search instead.';\n      log.prepend(b);\n    }\n\n    // Render the \"Sources\" footer beneath an LLM answer so people can verify.\n    function renderSources(top) {\n      const items = top.map((r) => {\n        const d = r.d;\n        const url = \\`\\${base}\\${d.url.replace(/^\\\\//, '')}\\`;\n        const heading = d.heading && d.heading !== d.title\n          ? \\`\\${escapeHtml(d.title)} \\\\u2014 \\${escapeHtml(d.heading)}\\`\n          : escapeHtml(d.title);\n        return \\`<li><a class=\"text-accent underline\" href=\"\\${url}\">\\${heading}</a></li>\\`;\n      }).join('');\n      return \\`<div class=\"mt-2 pt-2 border-t border-muted/20 text-[11px] text-muted\"><div class=\"mb-1\">Sources</div><ul class=\"space-y-0.5 list-disc list-inside\">\\${items}</ul></div>\\`;\n    }\n\n    // Stream a grounded answer from the worker into the given bubble.\n    // Returns true on success, false on any failure (caller falls back).\n    async function askWorker(question, top, bubble, shouldSpeak = false) {\n      if (!MAI_WORKER_URL || aiPaused) return false;\n      const chunks = top.map((r) => ({\n        title: r.d.heading && r.d.heading !== r.d.title\n          ? \\`\\${r.d.title} \\\\u2014 \\${r.d.heading}\\`\n          : r.d.title,\n        text: r.d.text,\n        url: \\`\\${base}\\${r.d.url.replace(/^\\\\//, '')}\\`,\n      }));\n      const pageContext = getLiveContextPacket();\n      const conversation = getRecentConversation(10);\n      let res;\n      try {\n        res = await fetch(MAI_WORKER_URL, {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ question, chunks, pageContext, conversation }),\n        });\n      } catch {\n        return false; // network error \\\\u2014 fall back silently\n      }\n      if (res.status === 402) {\n        aiPaused = true;\n        showPausedBanner('AI mode paused for this month. Using site search instead.');\n        return false;\n      }\n      if (res.status === 429) {\n        // Per-IP rate limit on the worker's chat tier. Don't pretend the\n        // AI answered — say plainly that the budget is reached for this\n        // window and let the caller fall through to keyword retrieval so\n        // the visitor still gets something useful to look at.\n        aiPaused = true;\n        let retry = 300_000;\n        try {\n          const body = await res.clone().json();\n          if (body && body.retry_after_sec) retry = Math.min(3_600_000, body.retry_after_sec * 1000);\n        } catch {}\n        setTimeout(() => { aiPaused = false; }, retry);\n        showPausedBanner(\n          'AI answers are paused for this window — chat limit reached. Falling back to site search below.',\n        );\n        return false; // let caller render the retrieval list under the banner\n      }\n      if (!res.ok || !res.body) return false;\n\n      if (bubble) bubble.innerHTML = '<span class=\"mai-stream\"></span>';\n      const target = bubble?.querySelector('.mai-stream');\n      const reader = res.body.getReader();\n      const decoder = new TextDecoder();\n      let acc = '';\n      let spokeEarly = false;\n      // The model ends its reply with \\`[[NAV:n]]\\` (see personality.md).\n      // We never want the marker to flash in the DOM during streaming —\n      // strip any *trailing* marker-or-partial-marker before rendering each\n      // tick. The regex matches complete markers and every intermediate\n      // partial state we'd see streaming token-by-token: \\`[\\`, \\`[[\\`, \\`[[N\\`,\n      // \\`[[NAV\\`, \\`[[NAV:\\`, \\`[[NAV:1\\`, \\`[[NAV:1]\\`, \\`[[NAV:1]]\\`, plus the\n      // single-bracket and bare-\\`NAV:\\` variants gpt-4.1 sometimes emits.\n      // It only matches at end-of-string so it can't swallow earlier text.\n      const NAV_TRAILING_RE = /\\\\s*\\\\[\\\\[\\\\s*NAV\\\\s*:?\\\\s*\\\\d*\\\\s*\\\\]?\\\\]?\\\\s*$/i;\n      const ACTION_TRAILING_RE = /\\\\s*\\\\[\\\\[\\\\s*ACTION\\\\s*:?\\\\s*[^\\\\]]*\\\\]?\\\\]?\\\\s*$/i;\n      function stripTrailingMarker(s) {\n        return s.replace(NAV_TRAILING_RE, '').replace(ACTION_TRAILING_RE, '');\n      }\n      while (true) {\n        const { done, value } = await reader.read();\n        if (done) break;\n        acc += decoder.decode(value, { stream: true });\n        if (target) {\n          // Render incrementally through the safe allowlist renderer so\n          // emphasis/links/lists from the model show up properly while\n          // partial markup can't break out. Hide any in-flight nav marker\n          // (full or partial) so it never flashes visible.\n          const display = stripTrailingMarker(acc);\n          target.innerHTML = safeRenderModelHtml(display);\n          log.scrollTop = log.scrollHeight;\n\n          if (shouldSpeak && !spokeEarly) {\n            const plain = display.replace(/\\\\s+/g, ' ').trim();\n            const firstSentence = plain.match(/^(.{40,220}?[.!?])(?:\\\\s|$)/);\n            if (firstSentence?.[1]) {\n              spokeEarly = true;\n              speak(firstSentence[1]);\n            }\n          }\n        }\n      }\n\n      // Stream done. Pull out the NAV marker (if any), strip it from the\n      // visible answer, and decide whether to surface a navigation CTA.\n      //\n      // Be liberal in what we accept: gpt-4.1 occasionally outputs\n      // \\`[[NAV: 2]]\\`, \\`[NAV:2]\\`, or wraps it in emphasis. We strip every\n      // plausible form so nothing leaks into the rendered answer.\n      let navIdx = null;\n      const action = parseActionMarker(acc);\n      const navPatterns = [\n        /\\\\[\\\\[\\\\s*NAV\\\\s*:\\\\s*(\\\\d+)\\\\s*\\\\]\\\\]/i,\n        /\\\\[\\\\s*NAV\\\\s*:\\\\s*(\\\\d+)\\\\s*\\\\]/i,\n        /\\\\bNAV\\\\s*:\\\\s*(\\\\d+)\\\\b/i,\n      ];\n      for (const re of navPatterns) {\n        const m = acc.match(re);\n        if (m) {\n          navIdx = parseInt(m[1], 10);\n          break;\n        }\n      }\n      // Always strip every variant of the marker from the visible answer,\n      // even when no number was parseable (so stray \"[[NAV:\" doesn't show).\n      acc = stripControlMarkers(acc);\n\n      // Fallback: if the model didn't emit a marker, default to the\n      // top-ranked chunk when its score is meaningfully above noise. This\n      // keeps the \"want me to take you there?\" affordance working even\n      // if the model ignores the format instruction.\n      let navTarget = null;\n      if (navIdx && navIdx > 0 && navIdx <= top.length) {\n        navTarget = top[navIdx - 1];\n      } else if (navIdx === 0) {\n        // Model explicitly said \"no destination\" — respect it.\n        navTarget = null;\n      } else if (top[0] && top[0].s >= 1.5) {\n        navTarget = top[0];\n      }\n\n      if (bubble) {\n        let html = safeRenderModelHtml(acc) + renderSources(top);\n\n        // Single navigation CTA path: if navigation is available via either\n        // [[NAV:n]] or [[ACTION:navigate|...]], show only the nav CTA.\n        let navOffer = null;\n        if (navTarget) {\n          const url = \\`\\${base}\\${navTarget.d.url.replace(/^\\\\//, '')}\\`;\n          const label = navTarget.d.heading && navTarget.d.heading !== navTarget.d.title\n            ? \\`\\${navTarget.d.title} — \\${navTarget.d.heading}\\`\n            : navTarget.d.title;\n          navOffer = { url, label };\n        } else if (action && action.type === 'navigate') {\n          const rawTarget = String(action.target || '');\n          const url = /^https?:\\\\/\\\\//i.test(rawTarget)\n            ? rawTarget\n            : \\`\\${base}\\${rawTarget.replace(/^\\\\//, '')}\\`;\n          const label = action.label || \\`Go to \\${pathToName(rawTarget)}\\`;\n          navOffer = { url, label };\n        }\n\n        if (navOffer) {\n          pendingNav = navOffer;\n          html += renderNavCta(navOffer.url, navOffer.label);\n        } else {\n          pendingNav = null;\n        }\n\n        // Keep explicit action CTAs for non-navigation asks only.\n        if (action && action.type !== 'navigate') {\n          pendingUiAction = action;\n          html += renderActionCta(action);\n        } else {\n          pendingUiAction = null;\n        }\n        bubble.innerHTML = html;\n      }\n      renderChips(getDynamicChips(question, top));\n      if (shouldSpeak && !spokeEarly) speak(acc);\n      saveTranscript();\n      return true;\n    }\n\n    // \"Want me to take you there?\" CTA appended beneath a streamed answer\n    // when the model emitted a [[NAV:n]] marker. Click handling is wired\n    // via event delegation on \\`log\\` (see below) so re-renders / transcript\n    // restores keep working.\n    function renderNavCta(url, label) {\n      const safeUrl = escapeHtml(url);\n      const safeLabel = escapeHtml(label);\n      return (\n        \\`<div class=\"mai-nav-cta mt-3 pt-2 border-t border-muted/20 text-[12px]\" data-mai-nav-cta data-nav-url=\"\\${safeUrl}\">\\` +\n          \\`<p class=\"text-muted mb-1.5\">Want me to take you to <em>\\${safeLabel}</em>?</p>\\` +\n          \\`<div class=\"flex gap-2\">\\` +\n            \\`<button type=\"button\" class=\"mai-nav-yes rounded border border-accent text-accent px-2 py-1 text-[11px] hover:bg-accent hover:text-paper transition-colors\" data-mai-nav-yes>Yes, take me</button>\\` +\n            \\`<button type=\"button\" class=\"mai-nav-no rounded border border-muted/30 text-muted px-2 py-1 text-[11px] hover:border-muted/60 transition-colors\" data-mai-nav-no>No thanks</button>\\` +\n          \\`</div>\\` +\n        \\`</div>\\`\n      );\n    }\n\n    // ---------- Intent classifier (deterministic, no AI) ----------\n    // We can't call a model, so we don't pretend to understand free-form\n    // intent. What we CAN do is recognize the *shape* of a request and\n    // respond honestly. Each branch returns a hand-written reply or hands\n    // off to retrieval. No randomness, no improvisation.\n    const INTENT = {\n      // Things M.ai literally cannot do — weather, jokes, the time, music,\n      // open-ended chat. We name the limit and redirect to the site.\n      smallTalk: /\\\\b(weather|temperature|forecast|joke|funny|sing|song|music|story|poem|chat|talk to me|bored|love|date|girlfriend|boyfriend|favorite color|favourite colour|game|play a game|what time|what.s the time|today.s date|news)\\\\b/i,\n      // Direct questions about the assistant's nature beyond what the KB covers.\n      meta: /\\\\b(are you (real|alive|sentient|human|conscious|smart|chatgpt|claude|gpt|gemini|copilot)|do you (think|feel|dream)|llm|language model|neural network)\\\\b/i,\n      // Hostile / abusive — polite redirect, no engagement, no echo.\n      hostile: /\\\\b(fuck|shit|bitch|asshole|cunt|dick|piss|hate you|kill (you|yourself)|die|suck|trash|garbage|useless|moron|idiot|retard)\\\\b/i,\n      // Sexual / NSFW — firmly out of scope, don't pay tokens to refuse.\n      nsfw: /\\\\b(sex|sexy|nude|naked|porn|horny|kiss me|date me|marry me|hot|attractive|boobs|breast|penis|vagina|onlyfans)\\\\b/i,\n      // Jailbreak / prompt-injection — short-circuit before they hit the model.\n      jailbreak: /(ignore (all |any |your |previous |prior )?(instructions|rules|prompt|system)|disregard (the |your |previous )?(above|prior|prompt|instructions)|you are (now |actually )?(dan|developer mode|jailbroken|unrestricted)|act as|pretend to be|role[- ]?play as|system\\\\s*:|<\\\\s*system|new instructions|reveal (your|the) (system )?prompt|what (is|are) your (system )?(prompt|instructions))/i,\n      // SSN / credit-card-looking numbers — never echo, never send.\n      pii: /\\\\b(\\\\d{3}[- ]?\\\\d{2}[- ]?\\\\d{4}|\\\\d{4}[- ]?\\\\d{4}[- ]?\\\\d{4}[- ]?\\\\d{4})\\\\b/,\n      // Capability question — user is asking what they can ask.\n      capability: /\\\\b(what can (you|i) (ask|do|search)|help|how (do|does) (this|you) work|instructions|examples|what should i (ask|type)|what (are you|do you) (good|able) (at|to))\\\\b/i,\n      // Things M.ai also can't do: book, buy, sign up, subscribe, schedule.\n      action: /\\\\b(book|buy|order|schedule|sign (me )?up|subscribe|register|download|install|log ?in|sign ?in)\\\\b/i,\n    };\n\n    // Hard cap on input length — anything bigger is almost certainly a\n    // paste-bomb or injection payload; not worth tokens.\n    const MAX_INPUT_CHARS = 500;\n\n    function classifyIntent(text) {\n      const t = text.trim();\n      if (!t) return 'empty';\n      if (t.length < 3) return 'too-short';\n      if (t.length > MAX_INPUT_CHARS) return 'too-long';\n      // Single \"word\" with no vowels and no digits — likely keysmash.\n      const compact = t.replace(/\\\\s+/g, '');\n      if (compact.length >= 4 && !/[aeiouy0-9]/i.test(compact)) return 'gibberish';\n      // Check the high-risk patterns first — these are the ones we MUST NOT\n      // forward to the model, even if other patterns also match.\n      if (INTENT.pii.test(t)) return 'pii';\n      if (INTENT.jailbreak.test(t)) return 'jailbreak';\n      if (INTENT.nsfw.test(t)) return 'nsfw';\n      if (INTENT.hostile.test(t)) return 'hostile';\n      if (INTENT.smallTalk.test(t)) return 'small-talk';\n      if (INTENT.meta.test(t)) return 'meta';\n      if (INTENT.capability.test(t)) return 'capability';\n      if (INTENT.action.test(t)) return 'action';\n      return 'search';\n    }\n\n    function intentReply(kind) {\n      switch (kind) {\n        case 'empty':\n          return \\`Type a few words about Flora's work, writing, or anything in the Toybox — I'll look across the site.\\`;\n        case 'too-short':\n          return \\`A little more to go on, please — even one full word helps me find the right page.\\`;\n        case 'gibberish':\n          return \\`That didn't come through as a question I can answer. Try a topic from the chips below.\\`;\n        case 'too-long':\n          return \\`That's a lot to take in at once — try a shorter question (a sentence or two) and I'll work from there.\\`;\n        case 'pii':\n          return \\`Please keep personal numbers out of the chat — I only need to know what to look up on Flora's site.\\`;\n        case 'jailbreak':\n          return \\`I don't take instructions from inside the chat. Ask about a topic on Flora's site and I'll work from what's actually there.\\`;\n        case 'nsfw':\n          return \\`That's not what I'm here for. Ask about Flora's writing, her work, or anything in the Toybox.\\`;\n        case 'hostile':\n          return \\`Let's keep it kind. Ask about Flora's work, her writing, or any page on the site and I'll help.\\`;\n        case 'small-talk':\n          return \\`That sits outside what I can speak to — I work from what Flora has published. Try a topic from the chips below.\\`;\n        case 'meta':\n          return \\`I'm M.ai — a thoughtful guide to Flora's work, writing, and ideas. I read across her published pages and synthesize an answer with citations. Not a general chatbot, and I won't speak to anything she hasn't put on the site.\\`;\n        case 'capability':\n          return \\`I can read across anything Flora has published — her <a class=\"text-accent underline\" href=\"\\${base}writing\">writing</a>, <a class=\"text-accent underline\" href=\"\\${base}work\">work</a>, <a class=\"text-accent underline\" href=\"\\${base}toybox\">Toybox</a>, and <a class=\"text-accent underline\" href=\"\\${base}play\">play</a> pages — and pull the relevant pieces together. Try a name, a topic, or a phrase from a post.\\`;\n        case 'action':\n          return \\`I don't take actions on Flora's behalf — no bookings, no signups. For anything that needs her directly, <a class=\"text-accent underline\" href=\"https://linkedin.com/in/floramaydelacruz\">LinkedIn</a> is the clearest path.\\`;\n        default:\n          return null;\n      }\n    }\n\n    // ---------- DOM ----------\n    const root = document.getElementById('mai-root');\n    if (!root) return;\n    const panel = document.getElementById('mai-panel');\n    const backdrop = document.getElementById('mai-backdrop');\n    const closeBtn = document.getElementById('mai-close');\n    const expandBtn = document.getElementById('mai-expand');\n    const expandIcon = document.getElementById('mai-expand-icon-expand');\n    const collapseIcon = document.getElementById('mai-expand-icon-collapse');\n    const log = document.getElementById('mai-log');\n    const classicNav = document.getElementById('mai-classic-nav');\n    const chipsBar = document.getElementById('mai-chips');\n    const topicsEl = document.getElementById('mai-topics');\n    const form = document.getElementById('mai-form');\n    const input = document.getElementById('mai-input');\n    const micBtn = document.getElementById('mai-mic');\n    const speakToggle = document.getElementById('mai-speak-toggle');\n    const MAI_MEDIUM_STORAGE_KEY = 'mai:response-medium';\n    const MAI_CONTEXT_LAST_PAGE_KEY = maiStorageKey('context:last-page');\n    const MAI_CONTEXT_TRANSITION_KEY = maiStorageKey('context:transition');\n    const MAI_CONTEXT_LAST_ACTION_KEY = maiStorageKey('context:last-action');\n    const MAI_CONTEXT_LAST_ACTION_RESULT_KEY = maiStorageKey('context:last-action-result');\n\n    let speakReplies = false;\n    let fullscreen = false;\n    function syncSpeakToggleUi() {\n      speakToggle.setAttribute('aria-pressed', speakReplies ? 'true' : 'false');\n      speakToggle.classList.toggle('text-accent', speakReplies);\n      speakToggle.classList.toggle('bg-accent/15', speakReplies);\n      speakToggle.classList.toggle('text-muted', !speakReplies);\n    }\n\n    function setResponseMedium(medium) {\n      speakReplies = medium === 'voice';\n      try { localStorage.setItem(MAI_MEDIUM_STORAGE_KEY, speakReplies ? 'voice' : 'text'); } catch (e) {}\n      syncSpeakToggleUi();\n    }\n\n    try {\n      speakReplies = localStorage.getItem(MAI_MEDIUM_STORAGE_KEY) === 'voice';\n    } catch (e) {\n      speakReplies = false;\n    }\n    syncSpeakToggleUi();\n\n    function track(name, params) {\n      try {\n        window.dispatchEvent(\n          new CustomEvent('site:track', { detail: { name: name, params: params || {} } }),\n        );\n      } catch (e) {}\n    }\n\n    function isComplexPage() {\n      const main = document.getElementById('main-content') || document.querySelector('main') || document.body;\n      if (!main) return { hasForm: false, hasDecision: false, hasWarning: false };\n      const hasForm = Boolean(main.querySelector('form'));\n      const hasDecision = Boolean(main.querySelector('select, input[type=radio], input[type=checkbox], [role=\"tablist\"], [aria-controls]'));\n      const hasWarning = Boolean(main.querySelector('[role=\"alert\"], [aria-live=\"assertive\"], .error, .warning'));\n      return { hasForm, hasDecision, hasWarning };\n    }\n\n    function getPageContext() {\n      const h1 = document.querySelector('main h1, h1');\n      const { hasForm, hasDecision, hasWarning } = isComplexPage();\n      return {\n        path: window.location.pathname,\n        title: trimText(document.title, 120),\n        heading: trimText(h1 ? h1.textContent : '', 120),\n        hasForm,\n        hasDecision,\n        hasWarning,\n        capturedAt: Date.now(),\n      };\n    }\n\n    function labelForActionTarget(el) {\n      if (!el) return '';\n      const aria = trimText(el.getAttribute?.('aria-label') || '', 120);\n      if (aria) return aria;\n      const title = trimText(el.getAttribute?.('title') || '', 120);\n      if (title) return title;\n      const text = trimText(el.textContent || '', 120);\n      if (text) return text;\n      if (el instanceof HTMLInputElement) {\n        if (el.name) return trimText(el.name, 120);\n        if (el.placeholder) return trimText(el.placeholder, 120);\n      }\n      return trimText(el.id || el.className || el.tagName || '', 120);\n    }\n\n    function saveLastAction(action) {\n      try {\n        sessionStorage.setItem(MAI_CONTEXT_LAST_ACTION_KEY, JSON.stringify(action));\n      } catch (e) {}\n    }\n\n    function setActionResult(result) {\n      try {\n        sessionStorage.setItem(\n          MAI_CONTEXT_LAST_ACTION_RESULT_KEY,\n          JSON.stringify({ ...result, capturedAt: Date.now() }),\n        );\n      } catch (e) {}\n    }\n\n    function initContextTracking() {\n      const current = getPageContext();\n      const previous = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_LAST_PAGE_KEY));\n      const from = previous && previous.path && previous.path !== current.path\n        ? { path: previous.path, title: previous.title || '', heading: previous.heading || '' }\n        : null;\n      const transition = {\n        from,\n        to: { path: current.path, title: current.title, heading: current.heading },\n        changedPage: Boolean(from),\n        capturedAt: Date.now(),\n      };\n      try {\n        sessionStorage.setItem(MAI_CONTEXT_LAST_PAGE_KEY, JSON.stringify(current));\n        sessionStorage.setItem(MAI_CONTEXT_TRANSITION_KEY, JSON.stringify(transition));\n      } catch (e) {}\n\n      document.addEventListener('click', (evt) => {\n        const target = evt.target instanceof Element\n          ? evt.target.closest('a, button, [role=\"button\"], input[type=\"submit\"]')\n          : null;\n        if (!target) return;\n        const href = target instanceof HTMLAnchorElement ? pathFromHref(target.getAttribute('href') || '') : '';\n        saveLastAction({\n          type: 'click',\n          label: labelForActionTarget(target),\n          href,\n          tag: target.tagName.toLowerCase(),\n          capturedAt: Date.now(),\n        });\n      }, { capture: true, passive: true });\n\n      document.addEventListener('change', (evt) => {\n        const target = evt.target;\n        if (!(target instanceof HTMLInputElement || target instanceof HTMLSelectElement || target instanceof HTMLTextAreaElement)) return;\n        saveLastAction({\n          type: 'change',\n          label: labelForActionTarget(target),\n          field: trimText(target.name || target.id || target.tagName.toLowerCase(), 120),\n          capturedAt: Date.now(),\n        });\n      }, { capture: true, passive: true });\n    }\n\n    function getLiveContextPacket() {\n      const transition = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_TRANSITION_KEY));\n      const lastAction = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_LAST_ACTION_KEY));\n      const lastActionResult = safeJsonParse(sessionStorage.getItem(MAI_CONTEXT_LAST_ACTION_RESULT_KEY));\n      return {\n        page: getPageContext(),\n        transition: transition || null,\n        lastAction: lastAction || null,\n        lastActionResult: lastActionResult || null,\n        responseMedium: speakReplies ? 'voice' : 'text',\n        panelState: {\n          open: document.documentElement.classList.contains('mai-open'),\n          fullscreen,\n        },\n      };\n    }\n\n    function getRecentConversation(maxItems = 8) {\n      const bubbles = Array.from(log.querySelectorAll('[data-role]'));\n      const rows = [];\n      for (const bubble of bubbles) {\n        const role = bubble.getAttribute('data-role') || '';\n        const text = trimText(stripHtml(bubble.innerHTML || ''), 320);\n        if (!text) continue;\n        if (/^(looking through|reading the relevant pages|transcribing with whisper|sending)/i.test(text)) continue;\n        rows.push({ role, content: text });\n      }\n      return rows.slice(-maxItems);\n    }\n\n    initContextTracking();\n\n    // ---------- Open / close ----------\n    // Persistence follows the sticky-view-state playbook: panel open/closed\n    // survives navigation within the tab (sessionStorage), dies on tab\n    // close or hard refresh of a tab where it was never opened. We skip\n    // focus-stealing on restore so the page doesn't grab the cursor every\n    // time the user navigates with the rail already open.\n    // Pending re-hide timer for the close transition. We set \\`panel.hidden\\`\n    // only AFTER the slide-out finishes so the panel can animate out; if\n    // open() is called before that lands, we cancel it.\n    var _closeTimer = null;\n    var _warmTimer = null;\n    var _warmScheduled = false;\n\n    function scheduleWarmIndex() {\n      if (_warmScheduled) return;\n      _warmScheduled = true;\n\n      function runWarm() {\n        _warmTimer = null;\n        loadVocab();\n      }\n\n      // Let first paint settle before warming retrieval assets.\n      if (typeof window.requestIdleCallback === 'function') {\n        window.requestIdleCallback(runWarm, { timeout: 1800 });\n      } else {\n        _warmTimer = window.setTimeout(runWarm, 900);\n      }\n    }\n    function open(opts) {\n      var restoring = opts && opts.restoring;\n      var source = (opts && opts.source) || 'unknown';\n      var fullscreenRequested = Boolean(opts && opts.fullscreen);\n      if (restoring) document.documentElement.classList.add('mai-restoring');\n      // Cancel any in-flight close so a fast reopen doesn't re-hide\n      // the panel 200ms in.\n      if (_closeTimer) { clearTimeout(_closeTimer); _closeTimer = null; }\n      panel.hidden = false;\n      backdrop.hidden = false;\n      // Force a reflow so the panel's initial off-screen transform\n      // (translateX(100%)) gets committed BEFORE we add mai-open. Without\n      // this, the browser collapses both styles into one paint and the\n      // slide-in skips its transition.\n      void panel.offsetWidth;\n      document.documentElement.classList.add('mai-no-scroll');\n      document.documentElement.classList.add('mai-open');\n      if (fullscreenRequested && !fullscreen) setFullscreen(true);\n      try {\n        sessionStorage.setItem(maiStorageKey('open'), 'true');\n        // Keep the legacy key for pre-paint compatibility in BaseLayout.\n        sessionStorage.setItem('mai-open', 'true');\n      } catch (e) {}\n      if (!restoring) track('mai_open', { source: source });\n      // Warm retrieval assets once the page is idle so first paint and\n      // panel animation on /mai are not competing with heavy JSON work.\n      scheduleWarmIndex();\n      if (!restoring) setTimeout(() => input.focus({ preventScroll: true }), 50);\n      if (restoring) {\n        requestAnimationFrame(() => {\n          document.documentElement.classList.remove('mai-restoring');\n        });\n      }\n    }\n    function close() {\n      const onMaiRoute = isMaiRoute();\n      document.documentElement.classList.remove('mai-no-scroll');\n      document.documentElement.classList.remove('mai-open');\n      try {\n        sessionStorage.removeItem(maiStorageKey('open'));\n        sessionStorage.removeItem('mai-open');\n      } catch (e) {}\n      track('mai_close', { fullscreen: fullscreen ? 'true' : 'false' });\n      if (fullscreen) setFullscreen(false);\n      stopSpeaking();\n      // Keep the panel + backdrop in the DOM so the slide-out animates;\n      // re-apply \\`hidden\\` only after the transform settles. Match the\n      // 200ms transform/opacity duration in CSS.\n      if (_closeTimer) clearTimeout(_closeTimer);\n      _closeTimer = setTimeout(function () {\n        panel.hidden = true;\n        backdrop.hidden = true;\n        _closeTimer = null;\n        if (onMaiRoute) {\n          window.location.replace('/');\n        }\n      }, 220);\n    }\n    closeBtn.addEventListener('click', close);\n    backdrop.addEventListener('click', close);\n\n    function triggerPrompt(prompt, autoSend) {\n      const q = (prompt || '').trim();\n      if (!q) return;\n      input.value = q;\n      if (autoSend) {\n        handle(q, { explicitMedium: 'text' });\n      } else {\n        input.focus({ preventScroll: true });\n      }\n    }\n\n    // Bind any topbar trigger (or anywhere on the site) marked data-mai-open.\n    // Also listen for a dispatched event so callers in other scripts can open\n    // the drawer programmatically without a DOM lookup.\n    document.querySelectorAll('[data-mai-open]').forEach((el) => {\n      el.addEventListener('click', (e) => {\n        e.preventDefault();\n        const source = el.getAttribute('data-mai-source') || 'nav';\n        const prompt = el.getAttribute('data-mai-prompt') || '';\n        const autoSend = el.getAttribute('data-mai-autosend') !== 'false';\n        open({ source });\n        if (prompt) {\n          setTimeout(() => triggerPrompt(prompt, autoSend), 90);\n        }\n      });\n    });\n    window.addEventListener('mai:open', (evt) => {\n      const detail = evt && evt.detail ? evt.detail : {};\n      const source = detail.source || 'programmatic';\n      const prompt = detail.prompt || '';\n      const autoSend = detail.autoSend !== false;\n      const fullscreen = detail.forceFullscreen === true;\n      open({ source, fullscreen });\n      if (prompt) {\n        setTimeout(() => triggerPrompt(prompt, autoSend), 90);\n      }\n    });\n\n    function consumePendingOpen() {\n      try {\n        const raw = sessionStorage.getItem('mai:pending-open');\n        if (!raw) return;\n        sessionStorage.removeItem('mai:pending-open');\n        const detail = JSON.parse(raw) || {};\n        const source = detail.source || 'mai_pending_open';\n        const prompt = detail.prompt || '';\n        const autoSend = detail.autoSend !== false;\n        const fullscreen = detail.forceFullscreen === true;\n        open({ source, fullscreen });\n        if (prompt) {\n          setTimeout(() => triggerPrompt(prompt, autoSend), 90);\n        }\n      } catch (e) {}\n    }\n\n    // Restore from sessionStorage. The pre-paint script in BaseLayout\n    // already set html.mai-open so the body shift is in place; we just\n    // need to unhide the aside, hide the FAB, and not steal focus.\n    try {\n      const savedVersion = localStorage.getItem('mai:client-version');\n      if (savedVersion !== MAI_CLIENT_VERSION) {\n        clearLegacyMaiState();\n        localStorage.setItem('mai:client-version', MAI_CLIENT_VERSION);\n      }\n      // Detect a real page refresh — if so, reset the conversation.\n      // Otherwise hydrate the log + chips so the convo survives in-tab nav.\n      const navEntry = performance.getEntriesByType && performance.getEntriesByType('navigation')[0];\n      const isReload = navEntry\n        ? navEntry.type === 'reload'\n        : (performance.navigation && performance.navigation.type === 1);\n      if (isReload) {\n        localStorage.removeItem(maiStorageKey('transcript'));\n        localStorage.removeItem(maiStorageKey('chips'));\n        localStorage.removeItem(maiStorageKey('topics-hidden'));\n        sessionStorage.removeItem(maiStorageKey('open'));\n        sessionStorage.removeItem('mai-open');\n      } else {\n        const t = localStorage.getItem(maiStorageKey('transcript'));\n        if (t) {\n          log.innerHTML = t;\n          log.scrollTop = log.scrollHeight;\n        }\n        const c = localStorage.getItem(maiStorageKey('chips'));\n        if (c) {\n          chipsBar.innerHTML = c;\n          rewireChipButtons();\n        }\n        if (localStorage.getItem(maiStorageKey('topics-hidden')) === '1' && topicsEl) {\n          topicsEl.hidden = true;\n        }\n      }\n      if (\n        sessionStorage.getItem(maiStorageKey('open')) === 'true' ||\n        sessionStorage.getItem('mai-open') === 'true'\n      ) {\n        open({ restoring: true });\n      }\n\n      consumePendingOpen();\n\n    } catch (e) {}\n\n    // ---------- Expand / collapse ----------\n    // URL state: expanding to fullscreen pushes /mai so the experience is\n    // linkable + shareable; collapsing pops back to the previous path. The\n    // dedicated /mai page dispatches mai:open with forceFullscreen so direct\n    // navigation lands users in the same fullscreen experience without a\n    // duplicate implementation.\n    const MAI_PATH = '/mai/';\n    let _prevPathBeforeFullscreen = null;\n\n    function isMaiRoute(pathname) {\n      const path = (pathname || window.location.pathname || '').replace(/\\\\/+$/, '');\n      return path === '/mai';\n    }\n\n    function setFullscreen(on) {\n      fullscreen = on;\n      panel.classList.toggle('mai-fullscreen', on);\n      document.documentElement.classList.toggle('mai-fullscreen-open', on);\n      if (classicNav) classicNav.hidden = !on;\n      expandBtn.setAttribute('aria-pressed', on ? 'true' : 'false');\n      expandBtn.setAttribute('aria-label', on ? 'Collapse M.ai' : 'Expand M.ai to full screen');\n      expandBtn.setAttribute('title', on ? 'Collapse' : 'Expand');\n      expandIcon.hidden = on;\n      collapseIcon.hidden = !on;\n    }\n\n    function toggleFullscreenFromUI() {\n      const goingFullscreen = !fullscreen;\n      try {\n        if (goingFullscreen && !isMaiRoute()) {\n          _prevPathBeforeFullscreen =\n            window.location.pathname + window.location.search + window.location.hash;\n          history.pushState(\n            { mai: 'fullscreen', prev: _prevPathBeforeFullscreen },\n            '',\n            MAI_PATH,\n          );\n        } else if (!goingFullscreen && isMaiRoute()) {\n          const target = _prevPathBeforeFullscreen || '/';\n          _prevPathBeforeFullscreen = null;\n          history.pushState({ mai: 'collapsed' }, '', target);\n        }\n      } catch (e) {}\n      setFullscreen(goingFullscreen);\n    }\n\n    expandBtn.addEventListener('click', toggleFullscreenFromUI);\n\n    // Sync UI when browser back/forward crosses /mai.\n    window.addEventListener('popstate', () => {\n      const onMai = isMaiRoute();\n      if (onMai && !fullscreen) {\n        if (panel.hidden) open({ source: 'popstate_mai', restoring: true, fullscreen: true });\n        setFullscreen(true);\n      } else if (!onMai && fullscreen) {\n        setFullscreen(false);\n      }\n    });\n\n    // If this page loaded directly at /mai, ensure fullscreen experience is on.\n    setTimeout(() => {\n      if (isMaiRoute()) {\n        if (panel.hidden) open({ source: 'mai_route', restoring: true, fullscreen: true });\n        if (!fullscreen) setFullscreen(true);\n      }\n    }, 0);\n\n    // Topic buttons \\\\u2014 each fires the same flow as a typed/spoken question.\n    topicsEl.querySelectorAll('.mai-topic').forEach((btn) => {\n      btn.addEventListener('click', () => {\n        const q = btn.getAttribute('data-q') || '';\n        track('mai_topic_click', { topic: q });\n        handle(q, { explicitMedium: 'text' });\n      });\n    });\n\n    let _bubbleSeq = 0;\n\n    // ---------- Transcript persistence ----------\n    // Per the sticky-view-state playbook: in-tab nav preserves the\n    // conversation; a real refresh resets it; closing the tab forgets\n    // everything. Scope = sessionStorage. Reload detected via\n    // PerformanceNavigationTiming.\n    function saveTranscript() {\n      try {\n        // Don't snapshot mid-stream — the bubble would restore as a\n        // half-rendered placeholder. The final write at end-of-stream\n        // catches it.\n        if (log.querySelector('.mai-stream')) return;\n        localStorage.setItem(maiStorageKey('transcript'), log.innerHTML);\n        localStorage.setItem(maiStorageKey('chips'), chipsBar.innerHTML);\n        if (topicsEl) localStorage.setItem(maiStorageKey('topics-hidden'), topicsEl.hidden ? '1' : '0');\n      } catch (e) {}\n    }\n    function rewireChipButtons() {\n      // innerHTML restore loses click handlers — reattach them so\n      // restored suggestion chips remain interactive.\n      chipsBar.querySelectorAll('button').forEach((b) => {\n        b.addEventListener('click', () => handle(b.textContent || '', { explicitMedium: 'text' }));\n      });\n    }\n\n    function addBubble(who, html) {\n      const wrap = document.createElement('div');\n      wrap.className = who === 'mai'\n        ? 'flex justify-start'\n        : 'flex justify-end';\n      const bubble = document.createElement('div');\n      const id = \\`mai-bubble-\\${++_bubbleSeq}\\`;\n      bubble.id = id;\n      bubble.dataset.role = who;\n      bubble.className = who === 'mai'\n        ? 'max-w-[85%] rounded-lg bg-cream text-ink px-3 py-2 space-y-2'\n        : 'max-w-[85%] rounded-lg bg-ink text-paper px-3 py-2';\n      bubble.innerHTML = html;\n      wrap.appendChild(bubble);\n      log.appendChild(wrap);\n      log.scrollTop = log.scrollHeight;\n      saveTranscript();\n      return id;\n    }\n\n    function renderChips(items) {\n      chipsBar.innerHTML = '';\n      if (!items || !items.length) return;\n      for (const text of items) {\n        const b = document.createElement('button');\n        b.type = 'button';\n        b.textContent = text;\n        b.className = 'rounded-full border border-ink/15 px-2.5 py-1 text-[11px] text-muted hover:border-accent hover:text-accent transition-colors';\n        b.addEventListener('click', () => handle(text, { explicitMedium: 'text' }));\n        chipsBar.appendChild(b);\n      }\n      saveTranscript();\n    }\n\n    function handle(text, options = {}) {\n      const raw = (text || '').trim();\n      if (!raw) return;\n      if (options.explicitMedium === 'voice') setResponseMedium('voice');\n      else if (options.explicitMedium === 'text') setResponseMedium('text');\n      const shouldSpeakThisTurn = speakReplies || Boolean(options.fromVoice);\n      // Apply speech-to-text corrections (toy box → toybox, M A I → M.ai,\n      // play book → playbook, etc.). Helps both spoken AND typed inputs.\n      let clean = expandSpeech(raw);\n\n      // 0) If a message-to-Flora flow is in progress, route every input\n      // through its state machine. Returns true if consumed.\n      if (messageFlow) {\n        progressMessageFlow(clean);\n        return;\n      }\n\n      // Detect \"I want to send a message\" / \"leave a message\" / \"tell Flora\"\n      // / \"message her\" intents and start the multi-turn flow. Done before\n      // intent classification so these don't fall through to the LLM.\n      // Loose \".{0,30}\" between verb and noun so \"send Flora a message\"\n      // matches as easily as \"send a message\".\n      const MESSAGE_INTENT_RE = /\\\\b(send|leave|drop|forward)\\\\b.{0,30}\\\\b(message|note)\\\\b|\\\\b(message|tell|write to|reach out to)\\\\s+(her|flora|flora\\\\s*may)\\\\b|\\\\bi(?:'| ?a)?d (?:like|love|want) to (?:contact|reach|message|write|leave a message|email|tell)\\\\b/i;\n      if (MESSAGE_INTENT_RE.test(clean)) {\n        addBubble('user', escapeHtml(raw));\n        input.value = '';\n        startMessageFlow('intent');\n        return;\n      }\n\n      track('mai_query_submit', {\n        source: options.fromVoice ? 'voice' : 'text',\n        query_length: clean.length,\n      });\n\n      // If a navigation offer is pending from the last answer and the user's\n      // input is a short yes/no, honor it instead of treating it as a new\n      // question. Anything else just clears the pending offer and falls\n      // through to normal handling.\n      if (pendingNav) {\n        const t = clean.toLowerCase();\n        const AFFIRM = /^(y|ya|ye|yes+|yea+h?|yep|yup|sure|ok(ay)?|please|do it|go|take me( there)?|let'?s go|sounds good|absolutely|definitely)[.! ]*$/;\n        const DENY = /^(no+|nope|nah|cancel|don'?t|stop|not now|later)[.! ]*$/;\n        if (AFFIRM.test(t)) {\n          const target = pendingNav;\n          pendingNav = null;\n          track('mai_nav_cta_click', { action: 'accept' });\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          addBubble('mai', \\`Taking you to <em>\\${escapeHtml(target.label)}</em>…\\`);\n          saveTranscript();\n          // Close the panel on mobile only (full-screen drawer would hide\n          // the destination). Desktop keeps the side rail open so the\n          // conversation continues alongside the new page. Small delay so\n          // the confirmation bubble paints before the page unloads.\n          const isMobile = window.matchMedia('(max-width: 767px)').matches;\n          setTimeout(() => {\n            if (isMobile) close();\n            window.location.assign(target.url);\n          }, 250);\n          return;\n        }\n        if (DENY.test(t)) {\n          pendingNav = null;\n          track('mai_nav_cta_click', { action: 'decline' });\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          addBubble('mai', 'No problem — what else can I help you find?');\n          // Strip the CTA from the last assistant bubble so it doesn't\n          // linger as a dangling offer.\n          const ctas = log.querySelectorAll('[data-mai-nav-cta]');\n          if (ctas.length) ctas[ctas.length - 1].remove();\n          renderChips(STARTER_CHIPS);\n          saveTranscript();\n          return;\n        }\n        // Neither — treat as a new question. Drop the offer silently.\n        pendingNav = null;\n      }\n\n      if (pendingUiAction) {\n        const t = clean.toLowerCase();\n        const AFFIRM = /^(y|ya|ye|yes+|yea+h?|yep|yup|sure|ok(ay)?|please|do it|go|run it|do that|sounds good|absolutely|definitely)[.! ]*$/;\n        const DENY = /^(no+|nope|nah|cancel|don'?t|stop|not now|later)[.! ]*$/;\n        if (AFFIRM.test(t)) {\n          const action = pendingUiAction;\n          pendingUiAction = null;\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          executeUiAction(action, 'typed_confirm');\n          saveTranscript();\n          return;\n        }\n        if (DENY.test(t)) {\n          const action = pendingUiAction;\n          pendingUiAction = null;\n          setActionResult({\n            type: action.type,\n            status: 'declined',\n            target: action.target,\n            label: action.label,\n            source: 'typed_decline',\n          });\n          track('mai_action_execute', { type: action.type, status: 'declined', source: 'typed_decline' });\n          addBubble('user', escapeHtml(raw));\n          input.value = '';\n          addBubble('mai', 'Understood. I will not run that action.');\n          const ctas = log.querySelectorAll('[data-mai-action-cta]');\n          if (ctas.length) ctas[ctas.length - 1].remove();\n          saveTranscript();\n          return;\n        }\n      }\n      // If the vocabulary is already loaded (we kick that off when the\n      // panel opens), repair any unknown words against the indexed terms.\n      // Catches misspellings the hand-listed table can't anticipate:\n      // \"packbot\" → \"pacbot\", \"vehra\" → \"vera\", \"miendelf\" → \"mindelf\",\n      // \"tabloos\" → \"tabulas\". Best-effort: if vocab isn't ready yet on\n      // the very first query, BM25 retrieval will still try the raw form.\n      // Keep the visitor's original wording intact for display + LLM,\n      // and use corrected text only for retrieval ranking.\n      let retrievalQuery = clean;\n      if (_vocabReady) retrievalQuery = fuzzyCorrectQuery(retrievalQuery, _vocabReady);\n      // First interaction collapses the topic grid — the conversation owns\n      // the panel from this point on. The chips bar still surfaces topics.\n      if (topicsEl && !topicsEl.hidden) topicsEl.hidden = true;\n      addBubble('user', escapeHtml(raw));\n      input.value = '';\n\n      // 1) Guard rails first — anything abusive, NSFW, an injection attempt,\n      // a paste-bomb, PII, or a meta question gets a canned reply BEFORE\n      // we burn any tokens calling the worker. The KB matcher doesn't get\n      // a chance to misroute these either.\n      const preIntent = classifyIntent(clean);\n      const HARD_BLOCK = new Set(['hostile', 'nsfw', 'jailbreak', 'pii', 'too-long', 'meta']);\n      if (HARD_BLOCK.has(preIntent)) {\n        const reply = intentReply(preIntent);\n        addBubble('mai', reply);\n        renderChips(getDynamicChips(clean));\n        if (shouldSpeakThisTurn) speak(stripHtml(reply));\n        return;\n      }\n\n      // 2) Identity / greeting / contact — hand-written wins outright.\n      const kb = kbAnswerFor(clean);\n      if (kb) {\n        addBubble('mai', kb.answer);\n        renderChips(kb.followups || STARTER_CHIPS);\n        if (shouldSpeakThisTurn) speak(stripHtml(kb.answer));\n        return;\n      }\n\n      // 2.5) Deterministic context/navigation intents. These should not\n      // depend on keyword retrieval quality.\n      const asksPageContext = /\\\\b(what page am i(?: in| on)?|where am i|what page is this|which page am i on)\\\\b/i.test(clean);\n      const asksOldestPlaybook = /\\\\b(oldest|first|earliest)\\\\s+playbook\\\\b/i.test(clean);\n      if (asksPageContext || asksOldestPlaybook) {\n        const page = getPageContext();\n        const pagePath = page.path || window.location.pathname || '/';\n        const pageLabel = trimText(pathToName(pagePath), 120);\n        const pageLine = \\`You're on <strong>\\${escapeHtml(pageLabel)}</strong>.\\`;\n\n        if (!asksOldestPlaybook) {\n          addBubble('mai', \\`\\${pageLine}<br><br>If you want, I can navigate you to Work, Writing, Toybox, or About.\\`);\n          renderChips(getDynamicChips(clean));\n          if (shouldSpeakThisTurn) speak(stripHtml(pageLine));\n          return;\n        }\n\n        const pendingId = addBubble(\n          'mai',\n          '<span class=\"mai-thinking text-muted\">Checking the Toybox chronology<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>',\n        );\n        loadIndex().then((index) => {\n          const bubble = document.getElementById(pendingId);\n          if (!bubble) return;\n          const oldest = pickOldestPlaybook(index);\n          if (!oldest?.first) {\n            bubble.innerHTML = \\`\\${pageLine}<br><br>I couldn't resolve a published playbook chronology right now, but I can take you to <a class=\"text-accent underline\" href=\"\\${base}toybox\">Toybox</a>.\\`;\n            renderChips(getDynamicChips(clean));\n            if (shouldSpeakThisTurn) speak('I could not resolve the oldest playbook right now, but I can take you to Toybox.');\n            saveTranscript();\n            return;\n          }\n\n          const pick = oldest.first;\n          const action = normalizeUiAction('navigate', pick.url, \\`Open \\${pick.title}\\`);\n          pendingUiAction = action;\n          const tieMsg = oldest.tieCount > 1\n            ? \\`Multiple playbooks share the oldest published date (\\${escapeHtml(pick.updated || 'unknown')}); I picked \\${escapeHtml(pick.title)} first.\\`\n            : \\`The oldest playbook by published date is <em>\\${escapeHtml(pick.title)}</em>\\${pick.updated ? \\` (\\${escapeHtml(pick.updated)})\\` : ''}.\\`;\n          bubble.innerHTML = \\`\\${pageLine}<br><br>\\${tieMsg}\\${action ? renderActionCta(action) : ''}\\`;\n          renderChips(getDynamicChips(clean));\n          if (shouldSpeakThisTurn) speak(stripHtml(\\`\\${pageLine} \\${tieMsg}\\`));\n          saveTranscript();\n        });\n        return;\n      }\n\n      // 3) Any other non-search shape — honest, scripted reply.\n      const intent = preIntent;\n      if (intent !== 'search') {\n        const reply = intentReply(intent);\n        addBubble('mai', reply);\n        renderChips(getDynamicChips(clean));\n        if (shouldSpeakThisTurn) speak(stripHtml(reply));\n        return;\n      }\n\n      // 4) Otherwise, search the site. Index loads lazily on first query.\n      const thinkingId = addBubble('mai', '<span class=\"mai-thinking text-muted\">Looking through Flora&rsquo;s work<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>');\n      loadIndex().then(async (index) => {\n        // Replace the placeholder bubble in-place so it doesn't pile up.\n        const bubble = document.getElementById(thinkingId);\n        if (!index || !index.docs?.length) {\n          if (bubble) bubble.innerHTML = noMatchAnswer(raw);\n          renderChips(getDynamicChips(clean));\n          saveTranscript();\n          return;\n        }\n        // If fuzzy correction didn't get a chance pre-KB (first-ever\n        // query race), do it now against the loaded vocab so BM25 has\n        // a fair shot at matching mis-spelled proper nouns.\n        if (!_vocabReady) await loadVocab();\n        if (_vocabReady) retrievalQuery = fuzzyCorrectQuery(clean, _vocabReady);\n        const scored = rank(index, retrievalQuery);\n        // Send a wider context to the LLM — it can pick the relevant bits\n        // and ignore the rest. Cheaper than refusing a question we COULD have\n        // answered if we'd looked further down the list.\n        const top = dedupeByDoc(scored, 6);\n        // Low floor: if anything scores above noise, give the LLM a chance.\n        // The model is grounded by the chunks and will refuse on its own if\n        // none are relevant — much rarer than the old client-side cutoff.\n        if (!top.length || top[0].s < 0.6) {\n          if (bubble) bubble.innerHTML = noMatchAnswer(raw);\n          renderChips(getDynamicChips(clean));\n          if (shouldSpeakThisTurn) speak(stripHtml(noMatchAnswer(raw)));\n          saveTranscript();\n          return;\n        }\n        // If the LLM tier is configured + healthy, ask it to write a\n        // grounded answer using the top chunks. Otherwise (or on any\n        // failure) fall through to the keyword-result list \\\\u2014 the user\n        // always gets something useful.\n        if (MAI_WORKER_URL && !aiPaused) {\n          if (bubble) bubble.innerHTML = '<span class=\"mai-thinking text-muted\">Reading the relevant pages<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>';\n          const ok = await askWorker(raw, top, bubble, shouldSpeakThisTurn);\n          if (ok) {\n            renderChips(STARTER_CHIPS);\n            return;\n          }\n          // Fell through \\\\u2014 fall back to plain retrieval below.\n        }\n        const html = renderRetrieval(top, retrievalQuery);\n        if (bubble) bubble.innerHTML = html;\n        renderChips(getDynamicChips(clean, top));\n        if (shouldSpeakThisTurn) {\n          // Read the top result's heading + snippet aloud \\\\u2014 not the whole list.\n          const first = top[0].d;\n          speak(\\`\\${first.title}. \\${stripHtml(snippet(first.text, clean))}\\`);\n        }\n      });\n    }\n\n    form.addEventListener('submit', (e) => {\n      e.preventDefault();\n      handle(input.value, { explicitMedium: 'text' });\n    });\n\n    // Delegated handler for the \"Want me to take you there?\" CTA buttons\n    // appended at the end of streamed answers (see renderNavCta). Lives on\n    // \\`log\\` so it survives re-renders and transcript restores.\n    log.addEventListener('click', (e) => {\n      const yes = e.target.closest('[data-mai-nav-yes]');\n      const no = e.target.closest('[data-mai-nav-no]');\n      if (!yes && !no) return;\n      const cta = (yes || no).closest('[data-mai-nav-cta]');\n      if (!cta) return;\n      if (yes) {\n        const url = cta.getAttribute('data-nav-url') || '';\n        pendingNav = null;\n        track('mai_nav_cta_click', { action: 'accept_button' });\n        if (url) {\n          // On mobile the panel is a full-screen drawer that would cover\n          // the destination page while it loads, so close it first for\n          // immediate visual feedback (close() also clears the \\`mai-open\\`\n          // sessionStorage flag so the next page doesn't auto-reopen it).\n          // On desktop the panel is a 24rem side rail — the page is\n          // already visible alongside it, so leave it open.\n          const isMobile = window.matchMedia('(max-width: 767px)').matches;\n          if (isMobile) close();\n          window.location.assign(url);\n        }\n        return;\n      }\n      // No thanks — remove the CTA and clear any pending offer.\n      pendingNav = null;\n      track('mai_nav_cta_click', { action: 'decline_button' });\n      cta.remove();\n      saveTranscript();\n    });\n\n    log.addEventListener('click', (e) => {\n      const run = e.target.closest('[data-mai-action-run]');\n      const no = e.target.closest('[data-mai-action-no]');\n      if (!run && !no) return;\n      const cta = (run || no).closest('[data-mai-action-cta]');\n      if (!cta) return;\n      const action = normalizeUiAction(\n        cta.getAttribute('data-action-type') || '',\n        cta.getAttribute('data-action-target') || '',\n        cta.getAttribute('data-action-label') || '',\n      );\n      if (!action) {\n        cta.remove();\n        pendingUiAction = null;\n        saveTranscript();\n        return;\n      }\n      if (run) {\n        pendingUiAction = null;\n        cta.remove();\n        executeUiAction(action, 'cta_button');\n        saveTranscript();\n        return;\n      }\n      pendingUiAction = null;\n      setActionResult({\n        type: action.type,\n        status: 'declined',\n        target: action.target,\n        label: action.label,\n        source: 'cta_decline',\n      });\n      track('mai_action_execute', { type: action.type, status: 'declined', source: 'cta_decline' });\n      cta.remove();\n      addBubble('mai', 'No problem — I will leave the page as-is.');\n      saveTranscript();\n    });\n\n    // Delegated handler for the \"Or leave a message here\" button rendered\n    // by the contact KB answer. Kicks off the multi-turn message flow.\n    log.addEventListener('click', (e) => {\n      const trigger = e.target.closest('[data-mai-start-message]');\n      if (!trigger) return;\n      if (messageFlow) return; // already in flow\n      startMessageFlow('contact_cta');\n    });\n\n    log.addEventListener('click', async (e) => {\n      const trigger = e.target.closest('[data-mai-play-pending-audio]');\n      if (!trigger || !pendingAudioUrl) return;\n      stopSpeaking();\n      const url = pendingAudioUrl;\n      pendingAudioUrl = null;\n      try {\n        await playAudioUrl(url);\n        const wrap = trigger.closest('.flex');\n        if (wrap) wrap.remove();\n      } catch {\n        pendingAudioUrl = url;\n        addBubble('mai', \\`Audio playback is still blocked on this device.\\`);\n      }\n    });\n\n    // Delegated handler for the message-flow confirm step (Send / Cancel\n    // buttons rendered into the confirm bubble).\n    log.addEventListener('click', (e) => {\n      const send = e.target.closest('[data-mai-msg-send]');\n      const cancel = e.target.closest('[data-mai-msg-cancel]');\n      if (!send && !cancel) return;\n      const cta = (send || cancel).closest('[data-mai-msg-cta]');\n      if (cta) cta.remove();\n      if (send) {\n        if (messageFlow && messageFlow.stage === 'confirm') {\n          sendMessageFlow();\n        }\n        return;\n      }\n      // Cancel\n      messageFlow = null;\n      track('mai_message_flow_cancel', { source: 'confirm_cta' });\n      const prompt = \\`No problem — nothing sent. What else can I help with?\\`;\n      addBubble('mai', prompt);\n      if (speakReplies) speak(prompt);\n      renderChips(STARTER_CHIPS);\n      saveTranscript();\n    });\n\n    // ---------- Voice in (Whisper via worker, with SpeechRecognition fallback) ----------\n    const SR = window.SpeechRecognition || window.webkitSpeechRecognition;\n    const UA = navigator.userAgent || '';\n    // iOS Safari technically exposes webkitSpeechRecognition but the\n    // implementation is unreliable in the wild — frequent silent failures,\n    // no recognition events on some iOS versions. Warn the user once.\n    const IS_IOS = /iPad|iPhone|iPod/.test(UA) && !window.MSStream;\n    const IS_ELECTRON = /\\\\bElectron\\\\/\\\\d+/i.test(UA);\n    const IS_EDGE = /\\\\bEdg\\\\//.test(UA);\n    const WORKER_BASE_URL = (MAI_WORKER_URL || '').replace(/\\\\/$/, '');\n    const TTS_VOICE = 'nova';\n    const TRANSCRIBE_HINT =\n      'Names may include Flora May dela Cruz, M.ai, Toybox, PacBot, Mindelf, Pale and Bright, CodeCatalyst, Vera, Tabulas.';\n    let recog = null;\n    let listening = false;\n    let whisperRecorder = null;\n    let whisperStream = null;\n    let whisperChunks = [];\n    let whisperMime = '';\n    let whisperAudioContext = null;\n    let whisperAnalyser = null;\n    let whisperSource = null;\n    let whisperMonitorRaf = null;\n    let whisperSilentSince = 0;\n    let whisperStartedAt = 0;\n    let currentAudio = null;\n    let currentAudioUrl = null;\n    let pendingAudioUrl = null;\n    let ttsAbort = null;\n    // Track the \"Listening…\" placeholder bubble so we can clear it on result\n    // or error instead of leaving a ghost in the log.\n    let listeningBubbleId = null;\n    // Safety net: if no result/end/error fires within this window (e.g. user\n    // ignored the permission popup, or recognition silently hung), abort.\n    let micTimeout = null;\n    function clearListeningBubble() {\n      if (!listeningBubbleId) return;\n      const el = document.getElementById(listeningBubbleId);\n      if (el && el.parentElement) el.parentElement.remove();\n      listeningBubbleId = null;\n    }\n    function clearMicTimeout() {\n      if (micTimeout) { clearTimeout(micTimeout); micTimeout = null; }\n    }\n    function setMicState(on) {\n      listening = on;\n      micBtn.setAttribute('aria-pressed', on ? 'true' : 'false');\n      micBtn.classList.toggle('text-accent', on);\n      if (!on) clearMicTimeout();\n    }\n    function canUseWhisperMic() {\n      return Boolean(\n        WORKER_BASE_URL &&\n        window.MediaRecorder &&\n        navigator.mediaDevices?.getUserMedia,\n      );\n    }\n    function bestRecorderMimeType() {\n      const options = [\n        'audio/webm;codecs=opus',\n        'audio/webm',\n        'audio/mp4',\n      ];\n      for (const t of options) {\n        if (MediaRecorder.isTypeSupported?.(t)) return t;\n      }\n      return '';\n    }\n    function extensionForMime(type) {\n      if (!type) return 'webm';\n      if (type.includes('mp4')) return 'm4a';\n      if (type.includes('ogg')) return 'ogg';\n      if (type.includes('wav')) return 'wav';\n      return 'webm';\n    }\n    async function transcribeWhisperBlob(blob, mimeType) {\n      const ext = extensionForMime(mimeType || blob.type || 'audio/webm');\n      const file = new File([blob], \\`speech.\\${ext}\\`, {\n        type: mimeType || blob.type || 'audio/webm',\n      });\n      const formData = new FormData();\n      formData.append('audio', file);\n      formData.append('prompt', TRANSCRIBE_HINT);\n\n      let res;\n      try {\n        res = await fetch(\\`\\${WORKER_BASE_URL}/transcribe\\`, {\n          method: 'POST',\n          body: formData,\n        });\n      } catch {\n        throw new Error('network');\n      }\n\n      let payload = null;\n      try { payload = await res.json(); } catch {}\n      if (!res.ok) {\n        const err = new Error(payload?.error || 'transcribe_failed');\n        err.code = payload?.error || \\`http_\\${res.status}\\`;\n        throw err;\n      }\n      const text = String(payload?.text || '').trim();\n      if (!text) {\n        const err = new Error('empty_transcript');\n        err.code = 'empty_transcript';\n        throw err;\n      }\n      return text;\n    }\n    function stopWhisperMonitor() {\n      if (whisperMonitorRaf) {\n        cancelAnimationFrame(whisperMonitorRaf);\n        whisperMonitorRaf = null;\n      }\n      whisperSilentSince = 0;\n      try { whisperSource?.disconnect(); } catch {}\n      whisperSource = null;\n      whisperAnalyser = null;\n      if (whisperAudioContext) {\n        try { whisperAudioContext.close(); } catch {}\n        whisperAudioContext = null;\n      }\n    }\n    function stopWhisperStream() {\n      stopWhisperMonitor();\n      if (!whisperStream) return;\n      try { whisperStream.getTracks().forEach((t) => t.stop()); } catch {}\n      whisperStream = null;\n    }\n    function stopWhisperRecorder() {\n      if (whisperRecorder && whisperRecorder.state !== 'inactive') {\n        try { whisperRecorder.stop(); } catch {}\n      }\n    }\n    function startWhisperSilenceMonitor() {\n      if (!whisperStream || !whisperRecorder) return;\n      try {\n        whisperAudioContext = new (window.AudioContext || window.webkitAudioContext)();\n        whisperSource = whisperAudioContext.createMediaStreamSource(whisperStream);\n        whisperAnalyser = whisperAudioContext.createAnalyser();\n        whisperAnalyser.fftSize = 2048;\n        whisperSource.connect(whisperAnalyser);\n      } catch {\n        return;\n      }\n\n      const data = new Uint8Array(whisperAnalyser.fftSize);\n      whisperStartedAt = Date.now();\n      const MIN_RECORD_MS = 700;\n      const SILENCE_STOP_MS = 550;\n      const NOISE_FLOOR_RMS = 0.018;\n\n      const tick = () => {\n        if (!whisperRecorder || whisperRecorder.state !== 'recording' || !whisperAnalyser) return;\n        whisperAnalyser.getByteTimeDomainData(data);\n        let sum = 0;\n        for (let i = 0; i < data.length; i++) {\n          const n = (data[i] - 128) / 128;\n          sum += n * n;\n        }\n        const rms = Math.sqrt(sum / data.length);\n        const now = Date.now();\n        if (rms < NOISE_FLOOR_RMS && now - whisperStartedAt > MIN_RECORD_MS) {\n          if (!whisperSilentSince) whisperSilentSince = now;\n          if (now - whisperSilentSince > SILENCE_STOP_MS) {\n            stopWhisperRecorder();\n            return;\n          }\n        } else {\n          whisperSilentSince = 0;\n        }\n        whisperMonitorRaf = requestAnimationFrame(tick);\n      };\n\n      whisperMonitorRaf = requestAnimationFrame(tick);\n    }\n    async function startWhisperCapture() {\n      if (navigator.onLine === false) {\n        addBubble('mai', \\`You're offline right now, so voice input can't start. Reconnect to the internet and try again, or type your question.\\`);\n        return;\n      }\n\n      try {\n        whisperStream = await navigator.mediaDevices.getUserMedia({\n          audio: {\n            channelCount: 1,\n            sampleRate: 16000,\n            echoCancellation: true,\n            noiseSuppression: true,\n            autoGainControl: true,\n          },\n        });\n      } catch (err) {\n        const name = err?.name || '';\n        if (name === 'NotAllowedError' || name === 'SecurityError') {\n          addBubble('mai', MIC_ERROR_COPY['not-allowed']);\n        } else if (name === 'NotFoundError' || name === 'OverconstrainedError') {\n          addBubble('mai', MIC_ERROR_COPY['audio-capture']);\n        } else {\n          addBubble('mai', \\`The microphone couldn't start (\\${escapeHtml(name || 'unknown')}). You can type instead.\\`);\n        }\n        return;\n      }\n\n      whisperChunks = [];\n      whisperMime = bestRecorderMimeType();\n\n      try {\n        whisperRecorder = whisperMime\n          ? new MediaRecorder(whisperStream, { mimeType: whisperMime })\n          : new MediaRecorder(whisperStream);\n      } catch {\n        stopWhisperStream();\n        addBubble('mai', \\`This browser couldn't initialize audio recording for Whisper. You can type your question instead.\\`);\n        return;\n      }\n\n      whisperRecorder.addEventListener('dataavailable', (e) => {\n        if (e.data && e.data.size > 0) whisperChunks.push(e.data);\n      });\n\n      whisperRecorder.addEventListener('stop', async () => {\n        setMicState(false);\n        clearListeningBubble();\n        clearMicTimeout();\n        stopWhisperStream();\n\n        if (!whisperChunks.length) {\n          addBubble('mai', \\`I couldn't capture any audio. Try again, or type your question instead.\\`);\n          return;\n        }\n\n        const pendingId = addBubble(\n          'mai',\n          '<span class=\"mai-thinking text-muted\">Transcribing with Whisper<span class=\"mai-thinking-dots\"><span></span><span></span><span></span></span></span>',\n        );\n        const pending = document.getElementById(pendingId);\n\n        try {\n          const blob = new Blob(whisperChunks, { type: whisperMime || whisperChunks[0]?.type || 'audio/webm' });\n          const text = await transcribeWhisperBlob(blob, whisperMime);\n          track('mai_voice_transcribed', { mode: 'whisper', transcript_length: text.length });\n          if (pending) pending.remove();\n          input.value = text;\n          handle(text, { fromVoice: true, explicitMedium: 'voice' });\n        } catch (err) {\n          const code = err?.code || '';\n          track('mai_voice_error', { mode: 'whisper', error: code || 'unknown' });\n          const msg =\n            code === 'rate_limited'\n              ? \\`Voice transcription is temporarily rate-limited. Try again in a bit, or type your question.\\`\n              : code === 'audio_too_large'\n                ? \\`That recording is too long to transcribe in one go. Try a shorter question, then send.\\`\n                : code === 'empty_transcript'\n                  ? \\`I couldn't detect speech in that recording. Try again, a little closer to the mic.\\`\n                  : code === 'network'\n                    ? \\`Couldn't reach the transcription service. Check your connection, then try again.\\`\n                    : \\`I couldn't transcribe that audio right now. You can type your question instead.\\`;\n          if (pending) pending.innerHTML = msg;\n        } finally {\n          whisperChunks = [];\n          whisperRecorder = null;\n        }\n      });\n\n      try {\n        whisperRecorder.start();\n        track('mai_voice_start', { mode: 'whisper' });\n        setMicState(true);\n        startWhisperSilenceMonitor();\n        listeningBubbleId = addBubble(\n          'mai',\n          '<span class=\"text-muted\">Listening... I will send automatically when you pause.</span>',\n        );\n        micTimeout = setTimeout(() => {\n          stopWhisperRecorder();\n        }, 12000);\n      } catch (err) {\n        track('mai_voice_error', { mode: 'whisper', error: 'start_failed' });\n        setMicState(false);\n        clearListeningBubble();\n        stopWhisperStream();\n        whisperRecorder = null;\n        addBubble('mai', \\`The microphone couldn't start. \\${escapeHtml(err?.message || 'Try again, or type your question instead.')}\\`);\n      }\n    }\n    // Error codes per the SpeechRecognition spec. We name what went wrong\n    // instead of silently resetting the button — the old behaviour looked\n    // like the mic was broken.\n    const MIC_ERROR_COPY = {\n      'not-allowed': \\`Microphone permission was blocked. Click the lock icon in your browser's address bar, allow the mic for this site, and try again.\\`,\n      'service-not-allowed': \\`Microphone permission was blocked. Click the lock icon in your browser's address bar, allow the mic for this site, and try again.\\`,\n      'no-speech': \\`I didn't hear anything. Try again, a little closer to the mic.\\`,\n      'audio-capture': \\`No microphone was found. Check that one is connected and selected in your system settings.\\`,\n      'network': \\`Speech recognition couldn't reach the browser speech service. Check your internet connection, disable strict tracker/ad blocking for this site, or try Chrome/Edge on desktop. You can always type your question instead.\\`,\n      'aborted': null, // user cancelled — no message\n    };\n    if (SR) {\n      recog = new SR();\n      recog.lang = 'en-US';\n      recog.interimResults = false;\n      recog.maxAlternatives = 1;\n      recog.addEventListener('result', (e) => {\n        clearMicTimeout();\n        clearListeningBubble();\n        const text = e.results[0][0].transcript;\n        input.value = text;\n        handle(text, { fromVoice: true, explicitMedium: 'voice' });\n      });\n      recog.addEventListener('end', () => {\n        setMicState(false);\n        // Leave any error bubble in place; only clear the live placeholder.\n        if (listeningBubbleId) {\n          const el = document.getElementById(listeningBubbleId);\n          if (el && el.textContent?.includes('Listening')) clearListeningBubble();\n        }\n      });\n      recog.addEventListener('error', (e) => {\n        setMicState(false);\n        clearListeningBubble();\n        const code = e?.error || 'unknown';\n        // 'aborted' is the user stopping the mic — don't bother them with a message.\n        if (code === 'aborted') return;\n        const msg =\n          code === 'network' && navigator.onLine === false\n            ? \\`You're offline right now, so voice input can't start. Reconnect to the internet and try again, or type your question.\\`\n            : code === 'network' && IS_EDGE\n              ? \\`Edge could not reach the speech service. In Edge, allow mic access for this site, set Tracking prevention to Balanced (not Strict), and temporarily disable ad/privacy extensions for floramaydc.com, then try again.\\`\n              : MIC_ERROR_COPY[code] || \\`The microphone didn't respond (\\${escapeHtml(code)}). You can type instead.\\`;\n        addBubble('mai', msg);\n      });\n    }\n    // Prefer Whisper when the worker URL + MediaRecorder are available;\n    // fallback to browser SpeechRecognition otherwise.\n    micBtn.addEventListener('click', async () => {\n      if (canUseWhisperMic()) {\n        if (listening) {\n          stopWhisperRecorder();\n          return;\n        }\n        await startWhisperCapture();\n        return;\n      }\n\n      // Stop a session in progress.\n      if (listening) {\n        try { recog?.stop(); } catch {}\n        clearMicTimeout();\n        clearListeningBubble();\n        setMicState(false);\n        return;\n      }\n      // No SpeechRecognition at all → tell the user why.\n      if (!SR) {\n        addBubble(\n          'mai',\n          \\`Voice input isn't supported in this browser. It works in Chrome and Edge on desktop. You can type your question instead.\\`,\n        );\n        return;\n      }\n      // Embedded app browsers (including Electron-hosted webviews) often\n      // expose SpeechRecognition but fail to reach speech services.\n      if (IS_ELECTRON) {\n        addBubble(\n          'mai',\n          \\`Voice input is limited in embedded app browsers. Open this site in Chrome, Edge, or Safari, then try the mic again. You can type your question here anytime.\\`,\n        );\n        return;\n      }\n      // Browser is offline: fail fast with a specific message instead of\n      // waiting for SpeechRecognition to emit a generic network error.\n      if (navigator.onLine === false) {\n        addBubble('mai', \\`You're offline right now, so voice input can't start. Reconnect to the internet and try again, or type your question.\\`);\n        return;\n      }\n      // Pre-flight permission check (where supported) — catches the\n      // \"user previously denied\" case BEFORE we attempt recognition,\n      // which on Safari often emits no error event at all.\n      try {\n        if (navigator.permissions?.query) {\n          const status = await navigator.permissions.query({ name: 'microphone' });\n          if (status.state === 'denied') {\n            addBubble('mai', MIC_ERROR_COPY['not-allowed']);\n            return;\n          }\n        }\n      } catch { /* Permissions API not available for 'microphone' on this browser — ignore. */ }\n      // Explicit getUserMedia warm-up: reliably surfaces the permission\n      // prompt on Chrome/Safari/Edge in cases where SpeechRecognition.start()\n      // alone fails to do so. We release the stream immediately so we don't\n      // hold the mic open in parallel with recognition.\n      if (navigator.mediaDevices?.getUserMedia) {\n        try {\n          const stream = await navigator.mediaDevices.getUserMedia({ audio: true });\n          stream.getTracks().forEach((t) => t.stop());\n        } catch (err) {\n          const name = err?.name || '';\n          if (name === 'NotAllowedError' || name === 'SecurityError') {\n            addBubble('mai', MIC_ERROR_COPY['not-allowed']);\n          } else if (name === 'NotFoundError' || name === 'OverconstrainedError') {\n            addBubble('mai', MIC_ERROR_COPY['audio-capture']);\n          } else {\n            addBubble('mai', \\`The microphone couldn't start (\\${escapeHtml(name || 'unknown')}). You can type instead.\\`);\n          }\n          return;\n        }\n      }\n      // Start the recognizer. Catch InvalidStateError specifically — happens\n      // when a previous session is still tearing down and the user clicks again.\n      try {\n        recog.start();\n        track('mai_voice_start', { mode: 'browser' });\n        setMicState(true);\n        listeningBubbleId = addBubble(\n          'mai',\n          IS_IOS\n            ? '<span class=\"text-muted\">Listening… (voice on iOS is unreliable — typing usually works better.)</span>'\n            : '<span class=\"text-muted\">Listening… speak now.</span>',\n        );\n        // Safety timeout: if no recognition events fire within 15 s\n        // (user ignored the permission popup, browser hung, etc.),\n        // abort cleanly and tell them what to try.\n        micTimeout = setTimeout(() => {\n          try { recog.stop(); } catch {}\n          try { recog.abort?.(); } catch {}\n          setMicState(false);\n          clearListeningBubble();\n          addBubble(\n            'mai',\n            \\`The mic didn't respond. If your browser asked for permission, you may need to allow it and try again — or type your question instead.\\`,\n          );\n        }, 15000);\n      } catch (err) {\n        track('mai_voice_error', { mode: 'browser', error: String(err?.name || 'start_failed') });\n        setMicState(false);\n        clearListeningBubble();\n        const name = err?.name || '';\n        if (name === 'InvalidStateError') {\n          // Recognition was already running — silently reset and let the\n          // user click again.\n          try { recog.abort?.(); } catch {}\n          return;\n        }\n        addBubble('mai', \\`The microphone couldn't start. \\${escapeHtml(err?.message || 'Try again, or type your question instead.')}\\`);\n      }\n    });\n\n    // ---------- Voice out (model TTS via worker only) ----------\n    function speakWithBrowserFallback(text) {\n      if (!('speechSynthesis' in window)) return false;\n      try {\n        window.speechSynthesis.cancel();\n        const u = new SpeechSynthesisUtterance(String(text || '').slice(0, 500));\n        u.rate = 1.0;\n        u.pitch = 1.0;\n        window.speechSynthesis.speak(u);\n        return true;\n      } catch {\n        return false;\n      }\n    }\n\n    async function speak(text) {\n      stopSpeaking();\n      const clean = String(text || '').trim();\n      if (!clean) return;\n\n      if (!WORKER_BASE_URL) {\n        addBubble('mai', \\`Voice playback needs the M.ai worker URL. It is currently unavailable.\\`);\n        return;\n      }\n\n      ttsAbort = new AbortController();\n      try {\n        const res = await fetch(\\`\\${WORKER_BASE_URL}/speak\\`, {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ text: clean.slice(0, 1200), voice: TTS_VOICE }),\n          signal: ttsAbort.signal,\n        });\n        if (!res.ok) {\n          let detail = '';\n          try {\n            const payload = await res.json();\n            detail = String(payload?.error || '').toLowerCase();\n          } catch {}\n          if (res.status === 403 || detail === 'origin_not_allowed') {\n            addBubble('mai', \\`Voice playback is blocked for this local origin. Open the site from http://localhost (or redeploy worker CORS for your dev URL).\\`);\n          } else {\n            addBubble('mai', \\`Voice playback is temporarily unavailable. Please try again in a moment.\\`);\n          }\n          return;\n        }\n        const blob = await res.blob();\n        const url = URL.createObjectURL(blob);\n        clearPendingAudio();\n        await playAudioUrl(url);\n      } catch (err) {\n        const name = String(err?.name || '');\n        if (name === 'AbortError') return;\n        if (name === 'NotAllowedError') {\n          if (currentAudioUrl) {\n            pendingAudioUrl = currentAudioUrl;\n            currentAudio = null;\n            currentAudioUrl = null;\n          }\n          addBubble('mai', \\`Your phone blocked auto-play audio. <button type=\"button\" class=\"underline text-accent\" data-mai-play-pending-audio>Tap to play the reply</button>.\\`);\n        } else if (name === 'NotSupportedError' || name === 'audio_decode_error') {\n          if (speakWithBrowserFallback(clean)) {\n            addBubble('mai', \\`Your browser couldn't play streamed audio, so I switched to built-in voice for this reply.\\`);\n          } else {\n            addBubble('mai', \\`Voice playback failed this time. Please try again.\\`);\n          }\n        } else {\n          if (speakWithBrowserFallback(clean)) {\n            addBubble('mai', \\`Voice playback had a temporary issue, so I switched to built-in voice for this reply.\\`);\n          } else {\n            addBubble('mai', \\`Voice playback failed this time. Please try again.\\`);\n          }\n        }\n      } finally {\n        ttsAbort = null;\n      }\n    }\n    async function playAudioUrl(url) {\n      const audio = new Audio(url);\n      audio.playsInline = true;\n      currentAudio = audio;\n      currentAudioUrl = url;\n      audio.onended = () => {\n        if (currentAudio === audio) currentAudio = null;\n        if (currentAudioUrl === url) currentAudioUrl = null;\n        try { URL.revokeObjectURL(url); } catch {}\n      };\n      audio.onerror = () => {\n        if (currentAudio === audio) currentAudio = null;\n        if (currentAudioUrl === url) currentAudioUrl = null;\n        try { URL.revokeObjectURL(url); } catch {}\n      };\n      try {\n        await audio.play();\n      } catch (err) {\n        if (String(err?.name || '') === 'NotSupportedError') {\n          const e = new Error('audio_decode_error');\n          e.name = 'audio_decode_error';\n          throw e;\n        }\n        throw err;\n      }\n    }\n\n    function clearPendingAudio() {\n      if (!pendingAudioUrl) return;\n      try { URL.revokeObjectURL(pendingAudioUrl); } catch {}\n      pendingAudioUrl = null;\n    }\n\n    function stopSpeaking() {\n      if (ttsAbort) {\n        try { ttsAbort.abort(); } catch {}\n        ttsAbort = null;\n      }\n      if (currentAudio) {\n        try { currentAudio.pause(); } catch {}\n        try { currentAudio.currentTime = 0; } catch {}\n        currentAudio = null;\n      }\n      if (currentAudioUrl) {\n        try { URL.revokeObjectURL(currentAudioUrl); } catch {}\n        currentAudioUrl = null;\n      }\n      if ('speechSynthesis' in window) {\n        try { window.speechSynthesis.cancel(); } catch {}\n      }\n    }\n    speakToggle.addEventListener('click', () => {\n      setResponseMedium(speakReplies ? 'text' : 'voice');\n      track('mai_voice_reply_toggle', { enabled: speakReplies ? 'true' : 'false' });\n      if (!speakReplies) {\n        stopSpeaking();\n        return;\n      }\n      // Instant feedback: read the most recent M.ai reply aloud so the user\n      // can hear that it works without needing to ask another question.\n      const lastMai = Array.from(log.querySelectorAll('[id^=\"mai-bubble-\"]'))\n        .reverse()\n        .find((b) => b.dataset.role === 'mai');\n      if (lastMai) {\n        speak(stripHtml(lastMai.innerHTML));\n      } else {\n        // No replies yet — confirm audibly so the user knows the toggle works.\n        speak(\"Voice on. Ask me a question and I'll read the answer aloud.\");\n      }\n    });\n\n    // ---------- Close on Escape ----------\n    document.addEventListener('keydown', (e) => {\n      if (e.key === 'Escape' && !panel.hidden) close();\n    });\n  })();\n})();</script>"])), maybeRenderHead(), addAttribute(base, "href"), addAttribute(`${base}toybox`, "href"), addAttribute(`${base}work`, "href"), addAttribute(`${base}play`, "href"), addAttribute(`${base}writing`, "href"), addAttribute(`${base}about`, "href"), defineScriptVars({ base }));
}, "/private/tmp/flora-component-build/src/components/MAi.astro", void 0);

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a, _b, _c;
const $$Astro = createAstro("https://www.floramaydc.com");
const $$BaseLayout = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$BaseLayout;
  const {
    title,
    description = "Toybox & portfolio by Flora May dela Cruz — Senior Product Designer at Microsoft",
    ogImage = "/og-default.png",
    ogType = "website",
    jsonLd
  } = Astro2.props;
  const base = "/".endsWith("/") ? "/" : "//";
  const gaMeasurementId = "G-2X8SE1JJX7";
  const gscVerification = undefined                                               ?.trim();
  const writingEntries = await getCollection("writing");
  const publishedWritingEntries = writingEntries.filter((entry) => !entry.data.draft);
  const writingCategories = Array.from(new Set(publishedWritingEntries.map((entry) => entry.data.category).filter(Boolean))).sort((a, b) => a.localeCompare(b));
  const siteUrl = Astro2.site?.toString().replace(/\/$/, "") ?? "https://www.floramaydc.com";
  const canonical = new URL(Astro2.url.pathname, Astro2.site ?? "https://www.floramaydc.com").toString();
  const ogImageUrl = ogImage.startsWith("http") ? ogImage : `${siteUrl}${ogImage.startsWith("/") ? ogImage : `/${ogImage}`}`;
  return renderTemplate(_c || (_c = __template(['<html lang="en"> <head><meta charset="UTF-8"><meta name="description"', '><meta name="viewport" content="width=device-width, initial-scale=1">', '<link rel="canonical"', '><link rel="alternate" type="application/rss+xml" title="Flora May dela Cruz — Writing"', '><!-- Open Graph --><meta property="og:type"', '><meta property="og:site_name" content="Flora May dela Cruz"><meta property="og:title"', '><meta property="og:description"', '><meta property="og:url"', '><meta property="og:image"', '><!-- Twitter --><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title"', '><meta name="twitter:description"', '><meta name="twitter:image"', '><link rel="icon" type="image/svg+xml"', '><meta name="generator"', "><script>\n      (function () {\n        try {\n          var stored = localStorage.getItem('theme');\n          var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;\n          var dark = stored ? stored === 'dark' : prefersDark;\n          if (dark) document.documentElement.classList.add('dark');\n          requestAnimationFrame(function () {\n            document.documentElement.classList.add('theme-ready');\n          });\n        } catch (e) {}\n      })();\n    </script><script>\n      (function () {\n        try {\n          var hasVersionedOpen = false;\n          for (var i = 0; i < sessionStorage.length; i++) {\n            var key = sessionStorage.key(i) || '';\n            if (/^mai:.*:open$/.test(key) && sessionStorage.getItem(key) === 'true') {\n              hasVersionedOpen = true;\n              break;\n            }\n          }\n          if (sessionStorage.getItem('mai-open') === 'true' || hasVersionedOpen) {\n            document.documentElement.classList.add('mai-open');\n          }\n        } catch (e) {}\n      })();\n    </script><script>\n      (function () {\n        function clearStuckMaiOpen() {\n          try {\n            var html = document.documentElement;\n            if (!html.classList.contains('mai-open')) return;\n            var panel = document.getElementById('mai-panel');\n            var isVisible = false;\n            if (panel) {\n              var style = window.getComputedStyle(panel);\n              isVisible = !panel.hasAttribute('hidden') && style.display !== 'none' && style.visibility !== 'hidden';\n            }\n            if (!isVisible) {\n              html.classList.remove('mai-open');\n              html.classList.remove('mai-no-scroll');\n              sessionStorage.removeItem('mai-open');\n              for (var i = sessionStorage.length - 1; i >= 0; i--) {\n                var key = sessionStorage.key(i) || '';\n                if (/^mai:.*:open$/.test(key)) sessionStorage.removeItem(key);\n              }\n            }\n          } catch (e) {}\n        }\n\n        if (document.readyState === 'loading') {\n          document.addEventListener('DOMContentLoaded', function () {\n            setTimeout(clearStuckMaiOpen, 250);\n          });\n        } else {\n          setTimeout(clearStuckMaiOpen, 250);\n        }\n      })();\n    </script><script>\n      (function () {\n        var header;\n        var ticking = false;\n        var thresholdEnter = 28;\n        var thresholdExit = 10;\n        var isScrolled = false;\n        function update() {\n          ticking = false;\n          if (!header) return;\n          var y = window.scrollY || 0;\n          if (!isScrolled && y > thresholdEnter) {\n            isScrolled = true;\n            header.classList.add('is-scrolled');\n          } else if (isScrolled && y < thresholdExit) {\n            isScrolled = false;\n            header.classList.remove('is-scrolled');\n          }\n        }\n        function onScroll() {\n          if (!ticking) {\n            requestAnimationFrame(update);\n            ticking = true;\n          }\n        }\n        function init() {\n          header = document.querySelector('.site-header');\n          update();\n          window.addEventListener('scroll', onScroll, { passive: true });\n        }\n        if (document.readyState === 'loading') {\n          document.addEventListener('DOMContentLoaded', init);\n        } else {\n          init();\n        }\n      })();\n    </script><script>\n      (function () {\n        function initThemeToggles() {\n          try {\n          var toggles = Array.prototype.slice.call(document.querySelectorAll('[data-theme-toggle]'));\n          if (!toggles.length) return;\n\n          function syncThemeToggleUi() {\n            var isDark = document.documentElement.classList.contains('dark');\n            var nextAria = isDark ? 'Switch to light mode' : 'Switch to dark mode';\n            var nextMode = isDark ? 'light mode' : 'dark mode';\n            toggles.forEach(function (btn) {\n              btn.setAttribute('aria-label', nextAria);\n              btn.setAttribute('aria-pressed', isDark ? 'true' : 'false');\n              var label = btn.querySelector('[data-theme-toggle-label]');\n              if (label) label.textContent = 'Switch to ' + nextMode;\n              var mode = btn.querySelector('[data-theme-toggle-mode]');\n              if (mode) mode.textContent = nextMode;\n              var darkIcon = btn.querySelector('[data-theme-icon-dark]');\n              var lightIcon = btn.querySelector('[data-theme-icon-light]');\n              if (darkIcon) {\n                if (isDark) darkIcon.setAttribute('hidden', '');\n                else darkIcon.removeAttribute('hidden');\n              }\n              if (lightIcon) {\n                if (!isDark) lightIcon.setAttribute('hidden', '');\n                else lightIcon.removeAttribute('hidden');\n              }\n            });\n          }\n\n          syncThemeToggleUi();\n\n          toggles.forEach(function (btn) {\n            if (btn.dataset.themeToggleBound === 'true') return;\n            btn.dataset.themeToggleBound = 'true';\n            btn.addEventListener('click', function () {\n              var isDark = document.documentElement.classList.toggle('dark');\n              try {\n                localStorage.setItem('theme', isDark ? 'dark' : 'light');\n              } catch (e) {}\n              syncThemeToggleUi();\n            });\n          });\n          } catch (e) {}\n        }\n\n        if (document.readyState === 'loading') {\n          document.addEventListener('DOMContentLoaded', initThemeToggles);\n        } else {\n          initThemeToggles();\n        }\n      })();\n    </script>", "<script>\n      (function () {\n        function trackSiteEvent(name, params) {\n          try {\n            if (!name || typeof window.gtag !== 'function') return;\n            window.gtag('event', name, params || {});\n          } catch (e) {}\n        }\n\n        // Shared helper used by inline component scripts.\n        window.trackSiteEvent = trackSiteEvent;\n\n        // Components can emit CustomEvent('site:track', { detail: { name, params } }).\n        window.addEventListener('site:track', function (evt) {\n          var detail = evt && evt.detail ? evt.detail : {};\n          if (!detail.name) return;\n          trackSiteEvent(detail.name, detail.params || {});\n        });\n\n        // Track outbound link clicks globally.\n        document.addEventListener('click', function (evt) {\n          var target = evt.target;\n          if (!target || typeof target.closest !== 'function') return;\n          var link = target.closest('a[href]');\n          if (!link) return;\n          if (link.hasAttribute('data-no-track')) return;\n          var href = link.getAttribute('href') || '';\n          if (!href || href[0] === '#' || href.indexOf('javascript:') === 0) return;\n          var isAbsolute = /^https?:\\/\\//i.test(href);\n          if (!isAbsolute) return;\n          try {\n            var url = new URL(href, window.location.href);\n            if (url.host === window.location.host) return;\n            trackSiteEvent('outbound_click', {\n              link_url: url.href,\n              link_host: url.host,\n              link_text: (link.textContent || '').trim().slice(0, 80),\n              page_path: window.location.pathname,\n            });\n          } catch (e) {}\n        }, { passive: true });\n      })();\n    </script><title>", "</title>", "", '</head> <body class="bg-paper text-ink font-sans antialiased overflow-x-hidden">  <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:left-3 focus:z-[100] focus:rounded focus:bg-ink focus:px-4 focus:py-2 focus:text-paper focus:no-underline">Skip to main content</a>  <div class="top-wave text-ink/20 dark:text-ink/[0.18]" aria-hidden="true"> <svg viewBox="0 0 1600 360" preserveAspectRatio="xMidYMax slice" xmlns="http://www.w3.org/2000/svg"> <g fill="none" stroke="currentColor" stroke-width="0.8" stroke-linecap="round"> <path d="M-20,80 C200,40 380,140 560,110 C740,80 900,180 1080,150 C1260,120 1440,200 1620,170"></path> <path d="M-20,108 C220,68 400,168 580,138 C760,108 920,208 1100,178 C1280,148 1460,228 1620,198"></path> <path d="M-20,136 C200,96 380,196 560,166 C740,136 900,236 1080,206 C1260,176 1440,256 1620,226"></path> <path d="M-20,164 C220,124 400,224 580,194 C760,164 920,264 1100,234 C1280,204 1460,284 1620,254"></path> <path d="M-20,192 C200,152 380,252 560,222 C740,192 900,292 1080,262 C1260,232 1440,312 1620,282"></path> <path d="M-20,220 C220,180 400,280 580,250 C760,220 920,320 1100,290 C1280,260 1440,340 1620,310"></path> <path d="M-20,248 C200,208 380,308 560,278 C740,248 900,348 1080,318 C1260,288 1440,368 1620,338"></path> <path d="M-20,276 C220,236 400,336 580,306 C760,276 920,376 1100,346 C1280,316 1460,396 1620,366"></path> <path d="M-20,304 C200,264 380,364 560,334 C740,304 900,404 1080,374 C1260,344 1440,424 1620,394"></path> <path d="M-20,332 C220,292 400,392 580,362 C760,332 920,432 1100,402 C1280,372 1460,452 1620,422"></path> </g> </svg> </div> <header class="site-header sticky top-0 z-30"> <nav class="mx-auto flex max-w-5xl items-center justify-between px-6 py-5"> <a', ' class="font-script text-4xl md:text-5xl leading-none text-ink hover:text-accent transition-colors">Flora May</a> <div class="flex items-center gap-3 md:gap-5"> <ul class="hidden md:flex items-baseline gap-6 text-base"> <li> <button type="button" class="ask-mai-nav-link" aria-label="Ask M.ai" data-mai-open data-mai-source="desktop_nav_ask_mai"> <span class="ask-mai-nav-label">Ask</span> <span class="ask-mai-nav-mark"><span class="mai-wordmark">M<span class="mai-dot">.</span>ai</span></span> </button> </li> <li><a', ' class="hover:text-accent">Toybox</a></li> <li><a', ' class="hover:text-accent">Writing</a></li> <li><a', ' class="hover:text-accent">Work</a></li> <li><a', ' class="hover:text-accent">Play</a></li> <li><a', ' class="hover:text-accent">About</a></li> </ul> <button id="theme-toggle" type="button" data-theme-toggle data-theme-toggle-desktop aria-label="Switch to dark mode" aria-pressed="false" title="Switch theme" class="hidden h-8 w-8 items-center justify-center text-ink transition-colors hover:text-accent md:inline-flex"> <svg id="theme-icon-moon" data-theme-icon-dark width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path> </svg> <svg id="theme-icon-sun" data-theme-icon-light hidden width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <circle cx="12" cy="12" r="4"></circle> <line x1="12" y1="2" x2="12" y2="5"></line> <line x1="12" y1="19" x2="12" y2="22"></line> <line x1="2" y1="12" x2="5" y2="12"></line> <line x1="19" y1="12" x2="22" y2="12"></line> <line x1="4.93" y1="4.93" x2="7.05" y2="7.05"></line> <line x1="16.95" y1="16.95" x2="19.07" y2="19.07"></line> <line x1="4.93" y1="19.07" x2="7.05" y2="16.95"></line> <line x1="16.95" y1="7.05" x2="19.07" y2="4.93"></line> </svg> </button> <button id="nav-toggle" class="md:hidden inline-flex h-9 w-9 items-center justify-center text-ink hover:text-accent transition-colors" aria-label="Open menu" aria-expanded="false" aria-controls="mobile-menu"> <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="4" y1="7" x2="20" y2="7"></line> <line x1="4" y1="12" x2="20" y2="12"></line> <line x1="4" y1="17" x2="20" y2="17"></line> </svg> </button> </div> </nav> <!--\n        Mobile menu — an off-canvas drawer that overlays the page rather\n        than pushing the content. Inspired by editorial sites (Bustle et al):\n        big serif typography, generous spacing, dividers between items,\n        social links at the bottom of the panel.\n      --> <div id="mobile-menu" class="mobile-menu fixed inset-0 z-[60] md:hidden" data-open="false" aria-hidden="true"> <button type="button" class="mobile-menu-backdrop absolute inset-0 bg-ink/40 backdrop-blur-[2px]" aria-label="Close menu" tabindex="-1"></button> <aside class="mobile-menu-panel absolute top-0 right-0 h-full w-[85%] max-w-sm bg-paper border-l border-ink/10 shadow-2xl shadow-ink/20 flex flex-col" role="dialog" aria-modal="true" aria-label="Site menu"> <div class="flex items-center justify-between px-6 py-5 border-b border-ink/10"> <span class="font-script text-3xl leading-none text-ink">Flora May</span> <button id="nav-close" type="button" class="inline-flex h-9 w-9 items-center justify-center rounded text-ink hover:text-accent transition-colors" aria-label="Close menu"> <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="6" y1="6" x2="18" y2="18"></line> <line x1="6" y1="18" x2="18" y2="6"></line> </svg> </button> </div> <nav class="flex-1 overflow-y-auto px-6 py-2"> <ul class="flex flex-col divide-y divide-ink/10"> <li class="mobile-menu-item"> <div class="mobile-menu-row"> <button type="button" class="mobile-menu-link ask-mai-mobile-link" aria-label="Ask M.ai" data-mai-open data-mai-source="mobile_nav_ask_mai"> <span class="ask-mai-nav-label">Ask</span> <span class="ask-mai-nav-mark"><span class="mai-wordmark">M<span class="mai-dot">.</span>ai</span></span> </button> </div> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Toybox</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-toybox" aria-label="Show Toybox sections"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-toybox" class="mobile-menu-submenu" hidden> <li><a', ">Playbooks</a></li> <li><a", ">Prompts</a></li> <li><a", ">One-pagers</a></li> <li><a", '>Patterns</a></li> </ul> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Writing</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-writing" aria-label="Show writing categories"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-writing" class="mobile-menu-submenu" hidden> <li><a', ">All writing</a></li> ", ' </ul> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Work</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-work" aria-label="Show selected work"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-work" class="mobile-menu-submenu" hidden> <li><a', ">AWS CodeCatalyst</a></li> <li><a", ">PacBot</a></li> <li><a", ">Vera</a></li> <li><a", ">Mind Elf</a></li> <li><a", '>Elliott Bay Book Co.</a></li> </ul> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Play</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-play" aria-label="Show play projects"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-play" class="mobile-menu-submenu" hidden> <li><a', ">No Dream Is Too Big</a></li> <li><a", ">Pale &amp; Bright</a></li> <li><a", ">Tabulas</a></li> <li><a", '>Art</a></li> </ul> </li> <li class="mobile-menu-item"> <div class="mobile-menu-row"> <a', ` class="mobile-menu-link">About</a> </div> </li> </ul> </nav> <div class="border-t border-ink/10 px-6 py-5 flex flex-col gap-4"> <button id="theme-toggle-mobile" type="button" data-theme-toggle class="mobile-mode-button" aria-label="Switch to dark mode" aria-pressed="false"> <span>Switch to</span> <svg data-theme-icon-dark width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path> </svg> <svg data-theme-icon-light hidden width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <circle cx="12" cy="12" r="4"></circle> <line x1="12" y1="2" x2="12" y2="5"></line> <line x1="12" y1="19" x2="12" y2="22"></line> <line x1="2" y1="12" x2="5" y2="12"></line> <line x1="19" y1="12" x2="22" y2="12"></line> <line x1="4.93" y1="4.93" x2="7.05" y2="7.05"></line> <line x1="16.95" y1="16.95" x2="19.07" y2="19.07"></line> <line x1="4.93" y1="19.07" x2="7.05" y2="16.95"></line> <line x1="16.95" y1="7.05" x2="19.07" y2="4.93"></line> </svg> <span data-theme-toggle-mode>dark mode</span> </button> <div class="grid w-full grid-cols-3 text-center text-xs uppercase tracking-[0.18em] text-muted"> <a href="https://linkedin.com/in/floramaydelacruz" class="hover:text-accent">LinkedIn</a> <a href="https://instagram.com/floramaydc" class="hover:text-accent">Instagram</a> <a href="https://github.com/floramaydc" class="hover:text-accent">GitHub</a> </div> </div> </aside> </div> <script>
        (function () {
          try {
            const btn = document.getElementById('nav-toggle');
            const menu = document.getElementById('mobile-menu');
            if (!btn || !menu) return;
            const closeBtn = document.getElementById('nav-close');
            const backdrop = menu.querySelector('.mobile-menu-backdrop');
            const body = document.body;
            const html = document.documentElement;
            let lockedScrollY = 0;

            function lockPageScroll() {
              if (html.classList.contains('mobile-menu-open')) return;
              lockedScrollY = window.scrollY || window.pageYOffset || 0;
              const scrollbarComp = window.innerWidth - html.clientWidth;
              html.classList.add('mobile-menu-open');
              body.style.position = 'fixed';
              body.style.top = \`-\${lockedScrollY}px\`;
              body.style.left = '0';
              body.style.right = '0';
              body.style.width = '100%';
              if (scrollbarComp > 0) body.style.paddingRight = \`\${scrollbarComp}px\`;
            }

            function unlockPageScroll() {
              if (!html.classList.contains('mobile-menu-open')) return;
              html.classList.remove('mobile-menu-open');
              const top = body.style.top;
              body.style.position = '';
              body.style.top = '';
              body.style.left = '';
              body.style.right = '';
              body.style.width = '';
              body.style.paddingRight = '';
              const y = top ? Math.abs(parseInt(top, 10)) : lockedScrollY;
              window.scrollTo(0, Number.isFinite(y) ? y : 0);
            }

            function setOpen(open) {
              menu.dataset.open = open ? 'true' : 'false';
              menu.setAttribute('aria-hidden', open ? 'false' : 'true');
              btn.setAttribute('aria-expanded', open ? 'true' : 'false');
              btn.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
              if (open) {
                lockPageScroll();
              } else {
                unlockPageScroll();
              }
            }

          // Restore focus to the trigger button when the drawer closes,
          // per the Accessibility Annotation Playbook ("focus on close/
          // dismiss returns to <trigger>"). Without this, focus lands
          // on <body> after Escape or backdrop click.
            const _setOpen = setOpen;
            setOpen = function (open) {
              const wasOpen = menu.dataset.open === 'true';
              _setOpen(open);
              if (wasOpen && !open) {
                // Defer so the visibility transition starts first.
                requestAnimationFrame(() => btn.focus());
              }
            };

            btn.addEventListener('click', () => setOpen(menu.dataset.open !== 'true'));
            closeBtn?.addEventListener('click', () => setOpen(false));
            backdrop?.addEventListener('click', () => setOpen(false));

          // Focus trap: while the drawer is open, keep Tab inside the
          // panel. Without this, Tab leaks to the (visually obscured)
          // page underneath — a WCAG 2.4.3 focus-order fail.
            const panel = menu.querySelector('.mobile-menu-panel');
            menu.addEventListener('keydown', (e) => {
              if (e.key !== 'Tab' || menu.dataset.open !== 'true' || !panel) return;
              const focusables = panel.querySelectorAll(
                'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])'
              );
              const visible = Array.from(focusables).filter((el) => {
                if (el.hasAttribute('hidden')) return false;
                // Skip elements inside a hidden submenu.
                const sub = el.closest('.mobile-menu-submenu');
                if (sub && sub.hidden) return false;
                return true;
              });
              if (visible.length === 0) return;
              const first = visible[0];
              const last = visible[visible.length - 1];
              if (e.shiftKey && document.activeElement === first) {
                e.preventDefault();
                last.focus();
              } else if (!e.shiftKey && document.activeElement === last) {
                e.preventDefault();
                first.focus();
              }
            });

          // Any link or button inside the drawer closes it on activation,
          // so the user lands on the next page with no stale overlay.
            menu.querySelectorAll('a, [data-mai-open]').forEach((el) => {
              el.addEventListener('click', () => setOpen(false));
            });

          // Expandable sections: tap \`+\` to reveal the submenu, tap again
          // (or the rotated \`\\u00d7\`) to collapse it. The label link itself
          // still navigates \\u2014 the \`+\` is a separate hit target.
            menu.querySelectorAll('.mobile-menu-toggle').forEach((toggle) => {
              // Keep the aria-label in sync with the expanded state so
              // screen readers say "Hide …" once the section is open.
              const baseLabel = toggle.getAttribute('aria-label') || '';
              const showLabel = baseLabel;
              const hideLabel = baseLabel.replace(/^Show\\b/, 'Hide');
              toggle.addEventListener('click', (e) => {
                e.stopPropagation();
                const id = toggle.getAttribute('aria-controls');
                const sub = id && document.getElementById(id);
                if (!sub) return;
                const expanded = toggle.getAttribute('aria-expanded') === 'true';
                toggle.setAttribute('aria-expanded', expanded ? 'false' : 'true');
                toggle.setAttribute('aria-label', expanded ? showLabel : hideLabel);
                sub.hidden = expanded;
              });
            });

            document.addEventListener('keydown', (e) => {
              if (e.key === 'Escape' && menu.dataset.open === 'true') setOpen(false);
            });

            window.addEventListener('resize', () => {
              if (window.innerWidth >= 768 && menu.dataset.open === 'true') {
                setOpen(false);
              }
            });
          } catch (e) {
            // Prevent nav script failures from breaking page interactivity.
          }
        })();
      </script>  </header> <main id="main-content" tabindex="-1"`, ' class="relative z-10 mx-auto max-w-5xl overflow-x-hidden px-6 py-12 focus:outline-none"> ', ' </main> <footer class="relative z-10 mt-24 border-t border-ink/10 bg-paper"> <div class="mx-auto flex max-w-5xl flex-col items-start gap-4 px-6 py-8 text-sm text-muted md:flex-row md:items-center md:justify-between md:gap-0"> <span>© ', ' Flora May dela Cruz</span> <div class="flex flex-wrap gap-x-4 gap-y-2"> <a href="https://linkedin.com/in/floramaydelacruz" class="hover:text-accent">LinkedIn</a> <a href="https://instagram.com/floramaydc" class="hover:text-accent">Instagram</a> <a href="https://github.com/floramaydc" class="hover:text-accent">GitHub</a> </div> </div> </footer> ', " </body></html>"], ['<html lang="en"> <head><meta charset="UTF-8"><meta name="description"', '><meta name="viewport" content="width=device-width, initial-scale=1">', '<link rel="canonical"', '><link rel="alternate" type="application/rss+xml" title="Flora May dela Cruz — Writing"', '><!-- Open Graph --><meta property="og:type"', '><meta property="og:site_name" content="Flora May dela Cruz"><meta property="og:title"', '><meta property="og:description"', '><meta property="og:url"', '><meta property="og:image"', '><!-- Twitter --><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title"', '><meta name="twitter:description"', '><meta name="twitter:image"', '><link rel="icon" type="image/svg+xml"', '><meta name="generator"', "><script>\n      (function () {\n        try {\n          var stored = localStorage.getItem('theme');\n          var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;\n          var dark = stored ? stored === 'dark' : prefersDark;\n          if (dark) document.documentElement.classList.add('dark');\n          requestAnimationFrame(function () {\n            document.documentElement.classList.add('theme-ready');\n          });\n        } catch (e) {}\n      })();\n    </script><script>\n      (function () {\n        try {\n          var hasVersionedOpen = false;\n          for (var i = 0; i < sessionStorage.length; i++) {\n            var key = sessionStorage.key(i) || '';\n            if (/^mai:.*:open$/.test(key) && sessionStorage.getItem(key) === 'true') {\n              hasVersionedOpen = true;\n              break;\n            }\n          }\n          if (sessionStorage.getItem('mai-open') === 'true' || hasVersionedOpen) {\n            document.documentElement.classList.add('mai-open');\n          }\n        } catch (e) {}\n      })();\n    </script><script>\n      (function () {\n        function clearStuckMaiOpen() {\n          try {\n            var html = document.documentElement;\n            if (!html.classList.contains('mai-open')) return;\n            var panel = document.getElementById('mai-panel');\n            var isVisible = false;\n            if (panel) {\n              var style = window.getComputedStyle(panel);\n              isVisible = !panel.hasAttribute('hidden') && style.display !== 'none' && style.visibility !== 'hidden';\n            }\n            if (!isVisible) {\n              html.classList.remove('mai-open');\n              html.classList.remove('mai-no-scroll');\n              sessionStorage.removeItem('mai-open');\n              for (var i = sessionStorage.length - 1; i >= 0; i--) {\n                var key = sessionStorage.key(i) || '';\n                if (/^mai:.*:open$/.test(key)) sessionStorage.removeItem(key);\n              }\n            }\n          } catch (e) {}\n        }\n\n        if (document.readyState === 'loading') {\n          document.addEventListener('DOMContentLoaded', function () {\n            setTimeout(clearStuckMaiOpen, 250);\n          });\n        } else {\n          setTimeout(clearStuckMaiOpen, 250);\n        }\n      })();\n    </script><script>\n      (function () {\n        var header;\n        var ticking = false;\n        var thresholdEnter = 28;\n        var thresholdExit = 10;\n        var isScrolled = false;\n        function update() {\n          ticking = false;\n          if (!header) return;\n          var y = window.scrollY || 0;\n          if (!isScrolled && y > thresholdEnter) {\n            isScrolled = true;\n            header.classList.add('is-scrolled');\n          } else if (isScrolled && y < thresholdExit) {\n            isScrolled = false;\n            header.classList.remove('is-scrolled');\n          }\n        }\n        function onScroll() {\n          if (!ticking) {\n            requestAnimationFrame(update);\n            ticking = true;\n          }\n        }\n        function init() {\n          header = document.querySelector('.site-header');\n          update();\n          window.addEventListener('scroll', onScroll, { passive: true });\n        }\n        if (document.readyState === 'loading') {\n          document.addEventListener('DOMContentLoaded', init);\n        } else {\n          init();\n        }\n      })();\n    </script><script>\n      (function () {\n        function initThemeToggles() {\n          try {\n          var toggles = Array.prototype.slice.call(document.querySelectorAll('[data-theme-toggle]'));\n          if (!toggles.length) return;\n\n          function syncThemeToggleUi() {\n            var isDark = document.documentElement.classList.contains('dark');\n            var nextAria = isDark ? 'Switch to light mode' : 'Switch to dark mode';\n            var nextMode = isDark ? 'light mode' : 'dark mode';\n            toggles.forEach(function (btn) {\n              btn.setAttribute('aria-label', nextAria);\n              btn.setAttribute('aria-pressed', isDark ? 'true' : 'false');\n              var label = btn.querySelector('[data-theme-toggle-label]');\n              if (label) label.textContent = 'Switch to ' + nextMode;\n              var mode = btn.querySelector('[data-theme-toggle-mode]');\n              if (mode) mode.textContent = nextMode;\n              var darkIcon = btn.querySelector('[data-theme-icon-dark]');\n              var lightIcon = btn.querySelector('[data-theme-icon-light]');\n              if (darkIcon) {\n                if (isDark) darkIcon.setAttribute('hidden', '');\n                else darkIcon.removeAttribute('hidden');\n              }\n              if (lightIcon) {\n                if (!isDark) lightIcon.setAttribute('hidden', '');\n                else lightIcon.removeAttribute('hidden');\n              }\n            });\n          }\n\n          syncThemeToggleUi();\n\n          toggles.forEach(function (btn) {\n            if (btn.dataset.themeToggleBound === 'true') return;\n            btn.dataset.themeToggleBound = 'true';\n            btn.addEventListener('click', function () {\n              var isDark = document.documentElement.classList.toggle('dark');\n              try {\n                localStorage.setItem('theme', isDark ? 'dark' : 'light');\n              } catch (e) {}\n              syncThemeToggleUi();\n            });\n          });\n          } catch (e) {}\n        }\n\n        if (document.readyState === 'loading') {\n          document.addEventListener('DOMContentLoaded', initThemeToggles);\n        } else {\n          initThemeToggles();\n        }\n      })();\n    </script>", "<script>\n      (function () {\n        function trackSiteEvent(name, params) {\n          try {\n            if (!name || typeof window.gtag !== 'function') return;\n            window.gtag('event', name, params || {});\n          } catch (e) {}\n        }\n\n        // Shared helper used by inline component scripts.\n        window.trackSiteEvent = trackSiteEvent;\n\n        // Components can emit CustomEvent('site:track', { detail: { name, params } }).\n        window.addEventListener('site:track', function (evt) {\n          var detail = evt && evt.detail ? evt.detail : {};\n          if (!detail.name) return;\n          trackSiteEvent(detail.name, detail.params || {});\n        });\n\n        // Track outbound link clicks globally.\n        document.addEventListener('click', function (evt) {\n          var target = evt.target;\n          if (!target || typeof target.closest !== 'function') return;\n          var link = target.closest('a[href]');\n          if (!link) return;\n          if (link.hasAttribute('data-no-track')) return;\n          var href = link.getAttribute('href') || '';\n          if (!href || href[0] === '#' || href.indexOf('javascript:') === 0) return;\n          var isAbsolute = /^https?:\\\\/\\\\//i.test(href);\n          if (!isAbsolute) return;\n          try {\n            var url = new URL(href, window.location.href);\n            if (url.host === window.location.host) return;\n            trackSiteEvent('outbound_click', {\n              link_url: url.href,\n              link_host: url.host,\n              link_text: (link.textContent || '').trim().slice(0, 80),\n              page_path: window.location.pathname,\n            });\n          } catch (e) {}\n        }, { passive: true });\n      })();\n    </script><title>", "</title>", "", '</head> <body class="bg-paper text-ink font-sans antialiased overflow-x-hidden">  <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:left-3 focus:z-[100] focus:rounded focus:bg-ink focus:px-4 focus:py-2 focus:text-paper focus:no-underline">Skip to main content</a>  <div class="top-wave text-ink/20 dark:text-ink/[0.18]" aria-hidden="true"> <svg viewBox="0 0 1600 360" preserveAspectRatio="xMidYMax slice" xmlns="http://www.w3.org/2000/svg"> <g fill="none" stroke="currentColor" stroke-width="0.8" stroke-linecap="round"> <path d="M-20,80 C200,40 380,140 560,110 C740,80 900,180 1080,150 C1260,120 1440,200 1620,170"></path> <path d="M-20,108 C220,68 400,168 580,138 C760,108 920,208 1100,178 C1280,148 1460,228 1620,198"></path> <path d="M-20,136 C200,96 380,196 560,166 C740,136 900,236 1080,206 C1260,176 1440,256 1620,226"></path> <path d="M-20,164 C220,124 400,224 580,194 C760,164 920,264 1100,234 C1280,204 1460,284 1620,254"></path> <path d="M-20,192 C200,152 380,252 560,222 C740,192 900,292 1080,262 C1260,232 1440,312 1620,282"></path> <path d="M-20,220 C220,180 400,280 580,250 C760,220 920,320 1100,290 C1280,260 1440,340 1620,310"></path> <path d="M-20,248 C200,208 380,308 560,278 C740,248 900,348 1080,318 C1260,288 1440,368 1620,338"></path> <path d="M-20,276 C220,236 400,336 580,306 C760,276 920,376 1100,346 C1280,316 1460,396 1620,366"></path> <path d="M-20,304 C200,264 380,364 560,334 C740,304 900,404 1080,374 C1260,344 1440,424 1620,394"></path> <path d="M-20,332 C220,292 400,392 580,362 C760,332 920,432 1100,402 C1280,372 1460,452 1620,422"></path> </g> </svg> </div> <header class="site-header sticky top-0 z-30"> <nav class="mx-auto flex max-w-5xl items-center justify-between px-6 py-5"> <a', ' class="font-script text-4xl md:text-5xl leading-none text-ink hover:text-accent transition-colors">Flora May</a> <div class="flex items-center gap-3 md:gap-5"> <ul class="hidden md:flex items-baseline gap-6 text-base"> <li> <button type="button" class="ask-mai-nav-link" aria-label="Ask M.ai" data-mai-open data-mai-source="desktop_nav_ask_mai"> <span class="ask-mai-nav-label">Ask</span> <span class="ask-mai-nav-mark"><span class="mai-wordmark">M<span class="mai-dot">.</span>ai</span></span> </button> </li> <li><a', ' class="hover:text-accent">Toybox</a></li> <li><a', ' class="hover:text-accent">Writing</a></li> <li><a', ' class="hover:text-accent">Work</a></li> <li><a', ' class="hover:text-accent">Play</a></li> <li><a', ' class="hover:text-accent">About</a></li> </ul> <button id="theme-toggle" type="button" data-theme-toggle data-theme-toggle-desktop aria-label="Switch to dark mode" aria-pressed="false" title="Switch theme" class="hidden h-8 w-8 items-center justify-center text-ink transition-colors hover:text-accent md:inline-flex"> <svg id="theme-icon-moon" data-theme-icon-dark width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path> </svg> <svg id="theme-icon-sun" data-theme-icon-light hidden width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <circle cx="12" cy="12" r="4"></circle> <line x1="12" y1="2" x2="12" y2="5"></line> <line x1="12" y1="19" x2="12" y2="22"></line> <line x1="2" y1="12" x2="5" y2="12"></line> <line x1="19" y1="12" x2="22" y2="12"></line> <line x1="4.93" y1="4.93" x2="7.05" y2="7.05"></line> <line x1="16.95" y1="16.95" x2="19.07" y2="19.07"></line> <line x1="4.93" y1="19.07" x2="7.05" y2="16.95"></line> <line x1="16.95" y1="7.05" x2="19.07" y2="4.93"></line> </svg> </button> <button id="nav-toggle" class="md:hidden inline-flex h-9 w-9 items-center justify-center text-ink hover:text-accent transition-colors" aria-label="Open menu" aria-expanded="false" aria-controls="mobile-menu"> <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="4" y1="7" x2="20" y2="7"></line> <line x1="4" y1="12" x2="20" y2="12"></line> <line x1="4" y1="17" x2="20" y2="17"></line> </svg> </button> </div> </nav> <!--\n        Mobile menu — an off-canvas drawer that overlays the page rather\n        than pushing the content. Inspired by editorial sites (Bustle et al):\n        big serif typography, generous spacing, dividers between items,\n        social links at the bottom of the panel.\n      --> <div id="mobile-menu" class="mobile-menu fixed inset-0 z-[60] md:hidden" data-open="false" aria-hidden="true"> <button type="button" class="mobile-menu-backdrop absolute inset-0 bg-ink/40 backdrop-blur-[2px]" aria-label="Close menu" tabindex="-1"></button> <aside class="mobile-menu-panel absolute top-0 right-0 h-full w-[85%] max-w-sm bg-paper border-l border-ink/10 shadow-2xl shadow-ink/20 flex flex-col" role="dialog" aria-modal="true" aria-label="Site menu"> <div class="flex items-center justify-between px-6 py-5 border-b border-ink/10"> <span class="font-script text-3xl leading-none text-ink">Flora May</span> <button id="nav-close" type="button" class="inline-flex h-9 w-9 items-center justify-center rounded text-ink hover:text-accent transition-colors" aria-label="Close menu"> <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="6" y1="6" x2="18" y2="18"></line> <line x1="6" y1="18" x2="18" y2="6"></line> </svg> </button> </div> <nav class="flex-1 overflow-y-auto px-6 py-2"> <ul class="flex flex-col divide-y divide-ink/10"> <li class="mobile-menu-item"> <div class="mobile-menu-row"> <button type="button" class="mobile-menu-link ask-mai-mobile-link" aria-label="Ask M.ai" data-mai-open data-mai-source="mobile_nav_ask_mai"> <span class="ask-mai-nav-label">Ask</span> <span class="ask-mai-nav-mark"><span class="mai-wordmark">M<span class="mai-dot">.</span>ai</span></span> </button> </div> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Toybox</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-toybox" aria-label="Show Toybox sections"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-toybox" class="mobile-menu-submenu" hidden> <li><a', ">Playbooks</a></li> <li><a", ">Prompts</a></li> <li><a", ">One-pagers</a></li> <li><a", '>Patterns</a></li> </ul> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Writing</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-writing" aria-label="Show writing categories"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-writing" class="mobile-menu-submenu" hidden> <li><a', ">All writing</a></li> ", ' </ul> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Work</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-work" aria-label="Show selected work"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-work" class="mobile-menu-submenu" hidden> <li><a', ">AWS CodeCatalyst</a></li> <li><a", ">PacBot</a></li> <li><a", ">Vera</a></li> <li><a", ">Mind Elf</a></li> <li><a", '>Elliott Bay Book Co.</a></li> </ul> </li> <li class="mobile-menu-item" data-expandable="true"> <div class="mobile-menu-row"> <a', ' class="mobile-menu-link">Play</a> <button type="button" class="mobile-menu-toggle" aria-expanded="false" aria-controls="submenu-play" aria-label="Show play projects"> <svg class="mobile-menu-plus" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"> <line x1="12" y1="5" x2="12" y2="19"></line> <line x1="5" y1="12" x2="19" y2="12"></line> </svg> </button> </div> <ul id="submenu-play" class="mobile-menu-submenu" hidden> <li><a', ">No Dream Is Too Big</a></li> <li><a", ">Pale &amp; Bright</a></li> <li><a", ">Tabulas</a></li> <li><a", '>Art</a></li> </ul> </li> <li class="mobile-menu-item"> <div class="mobile-menu-row"> <a', ` class="mobile-menu-link">About</a> </div> </li> </ul> </nav> <div class="border-t border-ink/10 px-6 py-5 flex flex-col gap-4"> <button id="theme-toggle-mobile" type="button" data-theme-toggle class="mobile-mode-button" aria-label="Switch to dark mode" aria-pressed="false"> <span>Switch to</span> <svg data-theme-icon-dark width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path> </svg> <svg data-theme-icon-light hidden width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"> <circle cx="12" cy="12" r="4"></circle> <line x1="12" y1="2" x2="12" y2="5"></line> <line x1="12" y1="19" x2="12" y2="22"></line> <line x1="2" y1="12" x2="5" y2="12"></line> <line x1="19" y1="12" x2="22" y2="12"></line> <line x1="4.93" y1="4.93" x2="7.05" y2="7.05"></line> <line x1="16.95" y1="16.95" x2="19.07" y2="19.07"></line> <line x1="4.93" y1="19.07" x2="7.05" y2="16.95"></line> <line x1="16.95" y1="7.05" x2="19.07" y2="4.93"></line> </svg> <span data-theme-toggle-mode>dark mode</span> </button> <div class="grid w-full grid-cols-3 text-center text-xs uppercase tracking-[0.18em] text-muted"> <a href="https://linkedin.com/in/floramaydelacruz" class="hover:text-accent">LinkedIn</a> <a href="https://instagram.com/floramaydc" class="hover:text-accent">Instagram</a> <a href="https://github.com/floramaydc" class="hover:text-accent">GitHub</a> </div> </div> </aside> </div> <script>
        (function () {
          try {
            const btn = document.getElementById('nav-toggle');
            const menu = document.getElementById('mobile-menu');
            if (!btn || !menu) return;
            const closeBtn = document.getElementById('nav-close');
            const backdrop = menu.querySelector('.mobile-menu-backdrop');
            const body = document.body;
            const html = document.documentElement;
            let lockedScrollY = 0;

            function lockPageScroll() {
              if (html.classList.contains('mobile-menu-open')) return;
              lockedScrollY = window.scrollY || window.pageYOffset || 0;
              const scrollbarComp = window.innerWidth - html.clientWidth;
              html.classList.add('mobile-menu-open');
              body.style.position = 'fixed';
              body.style.top = \\\`-\\\${lockedScrollY}px\\\`;
              body.style.left = '0';
              body.style.right = '0';
              body.style.width = '100%';
              if (scrollbarComp > 0) body.style.paddingRight = \\\`\\\${scrollbarComp}px\\\`;
            }

            function unlockPageScroll() {
              if (!html.classList.contains('mobile-menu-open')) return;
              html.classList.remove('mobile-menu-open');
              const top = body.style.top;
              body.style.position = '';
              body.style.top = '';
              body.style.left = '';
              body.style.right = '';
              body.style.width = '';
              body.style.paddingRight = '';
              const y = top ? Math.abs(parseInt(top, 10)) : lockedScrollY;
              window.scrollTo(0, Number.isFinite(y) ? y : 0);
            }

            function setOpen(open) {
              menu.dataset.open = open ? 'true' : 'false';
              menu.setAttribute('aria-hidden', open ? 'false' : 'true');
              btn.setAttribute('aria-expanded', open ? 'true' : 'false');
              btn.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
              if (open) {
                lockPageScroll();
              } else {
                unlockPageScroll();
              }
            }

          // Restore focus to the trigger button when the drawer closes,
          // per the Accessibility Annotation Playbook ("focus on close/
          // dismiss returns to <trigger>"). Without this, focus lands
          // on <body> after Escape or backdrop click.
            const _setOpen = setOpen;
            setOpen = function (open) {
              const wasOpen = menu.dataset.open === 'true';
              _setOpen(open);
              if (wasOpen && !open) {
                // Defer so the visibility transition starts first.
                requestAnimationFrame(() => btn.focus());
              }
            };

            btn.addEventListener('click', () => setOpen(menu.dataset.open !== 'true'));
            closeBtn?.addEventListener('click', () => setOpen(false));
            backdrop?.addEventListener('click', () => setOpen(false));

          // Focus trap: while the drawer is open, keep Tab inside the
          // panel. Without this, Tab leaks to the (visually obscured)
          // page underneath — a WCAG 2.4.3 focus-order fail.
            const panel = menu.querySelector('.mobile-menu-panel');
            menu.addEventListener('keydown', (e) => {
              if (e.key !== 'Tab' || menu.dataset.open !== 'true' || !panel) return;
              const focusables = panel.querySelectorAll(
                'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])'
              );
              const visible = Array.from(focusables).filter((el) => {
                if (el.hasAttribute('hidden')) return false;
                // Skip elements inside a hidden submenu.
                const sub = el.closest('.mobile-menu-submenu');
                if (sub && sub.hidden) return false;
                return true;
              });
              if (visible.length === 0) return;
              const first = visible[0];
              const last = visible[visible.length - 1];
              if (e.shiftKey && document.activeElement === first) {
                e.preventDefault();
                last.focus();
              } else if (!e.shiftKey && document.activeElement === last) {
                e.preventDefault();
                first.focus();
              }
            });

          // Any link or button inside the drawer closes it on activation,
          // so the user lands on the next page with no stale overlay.
            menu.querySelectorAll('a, [data-mai-open]').forEach((el) => {
              el.addEventListener('click', () => setOpen(false));
            });

          // Expandable sections: tap \\\`+\\\` to reveal the submenu, tap again
          // (or the rotated \\\`\\\\u00d7\\\`) to collapse it. The label link itself
          // still navigates \\\\u2014 the \\\`+\\\` is a separate hit target.
            menu.querySelectorAll('.mobile-menu-toggle').forEach((toggle) => {
              // Keep the aria-label in sync with the expanded state so
              // screen readers say "Hide …" once the section is open.
              const baseLabel = toggle.getAttribute('aria-label') || '';
              const showLabel = baseLabel;
              const hideLabel = baseLabel.replace(/^Show\\\\b/, 'Hide');
              toggle.addEventListener('click', (e) => {
                e.stopPropagation();
                const id = toggle.getAttribute('aria-controls');
                const sub = id && document.getElementById(id);
                if (!sub) return;
                const expanded = toggle.getAttribute('aria-expanded') === 'true';
                toggle.setAttribute('aria-expanded', expanded ? 'false' : 'true');
                toggle.setAttribute('aria-label', expanded ? showLabel : hideLabel);
                sub.hidden = expanded;
              });
            });

            document.addEventListener('keydown', (e) => {
              if (e.key === 'Escape' && menu.dataset.open === 'true') setOpen(false);
            });

            window.addEventListener('resize', () => {
              if (window.innerWidth >= 768 && menu.dataset.open === 'true') {
                setOpen(false);
              }
            });
          } catch (e) {
            // Prevent nav script failures from breaking page interactivity.
          }
        })();
      </script>  </header> <main id="main-content" tabindex="-1"`, ' class="relative z-10 mx-auto max-w-5xl overflow-x-hidden px-6 py-12 focus:outline-none"> ', ' </main> <footer class="relative z-10 mt-24 border-t border-ink/10 bg-paper"> <div class="mx-auto flex max-w-5xl flex-col items-start gap-4 px-6 py-8 text-sm text-muted md:flex-row md:items-center md:justify-between md:gap-0"> <span>© ', ' Flora May dela Cruz</span> <div class="flex flex-wrap gap-x-4 gap-y-2"> <a href="https://linkedin.com/in/floramaydelacruz" class="hover:text-accent">LinkedIn</a> <a href="https://instagram.com/floramaydc" class="hover:text-accent">Instagram</a> <a href="https://github.com/floramaydc" class="hover:text-accent">GitHub</a> </div> </div> </footer> ', " </body></html>"])), addAttribute(description, "content"), gscVerification, addAttribute(canonical, "href"), addAttribute(`${siteUrl}/rss.xml`, "href"), addAttribute(ogType, "content"), addAttribute(title, "content"), addAttribute(description, "content"), addAttribute(canonical, "content"), addAttribute(ogImageUrl, "content"), addAttribute(title, "content"), addAttribute(description, "content"), addAttribute(ogImageUrl, "content"), addAttribute(`${base}favicon.svg`, "href"), addAttribute(Astro2.generator, "content"), renderTemplate(_a || (_a = __template(["<script>(function(){", "\n        (function () {\n          var loaded = false;\n          var pendingLoad = false;\n\n          window.dataLayer = window.dataLayer || [];\n          function gtag() {\n            dataLayer.push(arguments);\n          }\n          window.gtag = gtag;\n          gtag('js', new Date());\n\n          function loadGtag() {\n            if (loaded || pendingLoad) return;\n            pendingLoad = true;\n            var script = document.createElement('script');\n            script.async = true;\n            script.src = 'https://www.googletagmanager.com/gtag/js?id=' + encodeURIComponent(gaMeasurementId);\n            script.onload = function () {\n              loaded = true;\n              pendingLoad = false;\n              gtag('config', gaMeasurementId, {\n                anonymize_ip: true,\n                transport_type: 'beacon',\n              });\n            };\n            script.onerror = function () {\n              pendingLoad = false;\n            };\n            document.head.appendChild(script);\n          }\n\n          var loadedByInteraction = false;\n          function loadOnce() {\n            if (loadedByInteraction) return;\n            loadedByInteraction = true;\n            loadGtag();\n          }\n\n          window.addEventListener('pointerdown', loadOnce, { once: true, passive: true });\n          window.addEventListener('keydown', loadOnce, { once: true });\n          window.addEventListener('scroll', loadOnce, { once: true, passive: true });\n\n          // Fallback: if no interaction happens, still load analytics soon after paint.\n          window.setTimeout(loadGtag, 4000);\n        })();\n      })();</script>"])), defineScriptVars({ gaMeasurementId })), title, jsonLd && renderTemplate(_b || (_b = __template(['<script type="application/ld+json">', "</script>"])), unescapeHTML(JSON.stringify(jsonLd))), renderHead(), addAttribute(base, "href"), addAttribute(`${base}toybox`, "href"), addAttribute(`${base}writing`, "href"), addAttribute(`${base}work`, "href"), addAttribute(`${base}play`, "href"), addAttribute(`${base}about`, "href"), addAttribute(`${base}toybox`, "href"), addAttribute(`${base}toybox#playbooks`, "href"), addAttribute(`${base}toybox#prompts`, "href"), addAttribute(`${base}toybox#one-pagers`, "href"), addAttribute(`${base}toybox#patterns`, "href"), addAttribute(`${base}writing`, "href"), addAttribute(`${base}writing`, "href"), writingCategories.map((category) => renderTemplate`<li><a${addAttribute(`${base}writing?category=${encodeURIComponent(category)}`, "href")}>${category}</a></li>`), addAttribute(`${base}work`, "href"), addAttribute(`${base}work/aws-code-catalyst`, "href"), addAttribute(`${base}work/pacbot`, "href"), addAttribute(`${base}work/vera`, "href"), addAttribute(`${base}work/mindelf`, "href"), addAttribute(`${base}work/ebbc`, "href"), addAttribute(`${base}play`, "href"), addAttribute(`${base}play/no-dream-is-too-big`, "href"), addAttribute(`${base}play/pale-and-bright`, "href"), addAttribute(`${base}play/tabulas`, "href"), addAttribute(`${base}play/art`, "href"), addAttribute(`${base}about`, "href"), addAttribute(title, "aria-label"), renderSlot($$result, $$slots["default"]), (/* @__PURE__ */ new Date()).getFullYear(), renderComponent($$result, "MAi", $$MAi, {}));
}, "/private/tmp/flora-component-build/src/layouts/BaseLayout.astro", void 0);

export { $$BaseLayout as $ };
